# Requisitos del módulo OERManager

> Fuente única de verdad de requisitos. IDs correlativos estables (`RF-NNN`, `NFR-NNN`, `PEND-NNN`): no se reutilizan ni se renumeran. Estados: `propuesto` / `aceptado` / `[PENDIENTE]`. Trazabilidad en [traceability.md](traceability.md).

## 1. Parámetros del entorno y decisiones bloqueantes

Los marcados `[PENDIENTE]` **no están decididos por el propietario**: no rellenar con conjeturas. Los marcados `Resuelto` (bloque §B del propietario, 2026-06-12) conservan su fila por trazabilidad.

| ID | Hueco | Valor actual | Bloquea |
| --- | --- | --- | --- |
| PEND-001 | Nombre CamelCase definitivo del módulo | **Resuelto (2026-06-12):** `OERManager`; namespace PHP `OERManager`; carpeta de vistas `view/oer-manager/`; raíz del proyecto `omeka-s-OERManager` | — (ya no bloquea) |
| PEND-002 | Versión exacta de Omeka-S y PHP del entorno + `omeka_version_constraint` | **Resuelto (2026-06-12):** Omeka-S **4.2**, PHP **8.4** (runtime/contenedor); `omeka_version_constraint = ^4.2.0`; `composer.json` declara `php >= 8.4`. El host usa PHP 8.5 solo para tooling de lint: no usar sintaxis 8.5 en el código del módulo | — (ya no bloquea; FASE 1 desbloqueada) |
| PEND-003 | Cómo se levanta Omeka en local | **Resuelto (2026-06-12):** Docker — `docker compose up -d` / `docker compose down`. El código vive en el host mapeado por volumen; lint/test se ejecutan **en el host, sin** `docker compose exec`. La configuración Docker **no forma parte del módulo** (no crearla ni editarla) | — (ya no bloquea) |
| PEND-004 | Comandos exactos de build/lint/test | **Resuelto (2026-06-12):** lint `make lint` (PHPCS **PSR-12**, no PSR2); fix `make fix` (phpcbf); test `make test` (PHPUnit con `test/phpunit.xml`, que aún no existe). **Sin análisis estático** (no introducir PHPStan/Psalm). Nota: Makefile del propietario en el repo (no regenerar); `composer.json` + `vendor/` instalados el 2026-06-12 con autorización del propietario; `make lint` pasa en limpio y el **Stop hook está activo**. `make test` diferido (PHPUnit sin instalar: su versión depende de PEND-002; `test/phpunit.xml` en fases posteriores) | — (solo queda el hook de test, ver TASK-008) |
| PEND-009 | Metadatos de `composer.json`: nombre de paquete (puesto provisionalmente `ate/oer-manager` por el agente) y licencia del módulo (campo `license` omitido a propósito) | **Resuelto (2026-06-15, ADR-0003):** paquete `ate/oer-manager`; licencia `GPL-3.0-or-later` aplicada a `composer.json` y `config/module.ini`. Cabeceras de licencia en código: follow-up; `author` del módulo aún sin confirmar | — (ya no bloquea `make package`) |
| PEND-005 | Mapeo property RDF → tipo de alineamiento curricular y tags | **Resuelto (2026-06-15, ADR-0004):** etapa `lrmi:educationalLevel`, materia `schema:about`, saberes `lrmi:teaches`, criterios `lrmi:assesses`, eje temático/tags `dcterms:relation` (controlado por `schema:DefinedTermSet` en config), proyecto `schema:isPartOf`, licencia `dcterms:rights` (CustomVocab). Todo `resource:item` salvo licencia (literal). Confirmado contra la instalación real | — (ya no bloquea; reglas de negocio → PEND-007) |
| PEND-006 | Decisión de auditoría de curación (A: History Log / B: value annotations / C: log fichero / D: riesgo aceptado) | **Resuelto (2026-06-15, ADR-0002):** auditoría RDF nativa vía **value annotations** con `dcterms` (quién `dcterms:contributor`, cuándo `dcterms:modified`, qué `dcterms:provenance`); sin módulo Log ni tablas propias. Visibilidad fuera del alcance de auditoría. Properties exactas → PEND-005 | — (mecanismo decidido; TASK-007 pasa a depender de PEND-005 y TASK-004) |
| PEND-007 | RF/NFR detallados (campos de gestión, cardinalidad de alineamiento, vocabulario de tags, acciones de curación y roles, reglas de integridad, columnas de la vista maestra, catálogo de estadísticas, matriz rol×acción, rendimiento, i18n, accesibilidad). Inputs 2026-06-15 (ADR-0004): proyecto `schema:isPartOf` = acción de gestor aparte; valorar plantilla REA (`resource_template`) para obligatoriedad; la config del módulo necesita el `schema:DefinedTermSet` de ejes y el `CustomVocab` de licencias | **Resuelto (2026-06-16):** punto 1 (columnas, filtros y panel de detalle de la vista maestra v1) fijado y **ya implementado** en ADR-0005 (`docs/superpowers/specs/2026-06-15-vista-maestra-design.md`, TASK-003); el resto (reglas del re-catalogador, matriz rol×acción, integridad, plantilla REA, estadísticas, rendimiento, i18n, accesibilidad) recogido en RF-002…RF-007 y NFR-003…NFR-006 abajo; mecanismo de localización de los `DefinedTermSet` raíz fijado en ADR-0006 | — (ya no bloquea; TASK-003 hecha v1, TASK-004/005/006 desbloqueadas) |
| PEND-008 | Skills preexistentes `rea-validacion-legal` y `spreadsheet-analyzer`: no localizadas en el repo ni a nivel de usuario en FASE 0 | **Resuelto (2026-06-15):** descartadas — no forman parte del módulo; se retiran del gobierno. Si en el futuro hace falta validación legal o QA de exports, se decidirá entonces | — (ya no bloquea) |
| PEND-010 | Infraestructura de embeddings para la recuperación semántica (TASK-016): proveedor y endpoint. Anthropic no ofrece embeddings nativos; un proveedor OpenAI-compatible sí (`/embeddings`). Decidir proveedor, modelo, dimensión y almacenamiento del índice (fichero, sin tablas Doctrine, NFR-002). | `[PENDIENTE]` | TASK-016 |
| PEND-011 | **Contenido** del CustomVocab de licencias (`dcterms:rights`): qué valores exactos, si llevan versión (`CC BY-SA 4.0` frente al actual `ccbysa`) y si se acompañan de la URI canónica en `dcterms:license`. Hoy el único valor del catálogo no permite distinguir versión ni jurisdicción, y hay **0 URIs** en todo el corpus. **Mecanismo resuelto (2026-07-28, ADR-0013):** CustomVocab identificado por el setting `oermanager_licence_vocab_id`, sembrado por el módulo bajo demanda desde la configuración (nunca en `install()`), editable después por el admin y jamás sobrescrito en `upgrade()`. Queda abierto **solo el contenido**. | `[PENDIENTE]` (solo contenido) | TASK-028, RF-015 |
| PEND-012 | **Plantilla REA** (`resource_template`). **Resuelto en mecanismo (2026-07-28, ADR-0013, «sembrar, no poseer»):** conviene crearla —es lo que hace que el **editor nativo de Omeka** exija los campos correctos, atacando el 0 % de cobertura en el origen—; la crea el módulo **bajo demanda** desde la configuración, nunca en `install()` y nunca antes de decidir sus campos obligatorios; se identifica por el setting `oermanager_rea_template_id`, que además **desactiva el riesgo** al permitir a `IntegrityChecker` distinguir «la plantilla REA» de «otra plantilla» (esta última aplica el mínimo y avisa); asignarla a los REA existentes es **acción de lote con preview y confirmación**, nunca automática, y su criterio de aceptación es que **los 18 avisos de «sin licencia» sigan apareciendo** después. Queda abierto **solo qué campos marca como obligatorios**, que es decisión editorial del propietario. | `[PENDIENTE]` (solo campos obligatorios) | TASK-028 |
| PEND-013 | ¿Se muestra la **justificación pedagógica de la IA** en el historial de curación de lo ya confirmado? Es un caso distinto del panel de propuesta, donde la decisión vigente de TASK-023 (no mostrarla, para no anclar al curador antes de decidir) **no se cuestiona**. 94 de las 340 anotaciones la tienen; sin ella el historial dice qué se cambió pero nunca por qué. | `[PENDIENTE]` | TASK-028 |

## 2. Requisitos funcionales (RF)

| ID | Descripción | Prioridad | Estado | Criterio de aceptación |
| --- | --- | --- | --- | --- |
| RF-001 | El módulo gestiona items de clase `lrmi:LearningResource`: la vista maestra y todas las operaciones parten de ese filtro | Alta | aceptado | Listado en panel admin muestra solo items `lrmi:LearningResource`; consulta API por `resource_class`. **Implementado y verificado (TASK-002 scaffolding, TASK-003 vista maestra):** `MasterViewQuery` acota por `resource_class_id` y el filtro se comprobó en el contenedor real contra el catálogo (19 REA, `resource_class_id` 4758). Sello del estado registrado el 2026-07-30 al detectarse que la fila seguía en `propuesto` con el código entregado desde 2026-06-16 |
| RF-002 | Vista maestra en panel admin: tabla extendiendo el sistema de columnas/browse del core (≥4.0) con columnas propias y panel de detalle configurable | Alta | aceptado | Objetivo completo: columnas propias etapa (`educationalLevel`), materia (`about`), proyecto (`isPartOf`), licencia (`rights`) y una columna calculada de estado de integridad (RF-006), además de las nativas (título, clase, fecha); panel de detalle con edición inline de visibilidad (RF-003) y de alineamiento curricular (RF-004), más historial de curación (value annotations `dcterms`, RF-008/ADR-0002). **v1 ya implementada (TASK-003, ADR-0005):** columnas visibilidad/etapa/materia/alineamiento/licencia/modificado, drawer de detalle fijo (solo lectura) y curación de visibilidad; edición inline de alineamiento, columna de integridad e historial quedan para una iteración siguiente |
| RF-003 | Curación de visibilidad: cambiar público/privado usando el campo nativo del recurso (no flag propio), individual y por lotes | Alta | aceptado | Cualquier usuario con rol `editor` o superior (no basta con acceso al panel admin) puede cambiar visibilidad desde la vista maestra o el panel de detalle, individual y por lotes. La visibilidad queda fuera de la auditoría RDF (ADR-0002). **v1 ya implementada (TASK-003, ADR-0005):** toggle individual + lote con confirmación desde la vista maestra, reusando el permiso nativo de edición; la restricción explícita por rol `editor`+ (matriz rol×acción, NFR-003) sigue como mejora futura |
| RF-004 | Re-catalogación curricular: escribir valores RDF (resource values) apuntando a items-término del currículo existente, con previsualización, confirmación y reversibilidad | Alta | aceptado | Properties (ADR-0004): `lrmi:educationalLevel`/`schema:about`/`lrmi:teaches`/`lrmi:assesses`/`dcterms:relation`. Cardinalidad: múltiple en las cuatro properties (un REA puede tener varios items-término por propiedad). Roles: `editor` o superior para alineamiento curricular y tags; `global_admin`/`site_admin` exclusivamente para `schema:isPartOf` (proyecto), acción de gestor aparte (ADR-0004). Localización de los DefinedTermSet curriculares: ADR-0006 (`lrmi:educationalFramework`, configurable) |
| RF-005 | Re-catalogación por etiquetas (tags) como valores RDF según vocabulario elegido | Media | aceptado | Tags = ejes temáticos en `dcterms:relation`, **controlado** por un único `schema:DefinedTermSet` raíz, identificado por ID de item fijado en la configuración del módulo (ADR-0006). Roles: `editor` o superior |
| RF-006 | Comprobación de integridad: servicio que valida valores RDF (existencia, destino vivo, completitud) | Alta | aceptado | Campos a validar (ADR-0004): alineamiento (`educationalLevel`/`about`/`teaches`/`assesses`) vivo + licencia (`dcterms:rights`) presente. Definición de «completo»: si el item tiene asignada la plantilla REA (`resource_template`), completo = cumple los campos marcados como obligatorios en esa plantilla; si no tiene plantilla asignada, se aplica el mínimo (alineamiento vivo + licencia presente). **Ampliado (2026-08-05, TASK-028 rebanada 2):** las reglas mínimas —anclaje presente y licencia presente— se aplican **siempre**, y los campos obligatorios de la plantilla se **suman** en vez de sustituirlas. Antes eran ramas excluyentes, así que asignar una plantilla que no declarase obligatorios anclaje y licencia habría silenciado ambos avisos sin que el catálogo mejorase (trampa anticipada en TASK-027 §8.1, que PEND-012 iba a abrir). Se añade además el aviso `literal_in_link_property`: un literal en una property de enlace contaba como valor presente y la integridad decía «ok» (defecto D2) **Firmado por [ADR-0016](decisions/0016-ampliacion-reglas-integridad-y-anclaje.md) (2026-08-11):** la ampliación descrita arriba queda formalizada como decisión del propietario; incluye el aviso nuevo `literal_in_link_property`. |
| RF-007 | Estadísticas visuales del catálogo (cursos/etapas, materias, proyectos, licencias) con exportación CSV/PDF | Media | aceptado | Dimensiones (ADR-0004): etapa `educationalLevel`, materia `about`, eje `relation`, proyecto `isPartOf`, licencia `rights`. Gráficos: conteo simple por dimensión, cruce de 2 dimensiones (p.ej. materia×etapa) y % de completitud (RF-006). Export: CSV de los datos agregados detrás de cada gráfico (PDF se evalúa en una fase posterior) |
| RF-008 | Auditoría de las acciones de curación (quién, qué, cuándo) | Media | aceptado | Value annotations `dcterms` sobre el valor curado registran quién (`dcterms:contributor`), cuándo (`dcterms:modified`) y qué (`dcterms:provenance`) — ADR-0002. **Properties exactas ya decididas:** las fijó **PEND-005 al resolverse** (2026-06-15, ADR-0004) — etapa `lrmi:educationalLevel`, materia `schema:about`, saberes `lrmi:teaches`, criterios `lrmi:assesses`, ejes/tags `dcterms:relation`; esta fila conservaba un `[PENDIENTE]` desfasado que apuntaba a un pendiente ya cerrado. **Implementado y verificado (TASK-004, TASK-023):** la escritura del re-catalogador anota las tres claves de ADR-0002 sobre cada valor curado, y TASK-023 añade el «porqué» de la IA como `dcterms:description` en el mismo `@annotation`. Verificado sobre un `apply` real el 2026-07-29 (item #4674): el `@annotation` conserva las tres claves. **Reversibilidad real: cerrada (TASK-007, 2026-07-31, ADR-0015).** El hueco era estructural —una value annotation no puede registrar una eliminación, porque vive en el valor que se borra—, así que cada apply añade además un **evento de curación** privado y append-only sobre el propio item con el estado previo por dimensión (y el «porqué» de la IA de los valores que se van), y el curador puede **deshacer la última re-catalogación**, que restaura ids y justificaciones. Verificado en el contenedor sobre el item #5045 (31/31), incluido el vaciado total de una dimensión. **Sigue fuera de esta fila:** la superficie de UI del historial (rebanada 3 de TASK-028) y si la justificación de la IA se muestra en él (PEND-013) |
| RF-009 | Catalogación IA-assistida: la IA **propone** alineamiento curricular y ejes temáticos a partir de los metadatos del item y del contenido de sus medios; el **curador confirma** | Alta | aceptado | La IA propone valores para `lrmi:educationalLevel`/`schema:about`/`lrmi:teaches`/`lrmi:assesses`/`dcterms:relation` como etiquetas que se resuelven a items-término del currículo (ADR-0007); las propuestas pre-rellenan el flujo de re-catalogación (preview + confirmación + auditoría) de RF-004/RF-005, nunca se escriben sin confirmación del curador. Reimplementación PHP inspirada en el REA-MCP-cataloguer (no se reusa como servicio externo). Resolución por búsqueda incremental, sin volcar el árbol curricular al LLM (NFR-004). Saberes (`lrmi:teaches`) y criterios (`lrmi:assesses`) son de primera clase: se evalúan siempre ambos en cada clasificación, pero la dimensión sin encaje se omite de la propuesta (no se fuerza `≥1`; sin alucinar), ADR-0010 §5. Lote en segundo plano → RF-011 (tarea aparte) |
| RF-010 | Conexión a una API LLM **configurable por proveedor** (modelo local / Anthropic / OpenAI-compatible) | Alta | aceptado | El proveedor, endpoint/base URL, modelo y clave API se fijan en la configuración del módulo (ADR-0008). La clave vive en los settings nativos de Omeka; **nunca** en el repo, en el código ni en logs (CLAUDE.md §Seguridad). Abstracción por adaptadores; HTTP con `Laminas\Http\Client` del core, sin dependencia nueva |
| RF-011 | Catalogación IA-assistida **en lote** como Omeka Job en segundo plano: marcar varios items en la vista maestra y lanzar un Job que los catalogue con IA | Media | propuesto | Marcado múltiple en la vista maestra → `Omeka\Job` despachado al cola/runner nativo; cada item sigue el flujo propone→confirma o se deja la propuesta pendiente de revisión. **Tarea aparte de TASK-004** (no se implementa en TASK-004). Programación/scheduler a detallar en su tarea |
| RF-012 | Visión para imágenes en la cascada de extracción de contenido (configurable) | Baja | propuesto | La extracción de contenido para la IA podrá incluir el análisis de imágenes adjuntas mediante un modelo de visión, activable desde la configuración del módulo. Mejora posterior; fuera de TASK-004 |
| RF-013 | Modelar `dcterms:type` de los términos del currículo como recurso controlado (`skos:Concept`) en vez de literal | Baja | propuesto | Hoy la dimensión de un término se marca con un literal `dcterms:type` (confirmado 2026-06-24; ver docs/referencia/curriculo-modelo-rdf.md). Migrarlo a `skos:Concept` da un vocabulario de tipos enlazable y sin erratas. Afecta al dato del currículo (que el módulo **consume**, no posee) y al re-catalogador, cuyo filtro pasaría de `eq` (literal) a `res` (resource). Mejora posterior |
| RF-014 | Acotación contextual del re-catalogador por el grafo curricular | Alta | aceptado | El re-catalogador acota cada dimensión hija por el ancestro ya elegido siguiendo el grafo LOMLOE (ADR-0009): cascada Etapa→Curso (`lrmi:educationalLevel`)→Asignatura (`schema:about`)→{Saberes (`lrmi:teaches`), Criterios (`lrmi:assesses`)}, con fallback por curso/etapa vía enlaces denormalizados. La Etapa es ayuda de navegación, no se escribe en el REA. Siempre con filtro `dcterms:type` + búsqueda incremental (NFR-004) |
| RF-015 | **Datos de gestión y autoría del REA**: el catálogo registra y controla quién crea, quién edita y bajo qué licencia se publica cada recurso | Alta | propuesto | Hueco de gobierno detectado en TASK-027 (2026-07-27): el catálogo real tiene **0/19 REA con cualquier property de autoría** (`dcterms:creator`, `contributor`, `publisher`, `source`, `rightsHolder`, `license`; `schema:author`, `creator`, `copyrightHolder`) y **1/19 con licencia**, pese a ser objetivos declarados del módulo. `o:owner` no sirve: los 19 pertenecen al mismo usuario técnico. **Modelo aprobado por el propietario (2026-07-27, ligero controlado):** `dcterms:creator` literal **múltiple** (docente o equipo autor); `dcterms:publisher` literal desde **CustomVocab** (centro/CEP/Consejería); `dcterms:rightsHolder` literal con **valor por defecto configurable**; `dcterms:source` como **URI** para adaptaciones y obras derivadas. La licencia sigue el mecanismo ya fijado en ADR-0004 §5 (`dcterms:rights`, literal de CustomVocab controlado). Editable desde la UI del módulo (ADR-0013) con rol `editor`+ y auditada por value annotations (ADR-0002). Valores exactos del vocabulario de licencias: `[PENDIENTE]` (PEND-011) | ADR-0013, ADR-0004, ADR-0002 |

## 3. Requisitos no funcionales (NFR)

| ID | Descripción | Prioridad | Estado | Criterio de aceptación |
| --- | --- | --- | --- | --- |
| NFR-001 | Extender el core, nunca parchearlo: integración solo vía `attachListeners`, `module.config.php` y mecanismos nativos | Alta | aceptado | `git diff` del core de Omeka vacío; guardrail PreToolUse activo en `.claude/settings.json` |
| NFR-002 | Sin tablas Doctrine propias (decisión del propietario); única excepción evaluable: auditoría (PEND-006) | Alta | aceptado | No existe `data/` con entidades ni migraciones propias, salvo ADR que documente la excepción |
| NFR-003 | Autorización por ACL nativa: privilegios propios vía `acl_resources`, `userIsAllowed()` en cada acción de curación | Alta | aceptado | Matriz rol×acción: visibilidad, alineamiento curricular y tags → rol `editor` o superior; proyecto (`schema:isPartOf`) → `global_admin`/`site_admin` exclusivamente. No se añaden restricciones más allá de las que ya impone el core de Omeka por encima de esos roles |
| NFR-004 | Rendimiento: la selección curricular del re-catalogador no carga el árbol completo (lazy-load/búsqueda incremental) | Alta | aceptado | Siempre búsqueda incremental desde el primer item-término (autocomplete), sin umbral numérico que mantener: nunca se carga el árbol curricular completo de golpe |
| NFR-005 | i18n: cadenas traducibles vía Laminas; idiomas al menos es/en | Media | aceptado | Cadenas traducibles vía Laminas; idiomas soportados: español e inglés |
| NFR-006 | Accesibilidad: mantener el nivel del admin de Omeka 4.x | Media | aceptado | Nivel objetivo = el que ya ofrece el admin nativo de Omeka 4.x, sin objetivo WCAG adicional específico del módulo |
| NFR-007 | Compatibilidad: mínimo Omeka-S 4.2 (`omeka_version_constraint = ^4.2.0`) y PHP 8.4 (uso interno; revisar si se publica al ecosistema) | Alta | aceptado | `config/module.ini` declara `^4.2.0`; `composer.json` declara `php >= 8.4`; el módulo instala y corre en Omeka-S 4.2 con PHP 8.4 |
| NFR-008 | Clasificación IA óptima: maximizar accuracy y minimizar consumo de tokens del LLM; ante conflicto, **prima la accuracy** | Alta | aceptado | El clasificador (TASK-010) acota candidatos por el grafo (clasificación jerárquica top-down y/o recuperación previa) en vez de volcar el árbol; acota el contenido enviado; valida toda propuesta contra el grafo y la confirma el curador; se mide la accuracy contra REAs ya catalogados. Detalle en `docs/superpowers/specs/2026-06-25-clasificador-ia-4b.md` |
| NFR-009 | Mantenibilidad y testeabilidad: el repo dispone en **GitHub Actions** de los flujos de CI/CD, revisión de código, pruebas y releases que automatizan la verificación y facilitan el mantenimiento y la seguridad del repositorio. Patrón de referencia: `ateeducacion/omeka-s-IsolatedSites` (`.github/workflows/`) | Media | aceptado | En `.github/workflows/` existen, como mínimo: (a) **CI** en cada `pull_request` y `push` que ejecuta `composer install` y los targets reales del Makefile —`make check-untranslated`, `make compile-mo`, `make lint` (PSR-12) y `make test`— sobre **PHP 8.4** (NFR-007); (b) **release** automatizado al publicar un tag `v*.*.*`: genera notas desde el historial, empaqueta con `make package VERSION=…` y crea la GitHub Release con el ZIP, usando el `GITHUB_TOKEN` nativo; (c) apoyo a la **revisión de código** en PR (p. ej. preview en Omeka-S Playground y/o revisión de seguridad/dependencias). Ningún workflow expone secretos (CLAUDE.md §Seguridad) ni modifica el core (NFR-001) |
| NFR-010 | **Fiabilidad de la clasificación IA:** la catalogación asistida debe ser robusta y degradar con elegancia; ningún fallo del proveedor LLM ni el coste síncrono corrompe el catálogo ni bloquea al curador | Alta | aceptado | (a) Cualquier fallo del proveedor (HTTP≠2xx, límite de tasa, **timeout**, respuesta no parseable) se traduce en un estado de error **limpio y reintentable**, sin escritura en el catálogo ni 5xx no controlado, y sin filtrar la clave (CLAUDE.md §Seguridad). (b) **IA propone, curador confirma** (ADR-0007): toda propuesta se valida contra el grafo curricular (ids existentes, `dcterms:type`/dimensión coherentes, ADR-0009) **antes** de ofrecerse; nada se escribe sin confirmación. (c) El propose **no depende de una única petición HTTP síncrona** que pueda agotar el timeout del proxy (**TASK-020**): ejecución asíncrona/Job para lotes (RF-011) y para items lentos. (d) El intercambio con el LLM es **trazable** (panel debug / `TraceableInterface`) para diagnosticar fallos. Umbrales operativos (reintentos, timeouts) los fija el propietario. Complementa NFR-008 (óptima) y NFR-011 (accuracy) |
| NFR-011 | **Accuracy de la clasificación IA:** la calidad del alineamiento curricular y de los ejes debe ser **medible por dimensión** y **sin alucinación**. Complementa NFR-008 (que fija el objetivo de optimización accuracy↔tokens): NFR-011 fija el requisito de *calidad del resultado* | Alta | aceptado | (a) Se mide **precisión/recall/F1 por dimensión** contra REAs ya catalogados (verdad-terreno) con la herramienta de evaluación existente (`EvaluationScorer` / acción `aiEvaluate`, NFR-008); el **umbral objetivo lo fija el propietario** y se monitoriza al iterar prompts. (b) **Sin alucinación:** toda etiqueta propuesta resuelve a un item-término **existente** del grafo; la dimensión sin encaje se **omite** (no se fuerza `≥1`, ADR-0010 §5). (c) **Coherencia por construcción** bottom-up (ADR-0010): curso/materia se derivan de las hojas elegidas; cross-etapa/cross-materia no debe degradar la precisión |
