# Backlog del módulo OERManager

> IDs `TASK-NNN` correlativos estables; no se reutilizan ni renumeran. Estados: `pendiente` / `bloqueada` / `en curso` / `hecha`. Enlaces a [requirements.md](requirements.md) y [decisions/](decisions/).

| ID | Descripción | Estado | RF/NFR/ADR enlazados | Bloqueada por |
| --- | --- | --- | --- | --- |
| TASK-001 | Resolver con el propietario los pendientes bloqueantes y registrar las decisiones (ADRs nuevos donde aplique). Resueltos el 2026-06-12: PEND-001, PEND-002 (Omeka-S 4.2 + PHP 8.4), PEND-003, PEND-004. Resueltos el 2026-06-15: PEND-005 (mapeo RDF, ADR-0004), PEND-006 (auditoría RDF, ADR-0002), PEND-008 (skills descartadas), PEND-009 (paquete/licencia, ADR-0003). Resuelto el 2026-06-16: PEND-007 (RF/NFR detallados, ADR-0005 vista maestra v1 + ADR-0006 raíces de árboles RDF) — **no quedan pendientes abiertos** | hecha | PEND-001…PEND-009, ADR-0001…ADR-0006 | — (la resuelve el propietario) |
| TASK-002 | Scaffolding del módulo (FASE 1): estructura nativa Omeka-S (`Module.php`, `config/`, `src/`, `view/oer-manager/`, `asset/`), `module.ini` con `omeka_version_constraint = ^4.2.0`. Hecho el 2026-06-12 y verificado en el contenedor real (PHP 8.4.15, Omeka 4.2.0): lint OK, `getConfig()` OK, ini/constraint OK; instalación/activación en el admin verificada por el propietario | hecha | RF-001, NFR-001, NFR-002, NFR-007 | — |
| TASK-003 | Vista maestra v1: column types propios sobre el browse del admin + drawer de detalle fijo + curación de visibilidad (JS jQuery + REST API). Diseño aprobado (ADR-0005). **Hecha (2026-06-16):** `IndexController`, `MasterViewQuery`, `ColumnType\AlignmentStatus`, vista + JS/CSS implementados; `make lint`/`make test` en verde; verificado en el contenedor real contra el catálogo (clase `lrmi:LearningResource` resuelta, 20 items, estado de alineamiento calculado correctamente). PHPUnit en host no puede instanciar clases que dependen del core Omeka (limitación del arnés de TASK-008: bootstrap sin el core) — la lógica de `AlignmentStatus`/`MasterViewQuery` se verificó manualmente en el contenedor en vez de con tests automatizados; cobertura automatizada de esa lógica queda como mejora futura (arnés de integración dentro del contenedor). El alcance completo de RF-002/RF-003 (edición inline de alineamiento, columna de integridad, historial de curación) fijado en `requirements.md` queda para una iteración siguiente sobre esta v1 | hecha | RF-001, RF-002, RF-003, NFR-003, ADR-0005 | — |
| TASK-004 | Re-catalogador curricular y por tags (**entrega 4a, manual**) — **ALTO RIESGO**: diseño explícito en plan mode (2026-06-22), preview + confirmación + reversibilidad (reconstrucción desde auditoría, ADR-0002), revisor en contexto fresco. Autocomplete curricular + tags (lazy-load, NFR-004), config form (ADR-0006), ACL (NFR-003), escritura RDF + value annotations, y **acotación contextual por el grafo curricular** (RF-014, cascada Etapa→Curso→Asignatura→{Saberes, Criterios}, ADR-0009). Mapeo RDF en ADR-0004, raíces en ADR-0006, modelo del grafo en ADR-0009. La capa IA-assist se entrega aparte en TASK-010. **Hecha (2026-06-25):** verificado en el contenedor (escritura correcta de resource values, value annotations `@annotation`, sin borrado de otros metadatos tras corregir el gotcha de `isPartial`→`clear_property_values`+`append`); acotación contextual por el grafo; endurecimientos de la revisión en contexto fresco aplicados (validación de tipo/pertenencia del destino, CSRF en la escritura, sin fuga de errores). Residuos: confirmar acceso efectivo por rol (editor/site_admin/global_admin) en el contenedor; reversibilidad real diferida a TASK-007 | hecha | RF-004, RF-005, RF-014, NFR-003, NFR-004, ADR-0002, ADR-0004, ADR-0006, ADR-0009 | — |
| TASK-005 | Comprobación de integridad: servicio de validación de valores RDF + enganche en eventos. Campos a validar y definición de «completo» fijados (ADR-0004, RF-006). **Hecha (2026-06-18):** `IntegrityChecker` (representation-level, `check(ItemRepresentation): IntegrityResult`) + `IntegrityResult` (value object con status ok/warning/error e issues); registrado en `service_manager.invokables`; `Module::attachListeners` engancha `api.create.post`/`api.update.post` en `ItemAdapter` → vuelca issues al logger de Omeka para items `lrmi:LearningResource`. Reglas (RF-006): dead-link de alineamiento = error; completitud con plantilla REA = campos obligatorios; sin plantilla = alineamiento vivo + licencia presente = warning. Lint/tests en verde (8 tests, 18 assertions). La acción ante errores (bloquear guardado o no) queda para un ADR futuro | hecha | RF-006, ADR-0004 | TASK-002 |
| TASK-006 | **Estadísticas visuales del catálogo + export CSV.** Dimensiones y catálogo de gráficos fijados (ADR-0004, RF-007); diseño en `docs/superpowers/specs/2026-08-27-task-006-estadisticas-design.md`, plan en `docs/superpowers/plans/2026-08-27-task-006-estadisticas.md`. **Arquitectura:** `StatsController` nuevo (`indexAction`/`exportAction`), con **ruta propia** (`admin/oer-manager-stats`, hermana de `oer-manager`), **entrada de navegación propia** («Estadísticas») y **bloque ACL propio** en `editor`+`site_admin` con privilegios `index`/`export` — el mismo nivel que la curación, no el de `config` (solo `site_admin`+). Deliberadamente **no** se acopla a `MasterViewQuery`: siempre agrega el catálogo **completo**, nunca los filtros activos de la vista maestra. Cinco dimensiones (`etapa`, `materia`, `eje`, `proyecto`, `licencia`); cardinalidad múltiple (un item con varios valores en una dimensión cuenta una vez por valor, semántica ya fijada en PEND-007/TASK-036). **Capa pura en `src/Service/Stats/`** (`DimensionCounter`, `DimensionCrosser`, `CompletenessAggregator`, `CsvExport`), sin dependencia del core, con TDD real en host — mismo patrón puerto/adaptador que `CurationEvent` (TASK-007); `DimensionFacts` (extrae hechos de `ItemRepresentation`) y `CatalogSnapshot` (trae hasta `ComputedFilter::HARD_CAP`=2000 items) son las dos piezas que tocan el core y no tienen test de host posible (misma limitación documentada que `CurriculumSearch`), verificadas en el contenedor. **Cruce genérico:** los 10 pares posibles entre las 5 dimensiones se precalculan en servidor en **ambos órdenes de clave** (`"dimA/dimB"` y `"dimB/dimA"`) y viajan en un único JSON embebido, así el selector de cruce en cliente repinta al vuelo sin ida y vuelta al servidor — decisión de implementación más allá de lo que fijaba el spec. **Gráficos:** SVG/HTML a mano en `asset/js/stats/charts.js`, puro y testeable por `node --test`, sin librería de terceros (no hay ninguna en `package.json`). **Export CSV** reutiliza los mismos agregadores/relabeling que la pantalla (`exportAction` llama a los mismos helpers privados que `indexAction`): las cifras no pueden divergir entre pantalla y exportación. **Hallazgo de seguridad y arreglo (ronda de revisión, commit `ebc52ac`):** una revisión de seguridad independiente (no el revisor de código con alcance de tarea) encontró **inyección de fórmulas CSV (CWE-1236)** en la primera versión de `CsvExport`: las celdas se escribían literales, así que un título de término curricular o un valor de `dcterms:rights` que empezara por `=`/`+`/`-`/`@`/tabulador/retorno de carro se ejecutaría como fórmula al abrir el CSV exportado en Excel/Sheets — con el `site_admin` que lo abre como víctima. Corregido con la mitigación estándar de OWASP (prefijo de apóstrofo ante cualquier celda de texto que empiece por esos caracteres), aplicada de forma uniforme dentro de `CsvExport::toCsv()` a **toda** cabecera y celda (no delegado a que cada llamador sanee selectivamente), con **5 tests de regresión nuevos**. Lección: una clase cuyo trabajo es «serializar datos agregados a un fichero descargable» debe tratar **todas** sus cadenas de entrada como no confiables, incluso las que vienen de datos internos ya curados (títulos de términos curriculares) — porque esos títulos son editables por un curador. **Verificado en el contenedor** (`test/container/stats-check.php`, sobre los 19 REA reales del catálogo, **7 OK, 0 FAIL**): la cadena `CatalogSnapshot → DimensionFacts → agregadores` funciona de extremo a extremo contra `ItemRepresentation` reales — el catálogo no está truncado, hay conteos no vacíos de materia, al menos un cruce materia×etapa real, la suma de completitud (ok+warning+error) cuadra con el total, y `distinctResourceIds()` no repite ids. **Revisión final de rama (2026-08-28):** con las 12 tareas ya hechas, la revisión de conjunto —la que ve lo que ninguna revisión por tarea puede ver, la costura entre tareas— encontró **1 hallazgo Crítico y 3 Importantes**, corregidos en una sola ronda de arreglo (commits `a2f49ce`, `f73207b`) y verificados en una re-revisión de alcance acotado (los 11, incluidos 7 menores, ADDRESSED, sin regresión nueva): **(Crítico)** el enlace «Exportar CSV» del cruce (`href="#"`) nunca llegaba a `exportAction` — el JS añadía los parámetros a la URL de la propia página en vez de a la ruta de export, así que el botón recargaba la página en silencio y no descargaba nada; costura entre la Tarea 8 (vista) y la Tarea 10 (JS) que ninguna de las dos revisiones por separado podía ver. **(Importante)** elegir la misma dimensión en los dos selectores del cruce no tenía combinación en el JSON (solo 20 de 25 posibles), dejaba la tabla vieja en pantalla sin avisar, y el CSV exportado no tenía guarda contra ese caso — arreglado deshabilitando en cada selector la opción ya elegida en el otro (imposible desde la UI) más un 404 de respaldo en `exportAction` si igualmente llegan `dimension1===dimension2` por URL. **(Importante)** el CSV exportado no arrastraba el aviso de catálogo truncado que sí ve la pantalla (ADR-0013, «nunca mentir en silencio») — arreglado con una fila de nota al final del CSV cuando aplica. **(Importante)** ninguna verificación automática —ni de host, imposible, ni de contenedor— llegaba a resolver `StatsController`: el factory de 8 argumentos, `resolveTitles()`, y el relabeling nunca se habían ejecutado de verdad contra el core; el hallazgo Crítico vivía exactamente en ese punto ciego. Ampliado `test/container/stats-check.php` para resolver el controlador vía `ControllerManager` y llamar a `indexAction()` de verdad (ahora **10 OK, 0 FAIL**, no 7); de paso, guarda añadida en `CatalogSnapshot::fetch()` para que una clase `lrmi:LearningResource` sin resolver devuelva catálogo vacío en vez de la búsqueda sin filtrar devolviendo **todo** el catálogo en silencio (mismo patrón de riesgo que ya tenía `MasterViewQuery`, sin tocar ese fichero — fuera de alcance de esta ronda). **Suites finales:** 369 tests PHP/822 aserciones, 82 tests JS. **Aparcado explícitamente, no es defecto:** duplicar en `CatalogSnapshot` la resolución de `lrmi:LearningResource` que ya tiene `MasterViewQuery` (un resolutor compartido sería la abstracción correcta si algún día se toca, pero refactorizar código ya enviado y revisado no entra en una ronda de arreglo sin segunda vuelta) · un cubo «sin licencia»/leyenda de cardinalidad múltiple en los gráficos (decisión de diseño, no bug) · tope de barras/desbordamiento del cruce a escala (ya deferido a TASK-030 por el spec §8). **Suites:** 366 tests PHP/819 aserciones, 82 tests JS (`node --test`, incluye los 7 nuevos de `test/js/stats/charts.test.js`) — **superadas por las cifras finales de arriba tras la ronda de arreglo**. **No verificado y declarado como tal: la página renderizada en el admin real con sesión iniciada** (navegar a `admin/oer-manager-stats`, comprobar que los 5 gráficos de conteo pintan, que los selectores de cruce repintan la tabla sin recargar, que el CSV exportado de cada bloque abre con las mismas cifras que el gráfico, y que la entrada «Estadísticas» solo es visible para `editor`+) — no hubo herramienta de navegador ni credenciales de admin disponibles en esta sesión agéntica, el mismo patrón ya documentado para TASK-032/033/034 en este backlog. Entra en la deuda de **TASK-030** (revisión funcional completa, sprints finales) | hecha | RF-007, ADR-0004 | — |
| TASK-007 | Auditoría de curación: value annotations `dcterms` sobre el valor curado (ADR-0002). Incluye la **reversibilidad real** (registrar el estado previo: ids added/removed) que 4a dejó pendiente (B3). **Hecha (2026-07-31), aprobada por ADR-0015.** La mitad «auditoría» ya venía de TASK-004/023 (340 anotaciones); lo que faltaba era el estado previo, y el hueco era **estructural, no un descuido**: una value annotation **no puede registrar una eliminación**, porque vive en el valor que se borra — al vaciar una dimensión desaparece con ella. **Decisiones del propietario (2026-07-30):** persistir *y* entregar el deshacer funcionando; el registro vive como **un valor `dcterms:provenance` privado y append-only en el propio item**, con resumen legible en el valor y payload JSON versionado en su `@annotation` (`dcterms:replaces`); se deshace **solo el último evento** (LIFO); y **la justificación de la IA se guarda y se restaura** (94 de 340 anotaciones; regenerarla cuesta otra pasada de LLM). **Piezas:** `CurationEvent` (nueva, pura, 17 tests de host — es la única parte testeable fuera del contenedor, porque `RecatalogService` depende del core); `apply()` pasa a leer el estado previo y a anexar el evento; `lastEvent()`/`undo()` con guarda de obsolescencia y filtrado de destinos desaparecidos; acciones `recatalog-last-event` (solo lectura) y `recatalog-undo` (CSRF+ACL como el apply); botón de deshacer en el panel. **Rutas y ACL sin cambios** (la ruta ya admite guiones; el `allow` en bloque deja ambas en editor+, que es el nivel correcto: deshacer no es más privilegiado que hacer). **Mejora incluida y autorizada:** un «Confirmar» sin cambios ya **no escribe** — antes reescribía todos los valores y re-sellaba sus anotaciones con fecha nueva, falsificando la auditoría que ADR-0002 viene a dar. **Defecto hallado AL VERIFICAR, que el plan no preveía:** con `dcterms:modified` a precisión de segundo, dos escrituras seguidas (un doble clic en «Deshacer») compartían sello y `lastEvent()` tenía que desempatar por el orden de la colección, **que Omeka no garantiza** → restauraría el estado equivocado; corregido pasando el sello a **microsegundos**, que además sigue siendo el mismo en el evento y en las anotaciones por valor (la clave `(contributor, modified)` de ADR-0013 §7.2 se mantiene). **Verificado en el contenedor** (`test/container/undo-harness.php`, item #5045, **31/31**): vaciado total de `lrmi:teaches` (4 valores, 3 con porqué) y restauración exacta de ids **y** porqués, canarios del `ValueHydrator` intactos, LIFO, no-op sin evento, guarda de obsolescencia ante una escritura hecha fuera del módulo, y sellos únicos. El arnés se autorrestaura y la residualidad de eventos que dejó en #5045 se limpió después (el REA tenía `prov=0` antes). **Confirmado de paso:** un valor privado **no** se ve sin autenticar, lo que valida haber dejado la lectura del evento en el servidor en vez de fiarla al JSON-LD del drawer. **Fuera de alcance:** la superficie de lectura del historial (rebanada 3 de TASK-028) y PEND-013 | hecha | RF-008, ADR-0002, ADR-0004, ADR-0015 | — |
| TASK-008 | Wiring de hooks en `.claude/settings.json`. **Hecha (2026-06-12):** PostToolUse → `make lint` (aviso, solo `.php`); **Stop hook = `make lint` + `make test`** (ambos verdes; probado lint con violación PSR-12 → exit 2). PHPUnit ^11.5 en `require-dev`, `test/phpunit.xml` y `test/ModuleConfigTest.php` (5 tests, contrato del scaffold) creados | hecha | NFR-001, ADR-0001 | — |
| TASK-009 | Destilar la skill `omeka-module` desde el Omeka 4.2 real del contenedor (anatomía, eventos, ACL, column types, REST API). **Hecha (2026-06-12)**, todo verificado contra `application/src` y módulos instalados | hecha | NFR-001 | — |
| TASK-010 | **Clasificador curricular y por etiquetas con LLM (entrega "4b") — ALTO RIESGO y alta importancia.** Dos clasificadores de items (curricular sobre el grafo ADR-0009; por ejes sobre el set de tags `dcterms:relation`) con motor LLM (ADR-0008); la IA propone y el curador confirma (ADR-0007) reusando el flujo endurecido de TASK-004. Entrada: metadatos + contenido textual de medios (pdf-to-text con `smalot/pdfparser`, descompresión ZIP/SCORM con `ZipArchive`). **Hecha (2026-06-25):** arquitectura puertos/adaptadores con **TDD real en host (66 tests, lint PSR-12 en verde)** — núcleo puro testeable, glue del core aislado tras interfaces. Entregado: `ContentExtractor` endurecido (zip-slip imposible por lectura en memoria + rechazo de traversal, anti zip-bomb por ratio/conteo/tamaño, sin recursión anidada, PDF con `Config` de límite de memoria, whitelist, truncado por presupuesto); cliente LLM con dos adaptadores (`AnthropicClient` Messages API + `OpenAiCompatibleClient`) sobre `Laminas\Http\Client` sin redirecciones (sin SSRF), clave write-only nunca logueada; `PromptBuilder` (contenido delimitado como dato no-instrucción, marcas neutralizadas) + `ResponseParser` tolerante; clasificador curricular **jerárquico top-down** (Etapa→Curso→Asignatura→{Saberes,Criterios}, selección por **índice** sobre lista cerrada) + clasificador de ejes; `AiCataloguer` pre-rellena el panel de 4a (no escribe: el curador confirma con preview/apply); `EvaluationScorer` (accuracy vs REAs ya catalogados, NFR-008). Glue del core (controller `aiPropose`/`aiEvaluate` con CSRF+ACL editor+, `OmekaMediaSource` solo ficheros locales, `CurriculumTermResolver`, ConfigForm+factorías, JS de pre-relleno) verificado por sintaxis/config/lint; ejecución contra el core **pendiente de verificar en contenedor** (igual que TASK-003/004/005). **Revisión adversaria** (workflow: 5 finders + verificación escéptica; 13 hallazgos, 5 confirmados) aplicada: selección por índice (anti truncado/colisión), neutralización de delimitador (anti prompt-injection), Config de PDF (anti DoS), gating de base_url. Diferido: RAG/embeddings, visión (RF-012), lote como Job (RF-011). Dependencia `smalot/pdfparser` autorizada e instalada | hecha | RF-009, RF-010, NFR-004, NFR-008, NFR-010, NFR-011, ADR-0002, ADR-0004, ADR-0006, ADR-0007, ADR-0008, ADR-0009 | TASK-004 |
| TASK-011 | Catalogación IA-assistida en lote como Omeka Job en segundo plano: marcar varios items en la vista maestra → Job que los cataloga con IA. **Comparte la infraestructura de Job con TASK-020** (propose síncrono de un item que agota el timeout del proxy → 504); conviene abordarlas juntas o encadenadas | pendiente | RF-011, NFR-010, NFR-011, ADR-0007, ADR-0008 | — |
| TASK-012 | Visión para imágenes en la cascada de extracción de contenido (configurable) **Hecha (2026-07-27, revisión de código): la entregó TASK-019 (ADR-0011), no trabajo nuevo.** `MediaVisionExtractor` describe las imágenes del item (filtro heurístico puro: ruido por nombre, tamaño mínimo/máximo, top-N por tamaño) y las envía al modelo de extracción como bloques `image`. **Configurable** por los ajustes del módulo: `oermanager_llm_vision_enabled` (master toggle, **apagado por defecto** porque es egress de binarios a un tercero) y `oermanager_llm_vision_max_images`, ambos en `ConfigForm`. Doble puerta: toggle + capacidad del proveedor (`supportsImages()`). Afinada después por TASK-025 (tope de PDF) y TASK-026 (rasterizado local del PDF). | **hecha (cubierta por TASK-019)** | RF-012, ADR-0007 | TASK-010 |
| TASK-013 | Modelar `dcterms:type` de los términos del currículo como `skos:Concept` (recurso) en vez de literal; adaptar el filtro del re-catalogador de `eq` a `res`. Afecta al dato del currículo (consumido, no poseído por el módulo) | pendiente | RF-013, ADR-0004, ADR-0006 | — |
| TASK-014 | CI/CD en GitHub Actions: crear `.github/workflows/` siguiendo el patrón de `ateeducacion/omeka-s-IsolatedSites` — (a) workflow de **CI** en `pull_request`/`push` (`composer install` + `make check-untranslated`/`compile-mo`/`lint`/`test` sobre PHP 8.4); (b) workflow de **release** en tags `v*.*.*` (`make package VERSION=…` + GitHub Release con notas y ZIP vía `GITHUB_TOKEN`); (c) workflow(s) de apoyo a la **revisión de código** en PR (preview en Omeka-S Playground y/o revisión de seguridad/dependencias). Sin exponer secretos en logs/código; sin tocar el core. **En curso (2026-06-25):** creados `ci.yml`, `release.yml`, `dependency-review.yml` y `pr-playground-preview.yml` + `blueprint.json` raíz en la rama `feature/gh_actions`. Decisiones: la release quita el prefijo `v` del tag para `module.ini`/ZIP; la revisión de dependencias usa `composer audit` (no `dependency-review-action`, porque el repo es **privado** sin GitHub Advanced Security); se corrigió el step duplicado del patrón (`compile-mo`); `blueprint.json` y `.github/` añadidos a `archive.exclude` de `composer.json` para no ensuciar el ZIP de release. **Endurecimiento (revisión de seguridad automatizada):** acciones de terceros (`shivammathur/setup-php`, `softprops/action-gh-release`) ancladas a SHA de 40 chars con comentario de versión; `release.yml` con `permissions` por defecto `contents: read` y `contents: write` solo en el job de release (`GITHUB_TOKEN` ya estaba acotado al step); `.github/dependabot.yml` (ecosistema `github-actions`) mantiene los pines al día. Verificado en el host: lint/test/i18n/`composer audit` en verde, `make package` produce `OERManager-X.Y.Z.zip` con estructura `OERManager/` y restaura `module.ini` a `0.0.0`, YAML/JSON válidos. **Residuo (→ `hecha`):** confirmar el run real de los workflows en un PR de GitHub (requiere `git push`, autorización del propietario, CLAUDE.md §Seguridad) **Hecha: residuo saldado.** Los workflows han corrido en verde sobre PRs reales de GitHub — el propietario lo confirmó primero (PR#3 fusionó los workflows, PR#8/PR#9 los ejercitaron; commit `e227edc`, 2026-06-30) y se reconfirmó en el **PR #15** (2026-07-27): `CI / lint_and_test` en `pull_request` y en `push`, `PR Playground Preview` y el check de `dependabot.yml`, todos en verde. **Nota:** `release.yml` (trigger en tag `v*.*.*`) sigue **sin ejercitarse**; se validará con la primera release real. (El cierre de `e227edc` se perdió al resolver el merge de `main` en `task-026-rasterizado-pdf-vision`; aquí se restaura.) | **hecha (verificada en el PR #15)** | NFR-009, NFR-001, NFR-005, NFR-007 | — |
| TASK-015 | **Clasificador semántico bottom-up (rework de TASK-010).** Anclaje bottom-up (ADR-0010): E1 candidatos con `dcterms:description`; delimitación por familia de materia; selección de saberes/criterios cruzando cursos; derivación de curso+materia de las hojas (coherencia por construcción); E2 pre-filtro por bloque. TDD real (PromptBuilder, clasificador, fake) + lint PSR-12. **Hecha (2026-07-27, revisión de código): estaba entregada desde TASK-018/019 y el estado se había quedado atrás.** `CurricularClassifier` implementa el anclaje de ADR-0010 completo: Fase A.1 etapas (multi, acotan y no se escriben), A.2 materias (unión de familias por etapa, sin fijar curso), Fase B saberes por `dcterms:description` cruzando etapas/materias/cursos, Fase C criterios acotados a los cursos derivados de los saberes elegidos (con fallback a los de la materia), y pre-filtro E2 por bloque temático cuando hay demasiados saberes (con fallback a todos, sin perder cobertura). Curso (`lrmi:educationalLevel`) y materia (`schema:about`) se **derivan de las hojas**, que es la coherencia por construcción de ADR-0010. 19 tests del clasificador, lint PSR-12. Afinada luego por TASK-018 (secuencia), TASK-019 (capa de contexto), TASK-021 (perfil de inferencia) y TASK-023 (justificaciones). | **hecha** | RF-009, RF-010, NFR-004, NFR-008, NFR-010, NFR-011, ADR-0004, ADR-0007, ADR-0009, ADR-0010 | TASK-010 |
| TASK-016 | **Recuperación semántica por embeddings (roadmap).** `EmbeddingTermResolver` sobre `TermResolverInterface`: índice en fichero de las descripciones, top-K por coseno, LLM rerank, misma derivación bottom-up. Bloqueada por PEND-010. | pendiente | NFR-004, NFR-008, NFR-010, NFR-011, ADR-0010, PEND-010 | PEND-010 (decisión del propietario) |
| TASK-017 | **Defecto: el contenido de los medios no llega al clasificador IA.** Hallado en las pruebas funcionales de TASK-015 (2026-06-29): el trazado del intercambio LLM (debug añadido en la consola/panel) muestra que el contenido enviado al LLM contiene **solo metadatos del item, no el texto de los medios**. Reproducido con items con medio PDF y con SCORM (HTML en ZIP). Sin ese texto el anclaje curricular es inviable. Diagnóstico pendiente; hipótesis de causa raíz aguas arriba del `ContentExtractor` (que sí tiene TDD verde en host): `OmekaMediaSource::filesFor()` no devuelve rutas locales —resolución `extension()`/`filename()`/`getLocalPath()` del store, o el medio SCORM no se reconoce como `zip`— o el extractor las salta. **Primer paso de diagnóstico:** surfacing de `content.sources` y `content.skipped` (mapa nombre→motivo) en el panel/consola de debug, que hoy solo muestra `content_text`; ese mapa revela el motivo exacto del salto. Verificar en el contenedor (rutas reales del store). **Causa raíz CONFIRMADA (2026-06-29, item #3181 SCORM "Partes de la célula", paquete Netex smartbook):** el ZIP **sí se lee** (está en `sources`), pero todo el contenido útil se descarta por whitelist — `project.json → unsupported:json` (el modelo de contenido Netex vive en JSON), los `.js` de contenido (`2304279631.js`, `1228349175.js`), y **todas** las imágenes `.jpg/.png` (`resources/...celula_graficos_NN.png`, donde está el infográfico real). Del HTML solo sale un shell casi vacío (`PARTES DE LA CÉLULA h1 p`). Es decir: el clasificador trabaja **solo con los metadatos de Omeka** (título+descripción), no con el medio. Para este item los metadatos bastaron para acertar materia/saberes, pero con metadatos pobres fallaría. **Decisión (brainstorming 2026-06-29): ambos**, en dos fases (spec `docs/superpowers/specs/2026-06-29-extraccion-contenido-medios-design.md`). **Fase 1 HECHA (2026-06-29):** extracción JSON genérica en `ContentExtractor` (recorrido recursivo acotado por nodos, filtra ruido técnico, sanea HTML embebido; `.json` tratado como fichero suelto y entrada de ZIP; `json` en la whitelist de `OmekaMediaSource`); plan `docs/superpowers/plans/2026-06-29-extraccion-json-fase1.md`, ejecutado por subagentes con TDD (77 tests verdes, lint PSR-12), revisión final "ready to merge", **verificado en el contenedor** (item #3181: `project.json` ya no se salta, el texto real del recurso llega al LLM). **Fase 2 (visión sobre imágenes, configurable) → TASK-012/RF-012.** | hecha (Fase 1) | RF-009, RF-010, RF-012, NFR-010, NFR-011, ADR-0007 | TASK-015 |
| TASK-018 | **Refactor de la secuencia de clasificación curricular (afinado de ADR-0010).** **Verificado (2026-06-29) en el contenedor con el trazado del intercambio LLM:** corre la bottom-up de esta rama `task-015` (secuencia real `Etapa(4)→Materia(10)→[Bloques(3)]→Saberes(106)→Criterios(144)→Ejes(72)`; el curso NO es llamada LLM, se deriva — Fase D). Es decir, derivar curso sin LLM y materia-antes-de-hojas **ya está hecho**. Delta real pendiente: **(a)** permitir **varias materias candidatas** (hoy `pickSubjectName` elige una sola, `maxSelections=1`); **(b)** **precisión en el nivel crítico saberes/criterios** — los criterios (144 candidatos) NO pasan por pre-filtro de bloque (solo los saberes, `if TEACHES`), lista enorme en un solo prompt → extender el pre-filtro por bloque a criterios y/o acotar mejor; **(c)** revisar si la Etapa debe seguir siendo el primer paso con LLM — **evidencia (2026-06-29):** el mismo item #3181 se clasificó como **Primaria→Conocimiento del Medio** en una pasada y **ESO→Biología y Geología** en otra; con contenido pobre el primer paso (Etapa) es una conjetura gruesa que cae a un lado u otro y arrastra toda la cascada (no-determinismo). Liderar por materia(s) o evaluar varias etapas mitigaría esto. **Nota:** que los Criterios salgan vacíos `[]` NO es bug — los criterios LOMLOE son competenciales/genéricos (CE1.1…), rara vez específicos de un tema como "la célula"; coherente con ADR-0010 §5 (se intentan, se omiten si vacíos). **Hecha (2026-06-30):** afinado de la Fase A de ADR-0010 (sin tocar mapeo RDF ni grafo) por SDD con TDD — `CurricularClassifier` reescrito a **etapa multi-select** (`pickEtapaIds`, mitiga el no-determinismo de la etapa única), **materia multi-select** (`pickSubjectNames` + `gatherFamilies` dedup por nombre cross-etapa; supera la limitación "Materia única v1"), hojas reunidas por el producto etapas×materias con dedup por id y tope `LEAF_CAP=200` (`gatherLeaves`), y **criterios acotados a los cursos de los saberes elegidos** (`if courseIds` filtra `assesses`; fallback sin saberes → criterios de la materia) — más preciso en el nivel crítico que el pre-filtro por bloque genérico. Curso (`lrmi:educationalLevel`) y materia (`schema:about`) se siguen derivando de las hojas (Fase D). 82 tests verdes (incl. dedup cross-etapa, hojas cross-materia, filtro de criterios y su fallback), lint PSR-12. Nota de afinado añadida a ADR-0010. Plan `docs/superpowers/plans/2026-06-29-task-018-secuencia-bottom-up.md`. | hecha | RF-009, NFR-008, NFR-010, NFR-011, ADR-0009, ADR-0010 | TASK-015 |
| TASK-019 | **Refactor de la capa de extracción de contexto del item que se pasa al LLM.** Surgida de las pruebas funcionales de TASK-015/017/018 (2026-06-30). Hoy el contexto se ensambla así: `IndexController::itemMetadataText($item)` produce un texto de metadatos, `OmekaMediaSource::filesFor($id)` da las rutas de medios, y `ContentExtractor::extract($metadataText, $files)` los **concatena en un único texto plano** (`content->text()`) truncado por presupuesto de tokens; ese mismo blob se pasa **idéntico** a ambos clasificadores y a **cada paso** del LLM (etapa, materia, bloques, saberes, criterios, ejes). Problemas a evaluar: (a) la concatenación plana **no distingue metadatos de contenido de medios** ni la procedencia (qué medio aportó qué), lo que puede enterrar la señal; (b) el **truncado por presupuesto** puede descartar el contenido de los medios cuando los metadatos van primero (orden de ensamblado); (c) se envía el **mismo contexto completo a todos los pasos** aunque cada paso necesite señal distinta (p. ej. la etapa se decide con poco, los saberes necesitan el detalle del medio); (d) no hay estructura/secciones ni pesos por fuente. **Objetivo:** rediseñar esta capa para que el contexto que llega al LLM sea más útil y trazable, manteniendo las garantías de seguridad ya existentes (contenido como dato no-instrucción, anti zip-bomb/zip-slip, límites de nodos JSON, sin SSRF, clave nunca logueada) y la arquitectura puertos/adaptadores (núcleo testeable en host). **Alcance y diseño a decidir** (regla de oro: preguntar antes que inventar) — abrir con brainstorming/plan mode antes de tocar código. **En curso (2026-06-30):** diseño aprobado en brainstorming y formalizado en **ADR-0011** (spec `docs/superpowers/specs/2026-06-30-refactor-contexto-llm-design.md`). Decisiones: (1) **dos modelos** — extracción barato vision-capable + clasificador actual (extiende ADR-0008); (2) **destilador fiel** que NO infiere currículo → ficha estructurada (tema/conceptos/vocabulario); (3) **visión siempre top-N imágenes + rescate de PDF escaneado**, binario directo al LLM (`image`/`document` pdf), master toggle `vision_enabled` off — **absorbe TASK-012/RF-012**; (4) **contexto por paso** (gruesos→ficha, finos→ficha+crudo); (5) **`ItemContext`** estructurado con procedencia sustituye el blob plano (el truncado protege el medio). Faseado: ADR/gobierno → adaptador multimodal → `ItemContext`/enrutado → `ContextDistiller` → `MediaVisionExtractor` → glue/ConfigForm. TDD real en host; verificación en contenedor diferida. **Progreso (2026-06-30):** Fases 1-3 (ADR-0011 + gobierno «en curso»; adaptador multimodal — `LlmClientInterface::supportsImages()/supportsPdf()`, `AnthropicClient`/`OpenAiCompatibleClient` traducen bloques `image`/`document`; `ItemContext` con `coarseText()`/`fineText()`/`rawForDistillation()` y enrutado por paso en `CurricularClassifier`/`TagClassifier`) hechas en `4b01d8a`. **Fase 4 (`ContextDistiller` + segundo modelo) hecha:** `PromptBuilder::buildDistillationPrompt()` (ficha fiel: tema/conceptos/vocabulario/qué enseña; instrucción explícita de **no inferir currículo**; contenido como dato-no-instrucción con marcas neutralizadas); `ContextDistiller` (puro, `TraceableInterface`, sin-LLM-si-vacío) que lee `rawForDistillation()` y devuelve la ficha; `AiCataloguer` orquesta extracción → destilación → `ItemContext::withFicha()` → clasificación, y expone `ficha`/`distillation` en el debug; factoría del **2º cliente LLM** (`OERManager\Llm\ExtractionClient` con `EXTRACTION_MODEL`, cae a `MODEL`) + factoría de `ContextDistiller` + cableado de `AiCataloguer` en `module.config.php`. **105 tests verdes** (267 assertions; +13), lint PSR-12. **Fase 5 (`MediaVisionExtractor`) hecha:** filtro heurístico PURO `selectImages()` (descarta ruido por nombre `logo|icon|sprite|bg|thumb…` con lookaround + tamaño mín/máx; ordena por tamaño y toma top-N) y `describe()` que manda top-N imágenes + PDF escaneado como bloques `image`/`document` al modelo de extracción; **doble puerta** master toggle `vision_enabled` (off) + capacidad del proveedor (`supportsImages()/supportsPdf()`); `TraceableInterface` con motivo de omisión (`disabled`/`provider_no_vision`/`no_candidates`); `PromptBuilder::buildVisionPrompt()` (fiel, texto visible como dato-no-instrucción); `AiCataloguer` enruta imágenes y rescata los PDF `pdf_unreadable`/`pdf_empty` a la visión → `ItemContext::visionDescriptions`. **Fase 6 (glue + ConfigForm) hecha:** `OmekaMediaSource::imagesFor()` (jpg/png/gif/webp + tamaño, aparte de la cascada de texto); `ConfigForm` + `Module` con `EXTRACTION_MODEL`, `vision_enabled` (off), `vision_max_images` (3); factoría de `MediaVisionExtractor` (gating por settings) inyectada en `AiCataloguer` y `IndexController` pasando `imagesFor()` a `propose()`. **120 tests verdes** (310 assertions; +15), lint PSR-12. **VERIFICADA EN CONTENEDOR (2026-07-22):** `propose` real end-to-end sobre 4 items del corpus vía OpenRouter (12-23 s por item con el arnés CLI, que esquiva el 504 de TASK-020). Confirmado: `debug.ficha` no vacía y estructurada en los 4; enrutado por paso operativo (gruesos→ficha, finos→ficha+crudo) con alineamiento en las 5 dimensiones; `content.sources`/`skipped` con procedencia real; **rescate por visión funcionando** (#4674: `images:0, pdfs:1` → la ficha completa, con «1º ESO»/«MATEMÁTICAS», salió del PDF vía visión cuando la extracción de texto falló). Nota: ese rescate estaba **enmascarando** el defecto TASK-024 — con `vision_enabled` off (el default) esos items se habrían quedado sin señal alguna. | **hecha (verificada en contenedor)** | RF-009, RF-010, RF-012, NFR-004, NFR-008, NFR-010, NFR-011, ADR-0007, ADR-0008, ADR-0010, ADR-0011 | TASK-015, TASK-017 |
| TASK-020 | **`ai-propose` síncrono agota el timeout del proxy → 504 con modelos/proveedores lentos.** Detectado probando «Proponer con IA» (2026-07-01): `POST /admin/oer-manager/ai-propose` devuelve **504 Gateway Time-out** según el modelo/proveedor. **Causa raíz:** la acción es **síncrona** y, tras ADR-0011, encadena **~7-9 llamadas al LLM secuenciales** en una sola petición HTTP: `ContextDistiller` (ficha) → `MediaVisionExtractor` (si `vision_enabled`) → cascada del `CurricularClassifier` (etapa → materia → [bloques] → saberes → criterios) → `TagClassifier` (ejes). Con un modelo lento/grande/«de razonamiento» cada llamada tarda 15-40 s y el **tiempo de pared acumulado** se va a varios minutos. **Dos capas de timeout:** (a) **por llamada**, del módulo — `LaminasHttpTransport` con `timeout = 60s` hardcodeado; si *una* llamada lo supera, el módulo corta y el controlador responde `{error:'llm'}`/`{error:'unexpected'}` con **HTTP 200**, no un 504; (b) **total de la petición**, del stack Docker (proxy inverso / php-fpm: `proxy_read_timeout`/`fastcgi_read_timeout`/`max_execution_time`, ~60 s) — **de aquí sale el 504** cuando la suma de llamadas supera el límite; esa capa **no forma parte del módulo** (CLAUDE.md). **Mitigaciones inmediatas (sin código):** usar modelos rápidos (`EXTRACTION_MODEL` barato, clasificador no-razonador), `vision_enabled` off si no se usa (quita 1 llamada + subida de binario); y, en la infra del propietario, subir el timeout del proxy/PHP. **Arreglo de fondo:** hacer el propose **asíncrono/resiliente** — ejecutarlo como Omeka **Job en segundo plano** con *polling* desde el front (la petición HTTP vuelve al instante y no aplica el timeout del proxy); **comparte la infraestructura de Job con TASK-011** (lote), así que conviene abordarlas juntas o encadenadas. Mejora menor del módulo, independiente: hacer **configurable el timeout por llamada** (hoy 60 s fijo) y afinar el mensaje de error para distinguir «proveedor lento/colgado» de «total excedido». **Alcance/diseño a decidir** (regla de oro: preguntar antes que inventar) — abrir con brainstorming/plan antes de tocar código. **Diseño aprobado (brainstorming 2026-07-24, «async completo»):** propose de 1 item como Omeka Job (PhpCli) + polling; progreso por fases, cancelar, y recuperación tras abandono (resultado en fichero privado hasta TTL + reenganche por localStorage). Solo transporte: el clasificador y la calidad de la propuesta no cambian; el Job nunca escribe. Spec `docs/superpowers/specs/2026-07-24-propose-asincrono-design.md`, plan `docs/superpowers/plans/2026-07-24-propose-asincrono.md`. **Hecha (2026-07-24, host + verificada en contenedor):** `ProposalStore` (canal por fichero privado, escritura atómica, sweep por TTL — TDD host), `ProgressReporter`/`NullProgressReporter`/`JobStoppedException`, cableado de fases + `shouldStop` en `AiCataloguer::propose` (5º arg opcional; TDD host, null-object = sin regresión), `ProposeRunner` (itemId→payload), `JobProgressReporter`, `AiProposeJob`, acciones `aiPropose`(despacha)/`aiProposeStatus`(cruza fichero+estado nativo)/`aiProposeCancel` del controlador, y JS de sondeo/progreso/cancelar/reenganche + guardia de doble arranque. Sin rutas nuevas (el segment `[/:action]` mapea `ai-propose-status`/`ai-propose-cancel`). **185 tests host verdes** (443 assertions; +8), lint PSR-12, JS `node --check` OK. **Verificado en contenedor:** PhpCli arranca (spike, dispatch en 53 ms + job en 2º plano); happy path (dispatch→progreso por fases reales Destilando→Curricular→Ejes→completed en 18 s); camino de error (item inexistente → `status=error`, no cuelga); cancelación (`stop()` a mitad → `status=stopped`); payload completado con la misma forma que el síncrono (alignment enriquecido `{id,title}` + debug). Pendiente: verificación de la UI en navegador real (progreso pintado, botón cancelar, reenganche por localStorage, ausencia de 504) — como toda verificación de JS. Mejora menor diferida: timeout por llamada configurable. | **hecha (host + contenedor); UI navegador diferida** | RF-011, NFR-008, NFR-010, NFR-011, ADR-0007, ADR-0008, ADR-0011 | — (comparte infra de Job con TASK-011) |
| TASK-021 | **Perfil de inferencia compartido entre proveedores (ADR-0012).** Detectado probando TASK-019 (2026-07-02): con **el mismo modelo** por Anthropic directo vs OpenRouter los resultados divergen — sobre todo la **ficha destilada**, que arrastra a la clasificación. Causa: el módulo dejaba `temperature`/razonamiento al default de cada proveedor (y OpenRouter puede traer reasoning activado por modelo, cuyos tokens consumen `max_tokens` → JSON truncado → selección vacía sin reintento). **Hecha (2026-07-02, host):** settings `oermanager_llm_temperature` (vacía = no enviar; los Opus 4.6+ la rechazan con 400) y `oermanager_llm_max_tokens` (default 1024) en `LlmSettings` (+ parsers puros `parseTemperature`/`parseMaxTokens`) + `ConfigForm`/`Module`; los 4 servicios LLM (`ContextDistiller`, `CurricularClassifier`, `TagClassifier`, `MediaVisionExtractor`) reciben el perfil por factoría y lo envían **idéntico en todos los pasos**, trazando `llm_options` en el panel de debug; `OpenAiCompatibleClient` apaga el razonamiento **solo en OpenRouter** (`reasoning: {"effort":"none"}`, detección por host de la base URL; los endpoints genéricos no reciben params no estándar). TDD: 136 tests verdes (343 assertions; +16), lint PSR-12. **Divergencia de PDF cerrada para OpenRouter (2026-07-02, debugging del destilador):** fichas <700 chars vía OpenRouter vs >4000 vía Anthropic en items con PDF escaneado — el rescate por visión solo corría en Anthropic (`supportsPdf()=false`). OpenRouter soporta PDF de entrada (part `file`, procesado nativo del modelo): `OpenAiCompatibleClient` traduce ahora el bloque `document` a ese part y reporta `supportsPdf()=true` solo con OpenRouter; endpoints genéricos siguen omitiéndolo (138 tests verdes). Pendiente: verificación funcional en contenedor (repetir propose sobre los mismos items por ambos proveedores y comparar tasa de acuerdo). **Incidente en las pruebas (2026-07-02):** con `claude-sonnet-5` como clasificador + temperatura 0.2, Anthropic directo devolvía 400 («El proveedor de IA no respondió correctamente») — **Sonnet 5, Opus 4.6+ y Fable 5 tienen los sampling params eliminados y rechazan `temperature`** (Haiku 4.5 y Sonnet 4.6 la aceptan), y el controlador tragaba la `LlmException` sin loguearla (indiagnosticable). Arreglo: `aiProposeAction` loguea el mensaje saneado de la excepción, el mensaje de UI remite al log, e info del campo Temperatura corregida (en blanco con esos modelos; paridad por omisión). ADR-0012 actualizado. **VERIFICADA EN CONTENEDOR (2026-07-23):** `propose` real sobre los mismos 3 items (#3181, #4359, #37129) por **Anthropic directo** y por **OpenRouter**, ambos con `claude-haiku-4.5` en clasificador y extracción, `temperature=0.3`, `max_tokens=1024`, visión on. **(1) Perfil idéntico en todos los pasos — CONFIRMADO:** los 7 pasos con LLM de cada item (destilación + cascada curricular + ejes) trazan `llm_options` con exactamente `{temperature:0.3, max_tokens:1024}` en ambos proveedores (el `json:true` de los pasos de selección es formato de respuesta, no sampling; el destilador pide texto libre, coherente por diseño). **(2) Tasa de acuerdo (criterio ADR-0012, no identidad bit-a-bit):** Jaccard medio por dimensión **0.55 global**, pero el desglose es la conclusión real — **0.72 en los items con pista de nivel** (#37129 cita «2.º ESO» literal → acuerdo **1.00** en curso y materia por ambos lados; #3181) frente a **0.20 en #4359** (el item «(sin título)», sin metadatos y con ficha «Nivel citado: No consta»). La divergencia de #4359 NO es un fallo del perfil: fichas casi idénticas («vasos sanguíneos» ambos), pero un lado deriva 3º ESO→Biología y Geología y el otro 2º Bachillerato→Biología Humana — **ambos curricularmente correctos** (la circulación se enseña en las dos etapas), y como saberes/criterios cuelgan del curso, los ids salen disjuntos. Es exactamente el no-determinismo por contenido ambiguo que anticipó ADR-0010 §Afinado TASK-019: sin señal de nivel, la derivación bottom-up cae a un curso plausible u otro. Confirma que el perfil compartido cumple su objetivo (paridad de parámetros y de comportamiento donde hay señal) y aísla la fuente de variación restante en la **calidad de metadatos del item**, no en el proveedor → refuerza las guías de TASK-022 (completar metadatos mínimos) y el valor de TASK-024/vision para los items pobres. | **hecha (verificada en contenedor)** | NFR-008, NFR-010, NFR-011, ADR-0008, ADR-0011, ADR-0012 | TASK-019 |
| TASK-022 | **Refactor del destilador de contexto (`ContextDistiller`) guiado por casos reales.** Hipótesis surgida al revisar la calidad de la catalogación IA (2026-07-04, sobre TASK-019): la **ficha destilada** es la **señal primaria** de la clasificación — los pasos gruesos (etapa/materia/bloques) se deciden **solo con la ficha** (`coarseText()`) y los finos (saberes/criterios) con ficha+crudo (`fineText()`), así que una ficha pobre, sesgada o que se desvíe del contenido **arrastra toda la cascada bottom-up** (ADR-0010) por correcto que sea el anclaje curricular. Hoy `PromptBuilder::buildDistillationPrompt()` es **zero-shot**: pide una ficha fiel (Tema/Conceptos clave/Vocabulario/Qué enseña) sin ejemplos que fijen el nivel de detalle, el formato ni la frontera «describir vs. inferir currículo». **Objetivo:** rediseñar el destilador **calibrándolo con casos/ejemplos reales** — (a) **few-shot** en el prompt con 2-4 pares (extracto real de un REA → ficha de referencia) que ejemplifiquen una buena ficha y la regla de **no inferir currículo** (ADR-0011); (b) construir un **pequeño corpus de evaluación** de REAs reales del catálogo (p. ej. #3181 «Partes de la célula» SCORM Netex, un PDF escaneado, un item con metadatos pobres) con ficha de referencia, para medir la calidad de la ficha y detectar regresiones al iterar el prompt; (c) evaluar si conviene una ficha **más estructurada/parseable** (secciones estables) en vez de texto libre, sin perder fidelidad. **Invariantes a preservar (ADR-0011):** el destilador es **fiel y NO clasificador** (no propone etapa/materia/curso salvo literal), el contenido viaja como **dato no-instrucción** (spec §6, marcas neutralizadas), perfil de inferencia compartido (ADR-0012, `llm_options` trazado), y el núcleo sigue **puro/testeable en host** con fake LLM (TDD real; verificación en contenedor diferida). **Ojo coste de tokens:** el few-shot engorda la llamada del destilador, que ya forma parte del propose síncrono → tenerlo presente por NFR-008 (accuracy↔tokens) y TASK-020 (latencia/504). Medición contra REAs ya catalogados vía `EvaluationScorer`/`aiEvaluate` (NFR-011). **Primer paso (bloquea el resto):** el propietario **extrae del contenedor** un pequeño conjunto de REAs reales representativos (p. ej. #3181 SCORM Netex, un PDF escaneado, un item con metadatos pobres) **con su catalogación correcta** (etapa/materia/saberes/criterios ya confirmados); a partir de ese material se redactan las fichas de referencia del few-shot y del corpus de evaluación. Sin estos casos reales no se arranca el rediseño (no inventarlos). **En curso (2026-07-07, host):** primer paso **cumplido** — 6 REAs reales de `resource_class_id=4758` (`localhost:8080`) descargados y pasados por el `ContentExtractor` real. **Diagnóstico clave: el problema no era solo el prompt, sino el crudo que lo alimenta.** Caso #37129 (SCORM 41 MB): los 24k chars del contexto eran basura vendor (samples CKEditor, licencias de fuentes, CSS Wiris, texto demo «Apollo 11») porque el ZIP se recorría en orden de archivo, ~900 entradas de editor agotaban `max_zip_entries`, y el truncado head-first cortaba antes del contenido real (al final del ZIP). Casos #3181/#4359 (Netex): ids de interfaz (`imagelink_<hash>`, `navigationSectionInteracted`) diluían la señal. #40442: PDF escaneado → `pdf_empty` (solo visión). **Solución (dos frentes, ADR-0011 §Afinado TASK-022, sin relajar seguridad):** (1) `ContentExtractor` endurecido — denylist de rutas vendor (`noise_path_segments`, saltadas antes de consumir cuota), **reparto equitativo del presupuesto** entre piezas (water-filling, sustituye al truncado head-first), filtro reforzado de ids de interfaz y CSS embebido en `meaningfulText()`; (2) prompt del destilador guiado por casos reales — instrucción anti-ruido, sección estable «Nivel citado textualmente» (literal o «No consta»), few-shot con 2 ejemplos reales (#4674 nivel literal, #3181 Netex ruidoso) en `PromptBuilder::DISTILLATION_EXAMPLES`. Corpus de evaluación en `test/fixtures/distiller-corpus/` (6 items: extracto real + ficha de referencia + protocolo de medición en contenedor). Re-ejecutado el arnés de diagnóstico: #37129 sin rastro CKEditor/Apollo y con porcentajes/REF repartidos; #3181/#4359 sin ids técnicos; #40437/#4674 intactos. **151 tests verdes, lint PSR-12.** Guías al propietario: activar `vision_enabled` (#40442/#3181), completar metadatos mínimos (#4359 sin título), subir `max_tokens` si la ficha se corta. **VERIFICADA EN CONTENEDOR PARA LOS CASOS ZIP/SCORM (2026-07-22)**, con `propose` real vía OpenRouter (`openai/gpt-4o-mini` clasificador + `openai/gpt-5.4-nano` extracción): **#37129** (SCORM 41 MB) sin rastro de CKEditor ni «Apollo 11» — la denylist saltó 651 entradas `noise_path` — y ficha sobre porcentajes/REF con «2.º ESO», «Matemáticas» citados **igual que la referencia del corpus**; **#3181** y **#4359** sin ids de interfaz (`imagelink_`, `navigationSectionInteracted`), fichas on-topic (célula / vasos sanguíneos) y sección «Nivel citado textualmente» estable («No consta» donde no consta). Concordancia ficha↔referencia confirmada en #3181 y #37129. **Bloqueado para los casos PDF (#4674, #40437, #40442) por TASK-024(b)**: en Alpine/musl el PDF rinde 0 chars, así que su ficha no es medible todavía. Pendiente también la validación de las fichas de referencia por el propietario. **Desbloqueada para PDF (2026-07-27):** TASK-024(b) resolvió el 0-chars de PDF en Alpine/musl y TASK-026 rescata además los PDF sin capa de texto rasterizándolos, así que los casos #4674/#40437/#40442 ya son medibles. **Residuo:** medir sus fichas y que el propietario valide las fichas de referencia. **HECHA (2026-08-02): residuo PDF medido sobre la pila glibc del 8081**, con `propose-harness.php` y LLM real (OpenRouter, `openai/gpt-4o-mini`, visión activada), **3 repeticiones por item** para poder dar tasa de acuerdo y no un tiro único (criterio del corpus, ADR-0012). **Resultado: acuerdo ficha↔referencia en Tema y «Qué enseña» en 9/9 ejecuciones**, y la sección «Nivel citado textualmente» presente en 9/9, siempre copiada del recurso o de sus metadatos y **nunca inferida** (invariante de ADR-0011); lo que varía entre repeticiones es la forma de la cita, no su origen. #4674 y #40437 **rinden ahora el texto completo del PDF** (`content.sources` con el fichero, `truncated:false`) donde en Alpine daban 0 chars, y sus fichas citan literalmente «1º ESO»/«MATEMÁTICAS» y «Educación Primaria». Detalle y tabla en [referencia/despliegue-pruebas-pdf.md](referencia/despliegue-pruebas-pdf.md) §6. **Hallazgo que invalida un caso del corpus:** **#40442 ya no tiene medios** —el único de los 19 REA sin ninguno, comprobado en las dos pilas—, así que el «PDF A3 escaneado» que motivaba el caso no está adjunto y su ficha sale solo de metadatos: mide *fallback a metadatos*, no *rescate por visión*, y la rasterización local de TASK-026 **sigue sin ejercitarse sobre un REA real** (→ TASK-030). **Hallazgo de calidad, fuera del alcance del destilador:** el «Nivel citado textualmente» —la sección estable que esta misma tarea añadió— **no poda la cascada curricular**: #4674, que cita «1º ESO», recibe además 1º Bachillerato + «Matemáticas I», y #40442, con «1º Primaria» literal en sus metadatos, recibe Infantil de 3/4/5 años. La ficha hace su trabajo; quien no la aprovecha es el clasificador (→ TASK-030, y candidato a tarea propia). **Corpus completado el mismo día:** los tres casos ZIP/SCORM, que en un primer intento no eran medibles en esta pila por la extensión `zip` ausente, se rehicieron **sobre glibc** tras reconstruir la imagen con `install-php-extensions zip` (receta verificada en el runbook §6) — otras 3 repeticiones cada uno: **acuerdo 3/3 en los tres**, `No consta` correcto en #3181/#4359, «2.º ESO»/«Matemáticas» en #37129, y **0 apariciones** de ruido vendor (`ckeditor`, `apollo`, `wiris`, `imagelink_`, `navigationSectionInteracted`) en las 9 ejecuciones. **Total del corpus: 18/18 ejecuciones concordantes** (9 PDF + 9 SCORM), que es la primera vez que se mide entero sobre una plataforma que lee bien los dos formatos. | hecha | RF-009, RF-010, NFR-008, NFR-010, NFR-011, ADR-0007, ADR-0010, ADR-0011, ADR-0012 | — |
| TASK-025 | **Rescate confirmado de PDF grandes (`pdf_too_large` no llega ni a texto ni a visión).** Hallado por el propietario el 2026-07-22 probando «Proponer con IA» sobre un item con `IA_alcaravan.pdf`: el panel mostraba `leídos: (ninguno) | saltados: IA_alcaravan.pdf → pdf_too_large`, ficha de **212 chars** (solo el título), Etapa `[1,2,3,4]` (sesgo de inclusividad) y Materia/Ejes vacíos. **Causa raíz:** `AiCataloguer::rescuablePdfs()` filtra por `in_array($reason, ['pdf_unreadable','pdf_empty'])`, así que un PDF de más de 20 MB (`max_pdf_bytes`) se queda **sin texto y sin visión**. De fondo es un error conceptual: `max_pdf_bytes` es una guarda anti PDF-bomb del **parseo en memoria** (TASK-010) que de hecho gobernaba también el camino de **visión, que no parsea nada** — manda el binario al proveedor. Agravado por TASK-024(b): en Alpine casi todo PDF con texto cae en `pdf_empty`, o sea que la visión sostiene en silencio toda la extracción de PDF. **Diseño aprobado (brainstorming 2026-07-22):** (1) rescatar `pdf_too_large` por visión con un **tope propio** `vision_max_pdf_bytes` (default 32 MB, configurable) separado del de parseo; (2) `max_pdf_bytes` (20 MB) **fijo y no expuesto** — es seguridad, no rendimiento; (3) **propose en dos fases** que corta **antes de la primera llamada al LLM** y devuelve `needs_confirmation` con nombre y tamaño (coste cero en tokens); (4) decisión **tri-estado** `ask`/`include`/`skip` (un booleano provocaría un bucle de preguntas al rechazar); (5) al rechazar, el propose **sigue sin ese PDF**; (6) por encima de 32 MB **no se ofrece confirmación**, se informa. Doble puerta de visión intacta: sin `vision_enabled` no hay confirmación que ofrecer. Spec `docs/superpowers/specs/2026-07-22-rescate-confirmado-pdf-grandes-design.md`. TDD real en host (incluido el test de que en `ask` el fake LLM registra **0 llamadas**); glue por el arnés de `test/container/`. Afina ADR-0011; no toca ADR-0004 ni ADR-0009. **Hecha (2026-07-23, TDD real en host + verificada en contenedor):** `LlmSettings::VISION_MAX_PDF_BYTES` (default 32 MB) + parser puro `parseVisionMaxPdfBytes`; `MediaVisionExtractor` con `maxPdfBytes` inyectable (reemplaza la const privada de 20 MB — **discrepancia hallada al aterrizar el spec:** sin unificar, un PDF confirmado se caía en silencio dentro de `binaryBlock`) y `canRescuePdf()` (doble puerta); `AiCataloguer` gana `visionMaxPdfBytes`, el método PURO `classifyOversizePdfs` (confirmable ≤ tope / fuera de alcance), `rescuablePdfs` admite `pdf_too_large` solo con decisión `include`, y `propose($largePdfDecision='ask')` corta **antes de cualquier llamada** devolviendo `needs_confirmation`; `OmekaMediaSource::filesFor` añade `size`; factorías inyectan el mismo setting a ambos servicios (fuente única); ConfigForm/Module con el campo nuevo; `IndexController` lee `large_pdf` del POST y devuelve `needs_confirmation`; JS con diálogo de confirmación reinvocando el propose (include/skip) + aviso de `too_large_pdfs`. 177 tests verdes (431 assertions; +11), lint PSR-12, JS `node --check` OK. **Verificado en contenedor sobre el item #4362 (`IA_alcaravan.pdf`, 28.4 MB — el mismo que originó el reporte):** `ask`→`needs_confirmation` en **0 s (cero tokens)**; `include`→el PDF llega a la visión (`vision.pdfs=1`) en 614 s; `skip`→propose completa sin el PDF en 4.3 s. El contraste 614 s vs 4.3 s confirma el motivo de la confirmación y es TASK-020 en vivo (614 s daría 504 por HTTP; el arnés CLI lo esquiva). | **hecha (verificada en contenedor)** | RF-009, RF-012, NFR-008, NFR-010, NFR-011, ADR-0007, ADR-0008, ADR-0011 | TASK-019 |
| TASK-026 | **El propose se cuelga ~10 min con un PDF de más de 25 MB y no devuelve nada.** Reportado por el propietario el 2026-07-26 probando «Proponer con IA» sobre el item #4362 (`IA_alcaravan.pdf`, 28,4 MB). **Evidencia (jobs 428/429/430 del contenedor):** con `ask` el propose corta en 0 s (correcto); con `include` tarda **10 min 19 s** y termina `completed` con `vision.description` **vacía** y `alignment: []` — ficha «No consta (metadatos insuficientes)». Los items normales tardan 12-15 s. **Causa raíz (tres defectos encadenados):** (1) **el transporte no acota la llamada en tiempo real** — `LaminasHttpTransport` pasaba `timeout` al adaptador Socket, que lo aplica con `stream_set_timeout()`, es decir un tope de **inactividad por lectura**, no de la llamada entera; un proveedor que emite tráfico de mantenimiento mientras procesa no lo agota nunca (medido: la subida NO era el problema, el contenedor sube a 8,2 MB/s y los 38 MB de base64 viajan en ~5 s; los 10 min son proceso remoto); (2) **el rescate por visión subía el binario original** (28,4 MB → ~38 MB en base64) porque `max_pdf_bytes` se entendía como tope de *parseo* y la visión mandaba el PDF entero; (3) **una respuesta vacía se aceptaba en silencio**: `choices[0].message.content ?? ''` convierte en cadena vacía tanto «el modelo no dijo nada» como un error de OpenRouter devuelto **con estado 200 y el error en el cuerpo**. El PDF, además, es de **1 página, sin capa de texto y 0 fuentes** (infografía escaneada), así que subir el tope de parseo no habría servido de nada. **Diseño aprobado (decisión del propietario 2026-07-26):** rasterizar el PDF en local con **Imagick** —el mismo motor con el que Omeka genera las derivadas— y enviar sus páginas como imágenes; **tope de 4 páginas** (la portada y las primeras concentran la señal); **se retira la confirmación de TASK-025**, cuyo motivo (coste y latencia de subir el binario) desaparece; **tope duro de 120 s** por llamada al LLM. **Hecha (2026-07-26, TDD en host + verificada en contenedor):** `PdfRasterizerInterface` + `ImagickPdfRasterizer` (4 páginas, 150 dpi, JPEG 80, tope de 5 MB por página con reintento a media resolución, todo fallo capturado → `[]`, que es la condición con la que la visión cae al bloque `document` nativo de ADR-0011/0012); `MediaVisionExtractor` gana `rasterizer` inyectable y traza `pdf_pages` y `empty_response`; `AiCataloguer` pierde `needs_confirmation`, `classifyOversizePdfs`, `visionMaxPdfBytes` y `too_large_pdfs`, y `rescuablePdfs` admite `pdf_too_large` siempre; `ProposeRunner`/`AiProposeJob`/`IndexController`/JS pierden `large_pdf`; `aiProposeStatusAction` corta el **estado zombi** (fichero `in_progress` con el Job ya terminado = murió sin escribir → `job_died`), que era otro cuelgue del sondeo hasta el techo de 12 min; `LaminasHttpTransport` usa el adaptador **Curl** (`CURLOPT_TIMEOUT` = llamada completa, 120 s; Socket como respaldo degradado sin ext-curl) y traduce el corte a `LlmException`; ambos adaptadores LLM detectan el **error en cuerpo con estado 2xx**. 189 tests verdes (453 assertions; +2 netos), lint PSR-12, JS `node --check` OK. **Verificado en contenedor sobre el item #4362:** **33 s** (antes 619 s) con descripción real de la infografía (especie, taxonomía, hábitat, alimentación, reproducción, Canarias), ficha real y alineamiento no vacío (`educationalLevel`, `about`, `assesses`, `relation`); el PDF de 28,4 MB se convierte en **una imagen de 659 KB** renderizada en 0,91 s. Sin regresión en items normales (4676: 10,9 s; 5047: 11,7 s). **Camino asíncrono real verificado (Job → ProposalStore → payload):** job 438 `completed` en **26 s** con alineamiento real. El job 437, lanzado con el proveedor ya degradado por cuatro proposes seguidos, tardó 933 s y terminó en `{"status":"error","code":"llm"}` con el log `El proveedor LLM no respondió dentro del tope de 120 s.` — o sea, **el tope nuevo hizo su trabajo**: cortó y reportó en vez de colgarse. **Límite conocido:** el propose encadena 7-9 llamadas, así que con un proveedor lento el peor caso (~18 min) supera el techo de sondeo del navegador (12 min); al agotarlo el JS conserva el jobId en `localStorage` y el curador reengancha volviendo a pulsar. Afina ADR-0011 (la visión rasteriza en local en vez de subir el binario) y **deja sin efecto el flujo de confirmación de TASK-025**. | **hecha (verificada en contenedor)** | RF-009, RF-012, NFR-008, NFR-010, NFR-011, ADR-0007, ADR-0008, ADR-0011 | TASK-025 |
| TASK-027 | **Estudiar los campos que debe mostrar/editar la UI del módulo** (vista maestra + panel de detalle), antes de tocar código. Motivación: RF-002 ya dejó pendiente de «iteración siguiente» la edición inline de alineamiento, la columna de integridad y el historial de curación, y el drawer actual (`asset/js/oer-master-view.js`) muestra una lista fija de 9 properties (`DRAWER_FIELDS`) sin que conste una revisión deliberada de cuáles son necesarias, cuáles sobran o cuáles faltan (p. ej. estado de integridad de RF-006, historial de auditoría de RF-008/ADR-0002, ficha destilada de la IA de ADR-0011). **Entregable:** documento (no código) con el catálogo de campos por vista (tabla maestra: columnas fijas + filtros; drawer: campos de solo lectura + editables) y su procedencia (property RDF, value annotation, cálculo), que sirva de base al diseño de TASK-028. No decide el ANTES/DESPUÉS visual ni la arquitectura JS, solo el QUÉ. **Bloquea TASK-028** por decisión del propietario (2026-07-27): no refactorizar la UI sin tener antes claro qué campos debe mostrar. **Hecha (2026-07-27).** Entregable en `docs/superpowers/specs/2026-07-27-campos-ui-design.md`, aprobado por **ADR-0013** (vista maestra v2), que complementa ADR-0005 sin derogarlo (su §25 exige ADR nuevo para ampliar el alcance v1). **Hallazgo central, medido sobre el catálogo real de 19 REA:** el alineamiento curricular está al **100 %** en las 5 properties (384 enlaces) mientras la ficha de gobernanza no existe —**0/19 autoría** en las 12 properties candidatas, **1/19 licencia** (literal `ccbysa` sin versión ni URI), **0/19 plantilla**, **0 URIs**—, y sin embargo la UI dedica su superficie a lo primero: la columna «Alineamiento» devuelve `complete` para los 19 REA, o sea **varianza cero**. Además hay dos activos ya construidos y jamás mostrados: **340 anotaciones de auditoría** (15/19 REA, 94 con justificación pedagógica) e `IntegrityChecker`, cuyo resultado solo va al log. **Decisiones del propietario:** autoría ligera controlada (`dcterms:creator` múltiple + `dcterms:publisher` de CustomVocab + `dcterms:rightsHolder` con defecto + `dcterms:source` URI) → **RF-015**; la UI edita la **ficha de gestión completa**; **inventario + cola de calidad**; `lrmi:learningResourceType` a **CustomVocab** —que **ya existe sin usarse**: `custom_vocab` id 1, «Tipos de recursos», 29 términos en 7 familias, mientras los 16 REA usan 9 literales libres solapados—. **7 defectos registrados (D1-D7)**, entre ellos: el filtro de tipo de recurso está **muerto por construcción** (operador `res` sobre dato literal); los 4 valores literales en properties de enlace son invisibles a la vez para el filtro y para `checkLiveLinks`, así que integridad los da por «ok»; y el preview de re-catalogación pide confirmar una escritura RDF mostrando **solo recuentos** aunque el backend ya devuelve `added`/`removed` con títulos. Abre **PEND-011** (contenido del vocabulario de licencias), **PEND-012** (¿plantilla REA?, con el riesgo de que aplicarla silencie todos los avisos de integridad) y **PEND-013** (¿justificación de la IA en el historial?). | **hecha** | RF-002, RF-006, RF-008, ADR-0002, ADR-0011 | — |
| TASK-028 | **Refactorizar la UI del módulo**: tanto la tabla/array de items a gestionar (vista maestra, `view/oer-manager/admin/index/index.phtml` + `MasterViewQuery`) como la UI JavaScript de detalle del item (`asset/js/oer-master-view.js`, 680 líneas). Pedido por el propietario (2026-07-27). **Puntos de partida hallados en el código (sin decidir solución, a discutir en diseño):** (a) la vista maestra es una tabla server-rendered de una sola pieza (columnas fijas del core + propias, filtros vía GET con recarga completa de página, paginación nativa, IDs numéricos en crudo para filtrar por etapa/materia/proyecto/eje sin autocompletar); (b) el JS es un único IIFE de 680 líneas sin módulos ni build, con al menos 8 responsabilidades mezcladas (drawer de detalle, panel de re-catalogación con un widget tipo Chosen reconstruido a mano vía cadenas `$('<div>').append(...)`, sondeo/progreso/cancelación del propose IA, renderizado de diff, panel de debug, autocompletado de búsqueda de términos, visibilidad en lote); (c) **sin infraestructura de test para JS** (no hay `test/js/`, ni `package.json`, pese a que el `Makefile`/lint ya lo excluyen previendo que exista) — todo el comportamiento del drawer/re-catalogador se verifica hoy solo manualmente. **Bloqueada por TASK-027** (decisión del propietario, 2026-07-27): primero se estudian los campos que la UI debe mostrar/editar; el refactor de arquitectura/tecnología se diseña después, con eso ya fijado. **Desbloqueada (2026-07-27):** TASK-027 entrega el catálogo de campos (`docs/superpowers/specs/2026-07-27-campos-ui-design.md`) y ADR-0013 lo aprueba. Entra en el alcance, además del refactor: columna de integridad, sección de gobernanza legal y autoría editable, historial de las 340 anotaciones, sustitución de los IDs numéricos por el autocompletado `search-terms` ya existente, y las correcciones D1-D7. **Habilitadores decididos en el estudio:** dejar de hardcodear las columnas en el `.phtml` (registrarlas como `column_types` y usar el mecanismo nativo de browse), un único componente de autocompletado compartido por drawer y filtros, y el patrón obligatorio de filtros computados (resolver ids → evaluar predicado → paginar en memoria, con tope duro) para no propagar el total aproximado del filtro «parcial». **Bloqueos parciales:** la columna y el filtro de autoría dependen de PEND-011; la higiene masiva (literal→enlace, `type` genérico, normalizar licencias) exige ADR propio por reescribir RDF ya escrito. **Settings nuevos a implementar en `ConfigForm` (ADR-0013, «sembrar, no poseer», 2026-07-28):** `oermanager_licence_vocab_id` (CustomVocab de licencias — **el módulo lo siembra bajo demanda**, no existe hoy), `oermanager_resource_type_vocab_id` (CustomVocab de tipos — **apunta al id 1 ya existente, el módulo NO lo crea**), `oermanager_rea_template_id` (plantilla REA — sembrada bajo demanda; el setting es lo que permite a `IntegrityChecker` distinguir «la» plantilla de «otra» y no silenciar avisos) y `oermanager_default_rights_holder`. Reglas: identificación **por ID, nunca por etiqueta**; siembra **idempotente y bajo demanda desde la configuración, nunca en `install()`**; `upgrade()` **jamás re-siembra ni corrige**; si el setting está vacío o el artefacto no existe, el campo **degrada a texto libre y la UI lo dice** (patrón de `CurriculumSearch`). **Dependencia blanda de CustomVocab**: no se declara en `module.ini`; si el módulo no está activo, los campos degradan y la acción de sembrar no se ofrece. **TROCEADA por decisión del propietario (2026-07-28):** la tarea mezclaba el *refactor* que se pidió originalmente con la *ampliación funcional* que añadieron TASK-027/ADR-0013, y no cabía en un spec revisable. Se aborda por rebanadas, cada una con su spec. **Rebanada 1 — refactor puro (spec `docs/superpowers/specs/2026-07-28-refactor-ui-design.md`):** misma funcionalidad, mejor estructura, **cero campos nuevos**. Decisiones del propietario: clave de configuración de columnas propia (`oer_items`) con columnas **configurables por curador** vía el formulario de usuario nativo (sustituye el hardcodeo del `.phtml`, D6, y da el selector de orden nativo, D5); `MasterViewQuery` partido en dos con un `ComputedFilter` nuevo (resolver ids con tope duro → predicado → paginar) que **cierra D4** y deja construido el patrón obligatorio de ADR-0013; filtro de tipo de recurso arreglado **con desplegable del CustomVocab id 1 ya existente** (cierra D1), adelantando solo el setting `oermanager_resource_type_vocab_id`, el único de los cuatro sin bloqueos; autocompletado `search-terms` con **linaje** en los filtros curriculares en vez de IDs numéricos; disposición híbrida (barra rápida + búsqueda avanzada + chips nativos de filtros activos); diff detallado con títulos en el preview (**cierra D7**); y el JS de 680 líneas troceado en **módulos ES nativos** con núcleo puro testeado por `node --test` (`package.json` mínimo, cero dependencias, sin bundler). **D2 y D3 quedan fuera a propósito** (son de `IntegrityChecker` y de la columna que ADR-0013 retira). **Autorización expresa del propietario (2026-07-28): editar el `Makefile`** para añadir el target de tests de JS, levantando puntualmente la prohibición de CLAUDE.md; sin él, el Stop hook nunca ejercitaría esos tests. **Rebanadas siguientes (pendientes de spec):** gobernanza legal y autoría editable + siembra de vocabularios (bloqueada en parte por PEND-011), integridad e historial de las 340 anotaciones (PEND-013), y cola de calidad con contadores (agregación compartida con TASK-006).  **REBANADA 1 HECHA Y VERIFICADA (2026-07-29)** — 16 tareas, 12 commits, `ae73645`…`a881d4a`. **Cierra D1, D4, D5, D6 y D7.** Estado final: JS pasa del IIFE de 680 líneas a **11 módulos ES** (1004 líneas) con núcleo puro sin DOM —frontera dura verificada por grep en cada tarea— y **33 tests `node --test`** sin dependencias; PHP suma 14 tests (203→217). **Verificado en contenedor:** las 6 columnas rinden cabecera y celda **idénticas** a la v1; el browse nativo de items no cambia; los 6 column types se ofrecen de vuelta en el selector del perfil (que es lo que justificaba las subclases); el filtro de tipo pasa de **0 resultados (`res`) a 1 (`eq`)** sobre «mapa conceptual»; el linaje distingue los **nueve** «Matemáticas» (el estudio decía siete); `preview()` devuelve títulos con su curso. **Verificado por el propietario en el navegador:** carga sin errores de consola, drawer, panel de re-catalogación, selector de orden, y preview/apply completos. **Defectos ENCONTRADOS AL IMPLEMENTAR que el plan no preveía** (relevantes para las rebanadas siguientes, que se apoyan en el mismo estudio): (1) `CurriculumSearch` pasaba `per_page` **sin `page`**, y el core solo lo aplica acompañado (`AbstractEntityAdapter::limitQuery`), así que el autocompletado traía el **árbol entero** —7884 saberes por pulsación, contra NFR-004— desde siempre; corregido en `b3de80c`. (2) Al pasar las columnas al mecanismo nativo, la celda de visibilidad cambió de `.column-is_public` a `.column-oerIsPublic` y el JS seguía pintando la vieja: el cambio se guardaba pero la tabla no lo reflejaba (regresión introducida y corregida dentro de la propia rebanada, `dda6de5`). (3) El plan colgaba los campos de columnas del **formulario raíz**, pero `UserController` solo persiste `$values['user-settings']`: se habrían pintado sin guardarse jamás. (4) El identificador del evento `view.search.filters` no es el del core sino **el controlador propio** (`View\Helper\Trigger` usa `$routeMatch->getParam('controller')`): con el del plan el listener no se dispara nunca. (5) `returnScalar` es **opción** de `search()`, no clave de query. (6) `AlignmentStatus::statusFor` NO se puede recalcular en JS sin duplicar una regla de ADR-0005. **Decisión de empaquetado resuelta (2026-07-29):** `package.json` se **excluye** del ZIP (`composer.json`), coherente con `/test`: solo declara `type: module` a Node para los tests del host y el navegador no lo consulta para los módulos ES. **Dos correcciones al estudio de TASK-027 confirmadas en código** (ya en el spec §10, se registran aquí porque las rebanadas siguientes parten del mismo estudio): el preview **no** devolvía títulos —`currentTargetIds()` recogía solo ids— así que D7 exigió cambio de servidor, no solo de cliente; y el setting `oermanager_resource_type_vocab_id` **ya existía** desde `55da9b6`: la rebanada no añade settings, estrena el primer consumidor de uno que se guardaba sin lector. **HALLAZGOS ABIERTOS que no son de esta rebanada:** (a) los 29 términos del `custom_vocab` id 1 y los 9 valores que los REA tienen en `lrmi:learningResourceType` tienen **intersección CERO** («Contenidos multimedia: Vídeos…» vs «mapa conceptual»), así que en cuanto se apunte el setting al vocab 1 el desplegable ofrecerá 29 opciones y **todas darán 0 resultados** — decisión pendiente: re-catalogar los REA, ajustar el vocabulario o dejar el texto libre; (b) tras «Confirmar», la **fila de la tabla no se refresca** (columna Alineamiento/Etapa/Materia) hasta recargar — **no es regresión**, el código original hacía lo mismo desde TASK-004; la salida correcta es que el apply devuelva el estado ya calculado en servidor, nunca recalcularlo en JS; (c) los 19 REA del catálogo están **todos** en estado `complete`, así que el filtro «parcial» de D4 no se pudo ejercitar con datos reales —el patrón se validó con el predicado `complete`, con total exacto y páginas estables (4,4,4,4,3)—; (d) **no verificado**: el propose IA asíncrono end-to-end (gasta tokens) y el ciclo de guardado real de las columnas por curador (sin credenciales de admin). **Paso 2 del cierre VERIFICADO (2026-07-29)** sobre un `apply` real del propietario con el código nuevo (item **#4674**, modificado 11:59:51): `dcterms:title` y `dcterms:description` del item **se conservan** —el canario de la trampa crítica del `ValueHydrator`, que borra los values no reenviados—, `lrmi:learningResourceType` intacto, la justificación de la IA aparece **solo** en `lrmi:teaches` (1/14) y `lrmi:assesses` (7/17) y **no** en educationalLevel/about/relation, y el `@annotation` conserva las tres claves de auditoría de ADR-0002 (contributor, modified, provenance). Contraste adicional en todo el catálogo: siguen siendo **1 de 19** los REA con `dcterms:rights`, la misma cifra que midió TASK-027 antes de la rebanada, así que el patrón `clear_property_values`+`append` no perdió nada por el camino. **PASADA DE DISEÑO VISUAL sobre la rebanada 1, pedida por el propietario ad hoc el 2026-07-29** sobre una captura de la vista maestra («mejorar el diseño»), **fuera del spec de la rebanada**. **Solo presentación: cero campos nuevos, cero cambios en `column_defaults`, en la query y en el controlador.** 4 ficheros: `asset/css/oer-master-view.css` (reescrito sobre un sistema de tokens), `view/oer-manager/admin/index/index.phtml`, `src/ColumnType/IsPublic.php` y `asset/js/ui/visibility.js`. **Decisión de encaje:** paleta y tipografía se **heredan del admin de Omeka** (Lato, acento `#a91919`, filetes `#dfdfdf`, leídos del `style.css` compilado del contenedor) en vez de inventar las propias — un módulo que pelea con el chrome de su anfitrión se lee como una pieza rota; el presupuesto de diseño se gasta en jerarquía, no en identidad. **Cambios estructurales:** (a) las **cuatro filas sueltas** de controles (filtros, chips, orden, paginación) se agrupan en una banda de barra de herramientas + una barra de resultados justificada; (b) **riel de alineamiento** de 3px en el canto de cada fila (`data-alignment` en el `<tr>`, calculado con `AlignmentStatus::statusFor`), con la saturación **invertida a propósito** —`completo` se retira en verde apagado, `parcial` y `sin alinear` quedan saturados— para que la vista se recorra buscando el defecto; (c) el chip de estado deja la píldora por una **escala ordinal marcada por forma** (●/◐/○) + rótulo, así la completitud se lee sin depender del color; (d) **anchuras de columna explícitas**: el `table-layout: fixed` del core repartía a partes iguales y partía los títulos largos en cuatro renglones; (e) cabeceras en un **registro único de versalitas de 11px**, que de paso corrige la caja desigual de los rótulos que vienen del vocabulario del core («Is public», «about», «Rights») sin tocar las etiquetas; (f) las acciones de lote **solo existen con selección** (antes eran dos botones grises permanentemente muertos), con recuento vivo, resalte de fila, `indeterminate` en el «seleccionar todo» y «Deseleccionar» — no se pierde nada sin JS porque ya eran acciones solo-JS. **Riesgo asumido y declarado:** los títulos **dejan el rojo de Omeka y pasan a tinta** (`#2b3240`), conservando el rojo en `:hover`/`:focus`; diecinueve enlaces rojos en una rejilla densa tapan la única señal que se viene a buscar. Se aparta de la convención del browse del core: **revocable por el propietario**. **Copy corregido:** `ColumnType\IsPublic` sobreescribe `getLabel()` (el core devolvía **`Is public` sin traducir**, visible en la captura) y `renderContent()` (`Sí`/`No` suelto → estado marcado `Público`/`Privado`). **Contrato duplicado a vigilar:** el marcado de `IsPublic::renderContent()` lo repinta `asset/js/ui/visibility.js` tras un cambio en lote — anotado en ambos ficheros; es el mismo tipo de acoplamiento que ya causó la regresión `.column-is_public`→`.column-oerIsPublic` dentro de la rebanada. **Verificado:** PHPCS PSR-12 en verde y **33/33** `node --test`; render comprobado en Chrome headless contra el `style.css` compilado del core reproduciendo el marcado real, a 1300 y 1540px y en **modo `stack` de tablesaw** (≤640px, donde el riel pasa al canto de la ficha y las anchuras y la cabecera pegajosa se desactivan); sonda de desbordamiento en estrecho: `scrollWidth == clientWidth`. **NO verificado y declarado como tal:** no se ha renderizado en el admin real logueado (sin credenciales). **Caveat que el propietario debe pesar:** el riel se apoya en `AlignmentStatus`, que **hoy tiene varianza cero** —los 19 REA están en `complete`, medido en TASK-027 y reconfirmado en esta rebanada—, así que sobre el catálogo real el canto se verá **uniformemente verde** hasta que entren REA sin alinear; y **ADR-0013 planea retirar esa columna** en la v2. Si la v2 la retira, el riel debe re-anclarse a la señal que la sustituya (integridad o gobernanza), no eliminarse: el mecanismo es la escala visual, no la property. **ADR-0014 encargado por el propietario (2026-07-29) y ACEPTADO por él (2026-07-30), sin cambios:** tras explicarle la diferencia entre registrar un hecho y fijar una norma, encarga elevar a norma **solo lo sistémico** — heredar el chrome del anfitrión y gastar el presupuesto en jerarquía; el rojo del acento reservado al estado que reclama acción y no a los enlaces en reposo; **la distinción que dispara una acción se codifica por forma, no solo por color** (la que solo gradúa severidad puede ir por color si el nombre accesible la enuncia); y un canal periférico continuo (el riel) anclado a la señal que defina la excepción, que es **mecanismo y no property**. Deliberadamente **NO** norma composición, anchuras, qué columnas existen (eso es ADR-0013) ni la elección concreta de glifos. **Aceptado el 2026-07-30: las cuatro reglas son vinculantes para las rebanadas 2-4 y para toda superficie de curación futura.** **Obligación que hereda la rebanada que estrene la columna de integridad:** medir el contraste de la tríada de estado contra **WCAG AA** — hoy no está medido, los valores se eligieron por criterio, y es esa rebanada la que lleva la regla 3 de gobernar tres estados a gobernar ocho (integridad ok/aviso/error, licencia en tres, literal-vs-enlace, proyecto, trazabilidad). Si la medición obliga a mover los hexadecimales de la tríada, se mueven: el ADR norma el criterio, no los valores. **DECISIÓN DEL PROPIETARIO sobre la retirada de la columna de alineamiento (2026-07-29), registrada en ADR-0013 §Afinado:** planteó que retirarla **pierde capacidad supervisora** —en producción habrá **miles de REA** y hace falta control rápido de los no ajustados al currículo—, aceptando comprimir el ancho a un simple ✓ o un ⚠ de advertencia, y confirmando que el control profundo va a la **cola de calidad**. Revisado el spec de TASK-027 §4, el plan **no eliminaba** la capacidad (la columna nueva de **Integridad** la absorbe explícitamente: `missing_alignment` es uno de sus avisos, y el **filtro** se mantenía en todo caso), pero el planteamiento **destapa dos cosas que el plan daba por buenas:** (1) la premisa **«varianza cero» está medida sobre 19 REA** y **no sobrevive a la escala de producción** —es el argumento que sostenía la retirada y solo vale para el catálogo de julio de 2026—; (2) el problema medido no era que la columna sobrase sino que gastaba ~10 em en rendir un rótulo de texto, y eso se arregla comprimiendo. **Implementado ya:** `AlignmentStatus::renderContent()` pasa a **solo glifo** (`✓` completo / `⚠` no completo) con el rótulo íntegro en el nombre accesible y en el `title`; la severidad `parcial` vs `sin alinear` **no se pierde** (color, riel de fila, nombre accesible y el filtro de tres estados, que no cambia). **Hallazgo de implementación:** en una columna de solo glifo **el ancho lo fija la cabecera, no la celda** —«ALINEAMIENTO» en versalitas no baja de ~7,5rem sin partir palabra—, así que la compresión real es 10,5em→7,5rem y no más; se descartó abreviar el rótulo o renombrarlo a «Currículo» porque **colisiona con la columna «Curricular»** que el spec §4 ya planea (fusión etapa+materia). De paso, el bloque de anchuras pasa de `em` a **`rem`**: el layout fijo del core toma la anchura de la fila de cabecera y la `th` va a 11px mientras la `td` va a 13px, así que `em` significaba dos cosas distintas en la misma columna. **Criterio fijado para cuando se estrene la columna de Integridad:** la integridad es la señal **más fiable** de las dos (`AlignmentStatus` da `Completo` a un REA con la materia literal rota, D3, falso positivo que `IntegrityChecker` no comete), así que si conviven la rectora es integridad y el riel se ancla a ella; lo que **no** se acepta es quedarse sin ninguna. 217 tests PHP en verde (ninguno afirmaba sobre este marcado). Las cadenas nuevas (`Visibilidad`, `Público`, `Privado`, `Buscar por título`, `Hacer público`, `%s REA seleccionados`, `Deseleccionar`) **no están en `language/*.mo`** y degradan al literal español, que es el texto correcto. **REBANADA 2 — integridad y gobernanza en la tabla, HECHA Y VERIFICADA (2026-08-05), spec `docs/superpowers/specs/2026-08-03-task-028-rebanada-2-design.md`, commits `fabe205`…`9bf5765`.** Estrena en la UI el `IntegrityChecker` que llevaba desde TASK-005 calculándose en cada guardado y **solo yendo al log**. Entregado: juego de columnas por defecto reequilibrado a 8 (Título, **Integridad** nueva, Curricular nueva —fusiona etapa+materia con dedup por título y marca de literal—, **Tipo de recurso** nuevo, Licencia con estado vacío explícito, Visibilidad, Modificado, y **Anclaje**, renombrada de «Alineamiento» por decisión del propietario); cuatro filtros `nex` de gobernanza en la búsqueda avanzada (sin licencia, sin descripción, sin título, sin tipo) más el filtro computado de integridad con una sola rama en el controlador (resolutor `query → predicado`, reutilizando `ComputedFilter` de la rebanada 1 como su segundo consumidor); riel de fila re-anclado de Anclaje a Integridad (ADR-0013 §Afinado ya lo dejó previsto); semáforo de integridad de **dos estados** (ok/aviso), con `error` definido pero sin pintar porque `dead_link` no tiene productor real (D-5); dos glifos por fila como máximo, cada uno pidiendo una acción distinta (ADR-0014 regla 3); licencia ausente y tipo fuera de vocabulario en tinta apagada y texto explícito, sin glifo. **Reglas de integridad ampliadas (RF-006, D-6):** el anclaje y la licencia se comprueban **siempre**, no solo sin plantilla; se suma `literal_in_link_property` (D-3) como aviso nuevo. **Arquitectura:** `IntegrityChecker` pasa de monolito intocable en host a **proyector puro** que delega en `IntegrityPolicy`/`CurricularSummary`/`ComputedPredicates`, piezas nuevas con TDD real (la clase llevaba desde TASK-005 sin un solo test de host). **Verificación en contenedor sobre el catálogo real (19 REA), arnés nuevo `test/container/columns-check.php`, salida `6 OK, 0 FAIL, 1 SKIP`, exit 0:** `ok=1`, `warning=18`, `error=0`; `dead_link=0` confirma D-5; `literal_in_link_property=4` coincide con los 4 valores literales que TASK-027 había contado, que hasta ahora pasaban por «ok»; `missing_license=18` de 19; la comprobación de D-6 quedó **SKIP** porque ningún REA tiene plantilla asignada todavía (PEND-012 sigue abierto), así que esa rama no se ejercitó sobre datos reales. **Suites: 285 tests PHP (669 aserciones) y 48 tests JS**, subiendo desde 252/37 al empezar la rebanada. **Cuatro hallazgos de la ejecución que no estaban en el plan:** (1) **dos tokens de color de la paleta que la rebanada 1 dejó aprobada no cumplían WCAG AA**, y nadie lo sabía porque nunca se había medido — `--oer-warn` daba 4.17:1 y `--oer-muted` 3.93:1, ambos por debajo de 4.5:1; corregidos a `#955c0d` y `#666d79`, con un test `node --test` nuevo que lee los tokens del propio CSS y falla si alguien los empeora (los valores viejos siguen citados en ADR-0014 como historia append-only, que es lo correcto); (2) **leer una constante de una clase que implementa una interfaz del core provoca un fatal en el host** —acceder a la constante obliga a declarar la clase, y declararla exige la interfaz—, por lo que se extrajo `Service\Governance\AlignmentStatusValue`; no era un problema nuevo, `IndexController` y `MasterViewQuery` ya usaban esas constantes, pero como ninguno tiene test de host nunca había aflorado; (3) **el `// @translate` debe ir en la misma línea que el literal**: en línea aparte rompe `extract-tagged-strings`, que busca hacia atrás y acaba extrayendo la clave del array en vez del texto — detectado ejecutando la herramienta real, no leyendo el código; (4) el riel de la fila colgaba de `data-alignment` en **dos** sitios, no en uno — también en la media query móvil. **Revisiones conscientes registradas en ADR-0013 §Afinado (TASK-028 rebanada 2):** la columna de Licencia no marca sus tres estados en la celda (con 18/19 sin licencia, marcarlos sería marcar la tabla entera) y la columna de Anclaje no se retira. **NO verificado y declarado como tal: la UI en navegador con sesión real** (columna Integridad, columna Curricular, riel re-anclado, filtros nuevos) — entra en la deuda de **TASK-030**, como el resto del JS del módulo. **REBANADA 3a — superficies de lectura del drawer, HECHA (2026-08-11/12), spec `docs/superpowers/specs/2026-08-11-task-028-rebanada-3a-design.md`, commits `aeefa32`…`7116395`.** El drawer estrena dos cosas que el módulo calculaba desde hace tiempo sin mostrar nunca: las incidencias de `IntegrityChecker` (que desde TASK-005 solo iban al log) y el historial de curación de ADR-0015 (eventos `dcterms:provenance`, append-only, con estado previo). **Entregado:** `Service\Governance\CurationHistory` (proyector puro: orden inverso, cambios por dimensión, vaciado total representable — el caso que una value annotation no puede registrar, E-1); `RecatalogService::history()` sobre un `eventsOf()` extraído de `lastEventOf()` que **conserva su desempate por microsegundos** (TASK-007) — es la pieza de la que depende `undo()`, de ahí el arnés de contenedor con ciclo completo; resolución de títulos **cualificada por la dimensión real de cada cambio** (`titlesFor()`/`dimensionsOf()`), corrigiendo un `lrmi:teaches` fijo que habría calificado mal los cambios de ejes temáticos; acción de solo lectura `drawer-details` (sin CSRF, privilegio ACL propio por la lección de TASK-029 — conceder por privilegio, no por controlador) que devuelve integridad e historial en una sola llamada, obligada a pasar por el servidor porque el evento se escribe `is_public => false` (ADR-0015) y el drawer carga el item con `fetch()` sin autenticar (aviso que dejó el cierre de TASK-007); `asset/js/core/integrityModel.js` (agrupa issues por severidad, sin fuga de `severity` redundante en cada incidencia); `asset/js/core/historyModel.js` con `HISTORY_EMPTY_NOTICE` (el aviso de cobertura de E-2) y `TERM_LABELS` reexportado desde `drawerModel.js`; `asset/js/ui/drawerDetails.js` (pinta ambas secciones, enlace al editor nativo de Omeka y lista de medios con aviso de REA sin ningún medio). **Resuelve PEND-013** (E-3): la justificación de la IA se muestra tras un «ver porqué» colapsado, sin anclar al curador antes de decidir porque aquí ya está decidido y escrito. **Sin ADR nuevo** (spec §11: nada aquí fija norma que ADR-0013/0014/0015 no cubran ya). **Suites al cerrar: 311 tests PHP (710 aserciones) y 67 tests JS**, subiendo desde 285/48 al empezar la rebanada; `make lint` limpio. **Corrección a la premisa del spec §4, hallada tras cerrarlo:** el spec midió «0 eventos `dcterms:provenance` en 19 REA» con la **API pública sin autenticar** — la misma ceguera a valores privados que el propio spec documenta en E-4 (§5) para justificar por qué el historial no puede venir del JSON-LD. Comprobado por el coordinador de la rebanada, durante el cierre de esta tarea (2026-08-12), por SQL directo contra la base del contenedor: **los REA #40437 y #40442 sí tienen eventos de curación genuinos**, firmados por `fmatdia`, fechados 2026-08-04 y 2026-08-05 — anteriores al propio spec (2026-08-11). El spec no se reescribe (historia append-only, ADR-0001); la corrección y la lección de instrumento quedan registradas en `docs/project-memory.md` («Estado actual»). **Lo que NO cambia:** el aviso de cobertura del historial (E-2) sigue siendo necesario y correcto — la mayoría del catálogo sigue sin eventos—; lo que cambiaba era solo el número que lo justificaba, no la decisión. **Declarado como no verificado:** el arreglo `7116395` (comprobación de restauración exhaustiva por dimensión, contra conjunto de ids) del arnés `test/container/drawer-details-check.php` **nunca se ha ejecutado** — solo corre en modo `--write`, prohibido mientras el propietario resolvía un item del catálogo; queda pendiente su autorización para una pasada limpia y una pasada deliberadamente corrompida que confirme que el FAIL nombra la dimensión y los ids exactos con salida 1. El modo solo lectura (comprobaciones 1-2, integridad e historial vacío) sí es seguro y no escribe nada. Entra en la deuda de **TASK-030** junto con la UI en navegador con sesión real (sin verificar, como el resto del JS del módulo). | rebanadas 1, 2 y 3a **hechas**; 3b y 4 pendientes de spec | RF-002, RF-003, RF-004, RF-006, RF-008, NFR-006 | TASK-027 (hecha); PEND-011 **resuelto el 2026-08-13** (CustomVocab id=2 «licencias», 7 términos sin versión; la versión irá en la URI canónica de `dcterms:license`, trabajo declarado y sin diseñar). La 3b queda **desbloqueada** y hereda dos encargos: implementar la validación contra el vocabulario —hoy NADA la hace, así que el único REA con el valor heredado `ccbysa` no aparece marcado en ninguna parte— y los tres estados de licencia de ADR-0013, que tampoco están implementados |
| TASK-029 | **Mover la configuración del módulo del listado de Módulos al menú lateral del admin, colgando de la entrada «OER Manager» como una entrada «Configuración», con ACL propia.** Pedido por el propietario el 2026-07-28. **Situación actual:** la configuración vive donde Omeka pone la de todos los módulos (`module.ini` con `configurable = true` → `getConfigForm()`/`handleConfigForm()` en `Module.php`), a la que solo se llega por *Módulos → OER Manager → Configurar*; es poco descubrible y queda lejos de la vista maestra, que es donde el curador trabaja. Además el formulario ya tiene 4 bloques (currículo, gobernanza, LLM, visión) y sigue creciendo. **Alcance:** ruta hija de `admin/oer-manager` (p. ej. `/admin/oer-manager/configuracion`), entrada en la navegación del módulo, acción de controlador que renderiza y guarda el mismo `ConfigForm`, y **privilegio ACL propio** —hoy `Module::onBootstrap` concede a `editor` **el controlador entero sin lista de privilegios**, así que una acción nueva sería alcanzable por `editor` por herencia (el mismo roce con NFR-003 ya registrado en ADR-0013 §8.3): hace falta `$acl->allow()` por privilegio, no por controlador. **Rol DECIDIDO por el propietario (2026-07-28): `site_admin` y por encima, es decir `global_admin` + `site_admin`.** «Supervisor» **sí es** un rol nativo de Omeka —corrige la nota anterior de esta fila—: es la **etiqueta** de `site_admin`, verificado en el core del contenedor (`application/src/Permissions/Acl.php:22`, `self::ROLE_SITE_ADMIN => 'Supervisor' // @translate`; `site_admin` está además en `$adminRoles` junto a `global_admin`). Coherente con NFR-003, que ya reserva a esos dos roles la acción de gestor sobre el proyecto (`schema:isPartOf`), y con que la configuración incluye la **clave API del LLM** y los vocabularios que gobiernan todo el catálogo. **Dos hallazgos verificados en la instalación real que condicionan la implementación:** (1) `site_editor` (3 usuarios) **no es rol nativo**: lo añade el módulo **IsolatedSites** (`Module.php:221`, `$acl->addRole('site_editor', Acl::ROLE_EDITOR)`) **heredando de `editor`** — luego el `allow(['editor','site_admin'], [IndexController::class])` actual de `OERManager\Module::onBootstrap` le concede hoy el controlador entero por herencia, y una acción de configuración colgada de ese controlador sería alcanzable por `editor` y `site_editor`; confirma que el arreglo es `allow()` **por privilegio (lista de acciones)**, no por controlador. (2) El reparto real de roles es `site_editor` 3, `global_admin` 2, `editor` 2, `researcher` 2: **ningún usuario tiene hoy `site_admin`**, así que al aplicar la decisión solo los 2 `global_admin` entrarán a la configuración hasta que se asigne el rol Supervisor a alguien. **Convivencia DECIDIDA por el propietario (2026-07-28): solo el menú lateral** — `configurable = false` en `module.ini`, desaparece el botón «Configurar» del listado de Módulos y queda una única puerta con un único criterio de permisos. Motivo: convivir dejaría dos puertas al mismo formulario con ACL distinta (la de Módulos la protege Omeka por su cuenta), que es justo la ambigüedad que la tarea viene a quitar. **Consideraciones:** mantener el tratamiento write-only de la clave API; y conservar CSRF. No cambia ningún setting ni el contenido del formulario: es dónde vive y quién entra.  **HECHA Y VERIFICADA EN CONTENEDOR (2026-08-01).** Sin ADR nuevo: las dos decisiones (rol y convivencia) ya eran del propietario y la granularidad de ACL cierra una deuda que ADR-0013 §8.3 tenía registrada, no abre gobierno. **Entregado:** `ConfigPayload` (nuevo, puro) se lleva las ~120 líneas de mapeo settings↔POST que vivían en `Module.php`, donde no había forma de probarlas —`getConfigForm()` exige el contenedor de Omeka—; `Module::getConfigForm/handleConfigForm/governanceIdSettings` desaparecen; `IndexController::configAction()` + `view/oer-manager/admin/index/config.phtml` sirven el formulario; entrada de navegación hija con `privilege` (sin él, un editor vería un enlace que le devuelve 403); `module.ini` a `configurable = false`. **Ruta `/admin/oer-manager/config`, no `/configuracion`:** el enunciado decía «p. ej.» y las 12 acciones existentes van en inglés con guiones. **ACL por privilegio:** curación (12 acciones) a `editor`+`site_admin`; `config` solo a `site_admin` (a `global_admin` ya se lo da el core, `AclFactory::addRulesForGlobalAdmin`). Los privilegios son los nombres de acción **con guiones**, que es lo que compara `MvcListeners::authorizeUserAgainstController`. **DEFECTO ENCONTRADO AL VERIFICAR, que el plan no preveía:** el formulario **nunca se había validado** —la página de Módulos de Omeka lee el POST directamente y no llama a `isValid()`—, así que al pasar a servirlo nosotros salió que los `Number` de Laminas son **obligatorios por defecto** y los tres ids de gobernanza vacíos invalidaban el formulario entero: un módulo recién instalado no habría podido guardar su propia configuración, y con PEND-011 abierto el vocabulario de licencias *tiene* que poder estar vacío. Resuelto con `getInputFilterSpecification()`: nada es obligatorio salvo el CSRF, porque «vacío = sin configurar» es un estado de diseño de ADR-0013. **Verificado en contenedor:** `test/container/acl-check.php` **25/25** (rol a rol: `editor` y el `site_editor` que añade IsolatedSites conservan la curación y no entran a la configuración) y `test/container/config-page-check.php --write` **10/10** (rinde los 21 campos y el CSRF; la clave API se pinta vacía y **no aparece en ninguna parte del HTML**; y la ida y vuelta del guardado conserva la clave con el campo en blanco sin mover ningún otro setting). **Trampa del arnés que casi da un falso verde:** invocar `configAction()` directamente hace que `getRequest()` devuelva la petición del CLI, así que el POST se leía como GET y las comprobaciones del guardado pasaban **en vacío**; se arregló despachando con `dispatch()` y añadiendo una comprobación que distingue las dos ramas por lo que devuelve la acción. **No regresión:** la vista maestra sigue rindiendo sus 19 filas con el panel de re-catalogación activo. **Verificado por el propietario en la instancia real (2026-08-01):** la entrada «Configuración» aparece en el menú lateral, la página carga y el formulario guarda. Con eso, la tarea no deja deuda de verificación en TASK-030 | hecha | NFR-003, RF-002, ADR-0013 | — |
| TASK-024 | **La extracción de texto de PDF no funciona en el contenedor (dos defectos encadenados).** Hallado en la verificación funcional de TASK-019/022 (2026-07-22), invisible para los 163 tests del host. **(a) El módulo nunca registraba su propio autoloader:** Omeka NO autocarga el `vendor/` de un módulo y el core no trae `smalot/pdfparser`, así que `Smalot\PdfParser\Config` no existía en runtime; `ContentExtractor::parsePdf()` capturaba el `Error` y marcaba **todo** PDF como `pdf_unreadable` en silencio. Los tests no lo veían porque `test/phpunit.xml` bootstrapea `vendor/autoload.php` directamente. **Arreglado (2026-07-22):** `Module` implementa `InitProviderInterface` y hace `require_once` guardado del autoload en `init()` — se declara la interfaz explícitamente porque `Omeka\Module\AbstractModule` solo implementa `ConfigProviderInterface` y el `InitTrigger` de Laminas solo invoca `init()` sobre un `InitProviderInterface` (copiar el idioma de BulkExport sin la interfaz habría sido otro no-op silencioso). El guard mantiene el módulo cargable si se distribuye sin `vendor/`. Lint PSR-12 y 163 tests en verde. **(b) PENDIENTE — `iconv //TRANSLIT` no existe en Alpine/musl:** con el autoloader ya arreglado, `smalot/pdfparser` sigue devolviendo **0 chars** en el contenedor. Causa raíz confirmada por A/B sobre el MISMO PDF (item #4674, `2a07d892…pdf`): **host (glibc/libiconv) → 901 chars** incluido el «1º ESO MATEMÁTICAS» de la referencia del corpus; **contenedor (Alpine/musl) → 0 chars**. `Font.php:651` hace `iconv($enc, 'UTF-8//TRANSLIT//IGNORE', $text)` y en musl eso devuelve `false` (verificado: `iconv('CP1252','UTF-8//TRANSLIT//IGNORE',…)===false`, `ICONV_IMPL='unknown'`; sin `//TRANSLIT` sí convierte). **Impacto:** en el despliegue real ningún REA en PDF aporta texto — o se pierde (con `vision_enabled` off, que es el default) o se rescata por visión. Invalida el diagnóstico `pdf_empty`=«PDF escaneado» de #40442 en TASK-022, que se hizo en host. **El arreglo es de imagen Docker, NO del módulo** (CLAUDE.md: la config Docker no forma parte del módulo) → decisión del propietario. **Investigación de vías (2026-07-22, con permiso del propietario):** (i) **`gnu-libiconv` + `LD_PRELOAD` NO funciona** en esta imagen — Alpine 3.23 empaqueta `gnu-libiconv` 1.18-r0 **sin** `preloadable_libiconv.so` y exportando solo símbolos con prefijo (`libiconv`, `libiconv_open`, `libiconv_close`), así que no hay interposición posible; probado e instalado/desinstalado en el contenedor, `ICONV_IMPL` seguía en `unknown`. Vía muerta: no recomendar. (ii) **Una imagen con glibc SÍ lo resuelve, sin tocar código**: mismo PDF (#4674) en `php:8.4-cli` (Debian) → `ICONV_IMPL: glibc`, sonda `//TRANSLIT` OK, **901 chars**, idéntico al host. (iii) **Alcance real del daño mayor de lo estimado:** el mapa de `smalot/pdfparser` incluye `WinAnsiEncoding → CP1252`, y `WinAnsiEncoding` es la codificación más común en PDF — no es un caso raro de MacRoman: en Alpine se pierde el texto de casi cualquier PDF estándar. (iv) **Sin arreglo upstream disponible**: v2.12.5 (2026-04-17) es la última publicada y sigue con el `//TRANSLIT`; el arreglo sería de una línea y demostrablemente seguro (en el contenedor `mb_convert_encoding($t,'UTF-8','Windows-1252')` da salida **idéntica** a `iconv` plano, y la propia librería ya lo usa en las líneas 557/605/682). (v) **Obstáculo para la vía glibc —SUPERADO el 2026-07-30, ver más abajo:** se dio por hecho que no existía imagen Debian de Omeka-S 4.2 mantenida (`erseco/alpine-omeka-s` es Alpine en todos sus tags; `omeka/omeka-s` no se actualiza desde 2017), y que adoptarla implicaría construir y mantener un Dockerfile propio desde `php:8.4-fpm`. **Era cierto para el nombre de imagen que se miró, no para el proyecto:** el mismo autor publica `ghcr.io/erseco/omeka-s-docker` —el nombre ya no lleva «alpine»— que es **Debian 13 trixie con glibc**. Sonda de diagnóstico para validar cualquier imagen candidata: `php -r 'var_dump(ICONV_IMPL, @iconv("CP1252","UTF-8//TRANSLIT//IGNORE","a"));'` — debe devolver un string, no `false`. **Decisión del propietario (2026-07-22): degradación honesta en el módulo ahora + imagen glibc cuando pueda.** **Degradación HECHA y verificada en contenedor (2026-07-22, TDD):** `ContentExtractor` gana el límite inyectable `iconv_translit_supported` (`null` = sonda en runtime, evaluada una vez; inyectable porque el host tiene glibc y **nunca podría probar la rama rota**) y, cuando un PDF vuelve vacío en plataforma degradada, reporta **`pdf_iconv_unsupported`** en lugar de un `pdf_empty` que miente; `AiCataloguer::rescuablePdfs()` trata el motivo nuevo igual que `pdf_empty`, así que la visión lo sigue cubriendo sin cambio de comportamiento. 166 tests verdes (404 assertions; +3), lint PSR-12. Verificado en el contenedor real: el mismo PDF #4674 que reportaba `pdf_empty` ahora reporta `pdf_iconv_unsupported`. **Residuo (propietario):** la imagen glibc, que es lo único que recupera el texto; mientras tanto `vision_enabled` es obligatorio de facto para PDF. Pendiente también (propuesto, no implementado): aviso en el ConfigForm cuando la plataforma esté degradada **y** la visión apagada — hoy esa combinación deja los PDF sin aportar nada, en silencio. Alternativa dentro del módulo a evaluar si no se toca la imagen: detectar el `iconv` roto y degradar explícitamente a visión en vez de fingir `pdf_empty`. Bloquea la verificación en contenedor de los items PDF del corpus (#4674, #40437, #40442). **VÍA GLIBC RESUELTA Y VALIDADA (2026-07-30), pendiente solo de que el propietario cambie la imagen:** el obstáculo (v) era falso — el mismo autor publica **`ghcr.io/erseco/omeka-s-docker:master`**, Debian 13 trixie / PHP 8.4.22 / Apache 2.4 / **glibc**, sucesor del `alpine-omeka-s`. No hace falta Dockerfile propio. Validado levantando una **pila paralela en el 8081 sobre copias de los dos volúmenes** (ficheros y BD), sin tocar el entorno del 8080: arranca (raíz 200, `/admin` 302), conserva los 12.608 items y los 19 REA, y OERManager/CustomVocab/Log quedan activos. **Medición A/B con el `ContentExtractor` real sobre los 10 PDF de los REA: 10.525 → 39.274 caracteres.** **El hallazgo que cambia el tamaño del problema: la pérdida PARCIAL.** Solo 2 de 10 daban `pdf_iconv_unsupported`; otros **5 reportaban `OK` con el texto mutilado** —dos de ellos perdían el **93 %** (105→1451 y 57→838 chars)— sin motivo de descarte, sin aviso y sin traza: el catalogador IA clasificaba con una fracción del contenido creyendo tenerlo entero. Eso explica que el defecto sobreviviera tanto, y **obliga a leer el diagnóstico de la sonda de plataforma, no los recuentos por fichero**, porque `pdf_iconv_unsupported` solo salta cuando el texto queda vacío del todo. Corolario para TASK-022: el `pdf_empty`=«PDF escaneado» del #40442 hay que re-medirlo. **Entregado (2026-07-30):** sonda reutilizable `tools/pdf-check.php` + objetivo `make pdf-check`, que corre el `ContentExtractor` real dentro de cualquier despliegue y **sale con 1 si la plataforma no sabe leer PDF** (apta para smoke test); verificada en las dos imágenes (Alpine exit 1 con `pdf_iconv_unsupported` sobre 129 PDF reales; Debian exit 0). Procedimiento completo de redespliegue de la pila de pruebas, escollos de la migración y vías descartadas en **[referencia/despliegue-pruebas-pdf.md](referencia/despliegue-pruebas-pdf.md)**. **Dos escollos que rompen la migración y no son opcionales** (hallados al levantarla, ninguno estaba previsto): (1) **`local.config.php` ausente** → fatal de Laminas al arrancar, porque en Debian `config/local.config.php` es un symlink al volumen que queda colgando y `glob()` lo casa por nombre, mientras en Alpine `config` es symlink a un `volume/config` que solo tiene `database.ini` y no casa nada; el entrypoint **no** lo crea. (2) **UID 65534→33**: el volumen viene de Alpine con `nobody` y modo 755, la imagen Debian corre como `www-data`, así que arranca bien pero **no puede subir ficheros**; hace falta `chown -R 33:33`. Menores: la imagen expone **80** (no 8080), el `.ini` va a `/usr/local/etc/php/conf.d/` (no `/etc/php84/`) y usa **Apache**, así que el montaje de `nginx.conf` sobra. **La pila de pruebas se desmontó y la imagen se borró tras validar** (~7 GB): rehacerla con el runbook cuando toquen las pruebas funcionales del final del desarrollo. **ESCOLLO SEXTO, hallado el 2026-08-02 al medir TASK-022 sobre la pila rehecha: la imagen Debian NO trae la extensión `zip` de PHP** (Alpine sí), así que `ZipArchive` no existe y **ningún REA empaquetado aporta contenido** — y son **9 de los 19**. Cambiar la imagen tal cual **no es una mejora neta**: sustituye la pérdida de texto en PDF por la misma pérdida en ZIP/SCORM. El cambio de imagen debe llevar `zip` instalado; el lado del módulo (degradar en vez de morir) es TASK-031. | (a) hecha / (b) **vía resuelta y validada 2026-07-30; pendiente del cambio de imagen por el propietario, que debe incluir la extensión `zip` (ver TASK-031)** | RF-009, RF-012, NFR-010, ADR-0007, ADR-0011 | — |
| TASK-023 | **Justificación de saberes/criterios en el clasificador.** El propietario quiere que, además de proponer curso/materia/saberes/criterios, el clasificador añada una **justificación breve de por qué** eligió cada saber/criterio, y que se **persista como value-annotation** sobre el valor del alineamiento, igual que la auditoría de la re-catalogación (quién/cuándo/qué, ADR-0002). Alcance **solo saberes (`lrmi:teaches`) y criterios (`lrmi:assesses`)**: curso y materia se derivan de las hojas (Fase D, ADR-0010), sin selección LLM que justificar. **Diseño aprobado (brainstorming 2026-07-07, defaults):** (1) persistencia como `dcterms:description` dentro del `@annotation`, junto a contributor/modified/provenance; (2) el `propose` genera la justificación pero la escritura ocurre en el `apply` tras confirmación del curador (ADR-0007) → la justificación **viaja oculta con la propuesta a través del panel** (dato `data-justification` en el chip, no se renderiza) hasta el POST del apply; (3) sin UI visible. Cambios **aditivos**: el mapa de alineamiento, preview/apply, scorer e integridad no cambian de forma. Componentes: `ResponseParser::parseSelections()` (índice→porqué, degradante), `PromptBuilder::buildSelectionPrompt($withReason)` (solo pasos finos piden `{"i":n,"why":"…"}`), `CurricularClassifier::getJustifications()` (patrón `TraceableInterface`), `AiCataloguer`/`IndexController` transportan el mapa, `RecatalogService::apply()` gana 4º arg opcional y anota `dcterms:description`. Seguridad: justificación como **dato** (spec §6), literal acotado (~200 chars); la integridad del alineamiento la garantiza `invalidTargets()` (sin cambios). Coste: solo 2 pasos piden texto breve (NFR-008/TASK-020). Spec `docs/superpowers/specs/2026-07-07-justificacion-saberes-criterios-design.md`, plan `docs/superpowers/plans/2026-07-07-justificacion-saberes-criterios.md`. TDD real en host; verificación en contenedor diferida. **Hecha (host, 2026-07-08):** `ResponseParser::parseSelections()` (índice→justificación, degradante: tolera `why` ausente y enteros pelados → nunca se pierde una selección); `PromptBuilder::buildSelectionPrompt($withReason)` (contrato `{"i":n,"why":…}` solo en pasos finos, justificación ≤15 palabras); `CurricularClassifier` con `askWithReasons`/`captureJustifications` + accesor `getJustifications()` (dimensión→{itemId→texto}, reseteado por `classify()`); `AiCataloguer::propose()` expone `justifications`; **glue (verif. contenedor):** `RecatalogService::apply()` gana 4º arg opcional y anota `dcterms:description` (≤200 chars) en el `@annotation` de teaches/assesses; `IndexController` devuelve `justifications` en `aiPropose` y las recoge en el apply (`collectJustifications`, acotadas); JS guarda `data-justification` oculto en el chip y lo emite como `justification[term][id]` en el POST. **163 tests verdes** (399 assertions; +7), lint PSR-12, JS `node --check` OK. **VERIFICADA EN CONTENEDOR (2026-07-22):** `apply` real sobre el item #3181 autenticado como `editor@example.com` (rol mínimo autorizado, NFR-003) con las justificaciones de un `propose` real. El JSON-LD del item confirma los tres criterios: (1) `dcterms:description` aparece como **cuarta** clave del `@annotation` con el texto real de la justificación (p. ej. sobre `PC9N03SBI.2.1`: «Aborda la identificación de estructuras celulares y su función.»), junto a contributor/modified/provenance; (2) aparece **solo** en `lrmi:teaches` y `lrmi:assesses`, y **no** en `educationalLevel`/`about`/`relation`; (3) `dcterms:title` y `dcterms:description` del item **se conservan** — el patrón `clear_property_values`+`append` (trampa crítica de la skill `recatalogador`) no borra el resto de metadatos. Arnés reusable en `test/container/`. | **hecha (verificada en contenedor)** | RF-009, RF-010, NFR-002, NFR-008, NFR-010, NFR-011, ADR-0002, ADR-0007, ADR-0010, ADR-0011 | TASK-015 |
| TASK-030 | **Revisión funcional completa sobre la imagen Debian/glibc, en los sprints finales.** Pedida por el propietario el 2026-07-30. **Motivo:** el módulo se ha construido y verificado casi siempre en el HOST (tests) y en el contenedor **Alpine** (glue), y hay una deuda de verificación acumulada que ninguna de las dos cubre. Al resolverse la vía glibc en TASK-024(b) existe por fin un entorno donde el catalogador trabaja con el contenido **completo** de los PDF —en Alpine perdía texto entero o parcialmente, ver ahí—, así que **cualquier medición de calidad de la IA hecha hasta hoy queda en entredicho** y hay que rehacerla sobre esta imagen. **Requisito previo:** levantar la pila con el runbook [referencia/despliegue-pruebas-pdf.md](referencia/despliegue-pruebas-pdf.md) (procedimiento, escollos y vías descartadas ya documentados) y comprobar con `make pdf-check` que la plataforma da `//TRANSLIT: SÍ` **antes** de medir nada. **Alcance — la deuda concreta, recogida de las tareas que la declararon, no inventada:** (1) **UI en navegador con sesión real**, que nunca se ha ejercitado por falta de credenciales: vista maestra rediseñada (ADR-0015: riel de alineamiento, glifos ✓/⚠, barra de selección, chips de filtros), drawer, re-catalogador con preview/apply, y el **ciclo de guardado de columnas por curador** de TASK-028 rebanada 1, que se pintó pero jamás se guardó de verdad. (2) **Propose IA asíncrono end-to-end** (TASK-020): progreso pintado, botón cancelar, reenganche por `localStorage` y ausencia de 504 — quedó declarado como *UI navegador diferida* porque gasta tokens. (3) **Re-medir la calidad de la catalogación IA con los PDF completos**: en particular el `pdf_empty`=«PDF escaneado» que TASK-022 anotó para el ítem **#40442**, diagnosticado en host y por tanto no fiable, y las fichas destiladas del corpus de TASK-022/025, que se midieron con texto mutilado. (4) **Comportamiento a escala**: el riel y los glifos de ADR-0015 solo se han visto sobre **19 filas** y se diseñaron para miles; el filtro «parcial» de D4 **nunca se ha ejercitado con datos reales** porque los 19 REA están todos en `complete`. (5) **Medición de contraste WCAG AA** de la tríada de estado, que ADR-0015 declara exigible y asigna a la rebanada que estrene la columna de integridad. (6) Glue de contenedor que quedó *pendiente de verificar* en TASK-003/004/005/010 y el residuo de TASK-023. **Criterio de aceptación:** cada punto se cierra con evidencia reproducible (captura, salida de comando o traza), y lo que no se pueda verificar se declara explícitamente como tal en vez de darse por bueno — la regla que ha regido todo el proyecto. **Depende de** que el propietario cambie la imagen en `docker-compose.yml` (la config Docker no es del módulo) o, como mínimo, de levantar la pila de pruebas del runbook. **Alcance ampliado por la medición de TASK-022 (2026-08-02), tres puntos nuevos que salieron al medir:** (6) **la ficha destilada ya está medida y en verde** sobre glibc (§6 del runbook), así que este punto (3) se reduce a re-medir la *clasificación*, no la ficha; (7) **conseguir un REA con PDF escaneado**: #40442 perdió sus medios y era el único caso de ese tipo, así que la rasterización local de TASK-026 sigue sin ejercitarse sobre datos reales; (8) **el «Nivel citado textualmente» no poda la cascada curricular** —#4674 cita «1º ESO» y recibe además 1º Bachillerato/«Matemáticas I»; #40442 tiene «1º Primaria» literal en metadatos y recibe Infantil de 3/4/5 años—: medir cuánto pesa ese desacuerdo antes de decidir si merece tarea propia. **Puesta al día del alcance (2026-08-11), tras cerrar la rebanada 2 de TASK-028:** varios puntos han quedado cerrados o caducados por trabajo posterior y se anotan aquí para no volver a medirlos. **(5) Contraste WCAG AA: HECHO** — y no solo medido: automatizado en `test/js/contrast.test.js`, que lee los tokens del propio CSS y falla si alguien los empeora. La medición destapó **dos fallos reales** en la paleta que ADR-0014 dio por buena sin medir: `--oer-warn` 4.17:1 y `--oer-muted` 3.93:1, ambos por debajo de 4.5:1; corregidos a `#955c0d` y `#666d79`. **(4) La premisa del filtro «parcial» CADUCÓ:** decía que nunca se había ejercitado porque los 19 REA estaban todos en `complete`; con ADR-0016 (curso huérfano ⇒ parcial) el reparto es **12/7** y el filtro ya tiene datos reales. Sigue abierto solo el comportamiento **a escala de miles de filas**. **(3) Se reduce a la CLASIFICACIÓN:** la ficha destilada quedó medida y en verde sobre glibc en TASK-022 (runbook §6, corpus 18/18 con 3 repeticiones y LLM real). Lo que falta es medir si el catalogador **acierta el anclaje** con el texto completo. **(1) Parcialmente ejercitado:** el propietario revisó la vista maestra renderizada en el admin real el 2026-08-10 y de ahí salieron dos defectos que ninguna revisión previa había visto (la celda curricular insinuaba que «Biología y Geología» estaba en Infantil, y 7 de 19 REA tenían un curso que ninguna materia sostenía). Siguen sin ejercitarse el drawer, el re-catalogador con preview/apply y el ciclo de guardado de columnas por curador. **CRITERIO OBSOLETO, corregido:** el punto (1) exigía comprobar «el riel de alineamiento y los glifos ✓/⚠ de ADR-0015». Ya no existen así: el ✓ se retiró por decisión del propietario (2026-08-10, lo correcto no gasta tinta) y el riel se **re-ancló a integridad** en la rebanada 2. Lo que hay que comprobar es el riel de integridad y los glifos ⚠/✗ de la columna «Anclaje curricular». **(6) Parcialmente cubierto** por los arneses de contenedor que la rebanada 2 dejó: `columns-check.php` (8 OK/0 FAIL/1 SKIP) y `search-filters-check.php` (8 OK/0 FAIL). Siguen **íntegramente abiertos**: (2) propose IA asíncrono end-to-end, (7) conseguir un REA con PDF escaneado y (8) el peso del desacuerdo entre «Nivel citado textualmente» y la cascada curricular. | pendiente (sprints finales) | RF-002, RF-009, RF-012, NFR-004, NFR-010, NFR-011, ADR-0013, ADR-0015 | TASK-024(b) (vía resuelta; falta el cambio de imagen) |
| TASK-031 | **La extracción degrada mal cuando falta `ZipArchive` (y la imagen glibc candidata no lo trae).** Hallado el 2026-08-02 al medir TASK-022 sobre la pila del 8081: `ghcr.io/erseco/omeka-s-docker:master` **no incluye la extensión `zip`** de PHP (Alpine sí), y `ContentExtractor::extractZip()` instancia `new \ZipArchive()` sin guarda, así que el fallo **no degrada** —como sí hace el PDF con `pdf_iconv_unsupported` desde TASK-024— sino que propaga un `Error: Class "ZipArchive" not found` que **tumba el propose entero**. Impacto: **9 de los 19 REA son `application/zip`** (SCORM Netex), o sea que la imagen candidata cambia un agujero de plataforma por otro del mismo tamaño (Alpine pierde el texto de los PDF; Debian, el de los ZIP). **Dos frentes, uno de cada lado:** (a) **imagen** —añadir `zip` a la imagen antes del cambio permanente, registrado ya como escollo obligatorio en [referencia/despliegue-pruebas-pdf.md](referencia/despliegue-pruebas-pdf.md) §3; es del propietario, la config Docker no es del módulo—. **Frente (a) resuelto y verificado el 2026-08-02:** basta `FROM ghcr.io/erseco/omeka-s-docker:master` + `RUN install-php-extensions zip` (el helper viene ya en la imagen, que es `php:8.4-apache`, y resuelve `libzip` solo); comprobado con la pila del 8081 reconstruida — `ZipArchive` disponible, `ICONV_IMPL` sigue en `glibc`, `make pdf-check` exit 0, coste de tamaño ≈ 0, y los tres SCORM del corpus medidos encima. Queda del propietario cablearlo en el `docker-compose` de `omeka-s-ModuleTemplate` (`build:` en vez de `image:`). **Merece además issue al autor de la imagen:** una imagen de Omeka-S sin `zip` afecta también a los módulos `Zip`/`BulkImport`/`CSVImport`, no solo a OERManager; (b) **módulo** —sondear `class_exists('ZipArchive')` y saltar el medio con un motivo propio (`zip_unsupported`), simétrico al tratamiento del `iconv` roto, para que el propose siga con el resto de medios en vez de morir; el límite debe ser **inyectable**, porque el host tiene la extensión y nunca podría probar la rama rota (misma lección que `iconv_translit_supported`). Bloquea de hecho a TASK-030 sobre la imagen Debian. **FRENTE (b) HECHO (2026-08-02, TDD real en host):** `ContentExtractor` gana el límite `zip_supported` (`null` = sonda `class_exists(\ZipArchive::class)` evaluada una sola vez) y `extractZip()` salta el medio con **`zip_unsupported`** antes de instanciar nada. **3 tests nuevos escritos primero y vistos fallar** (252 tests, 605 assertions; +3): el motivo propio, la **no regresión** donde la extensión sí está, y —el que captura el defecto de fondo— que **los demás medios se siguen extrayendo**: antes, un item con SCORM + PDF perdía también el PDF porque el `Error` tumbaba el propose entero. **Verificado además en la plataforma rota**, que es la única que prueba de verdad esta rama: ejecutando el `ContentExtractor` real dentro de `ghcr.io/erseco/omeka-s-docker:master` **sin** la extensión, la sonda de runtime devuelve `NO`, el ZIP sale como `zip_unsupported` y el `.txt` acompañante llega intacto; forzando la sonda a `true` en esa misma imagen se reproduce el fallo anterior (`Error: Class "ZipArchive" not found`), que es la prueba de que la guarda es lo que lo evita. **Sin cambios en `AiCataloguer`:** el rescate por visión es solo para PDF (`rescuablePdfs`) y un ZIP no se rasteriza, así que el motivo nuevo no entra ahí. **Residuo (a):** cablear la imagen en el `docker-compose` y, si se quiere, el issue upstream | (b) hecha / (a) resuelta y verificada, pendiente de cablear | RF-009, NFR-010, ADR-0011 | — (el frente (a) es del propietario) |
| TASK-032 | **Panel de detalle integrado en la tabla.** Pedido por el propietario el 2026-08-12. El detalle del REA deja de ser un cajón fijo de `28em` anclado a la derecha (`role="dialog"`, `position: fixed`, ADR-0005 §6) y pasa a **abrirse dentro de la propia tabla**: la fila se expande y el panel ocupa todo el ancho, con áreas delimitadas. **Motivo:** el trabajo real del curador es **abrir los archivos del REA** —son lo que hay que juzgar a nivel de calidad— y hoy el panel solo dice cuántos medios hay; además el anclaje se muestra como listas planas por dimensión, así que con dos cursos mezclados no se sabe qué saber pertenece a qué curso. **Entra:** fila expandible a todo el ancho · miniatura junto al título · anclaje **agrupado por curso · materia** con sus saberes y criterios, ejes aparte y bloque explícito para lo que no encaja · lista de medios abribles (nombre, tipo, tamaño; se abren en pestaña nueva) · historial plegado y **cargado solo al desplegarlo** · integridad como banda de veredicto · el panel de re-catalogación recolocado, sin reescribir. **No entra:** todo lo editable, que es la rebanada 3b de TASK-028 y necesita PEND-011 · previsualización en línea de los medios · la cola de calidad (rebanada 4). **Tres hechos medidos que obligan a servir el panel desde una llamada autenticada:** `o:thumbnail_display_urls` no viaja en el JSON del item; `o:media` solo trae `[{@id, o:id}]`, sin nombre ni tipo ni tamaño; y el drawer carga con `fetch(apiUrl)` **sin autenticar**, así que el primer REA que se ponga en privado dejaría de poder abrir su propio panel (defecto latente: hoy los 19 son públicos). **Declarado en el spec §7.1:** solo **4 de los 19 REA** tienen algún medio con miniatura real —los 9 SCORM no pueden tenerla—, así que la miniatura ancla y distingue el tipo, pero **no verifica contenido**; regenerar derivadas es trabajo propio, fuera de esta tarea. **Riesgo principal:** el agrupamiento se equivoca en silencio; se mitiga con `CurricularGrouping`, clase pura con suite en host donde el caso real de los **7 REA con un curso que ninguna materia sostiene** es uno de sus tests. **Verificación en navegador con sesión real declarada obligatoria y no negociable:** es un cambio de layout y ningún arnés puede juzgarlo. Diseño en `docs/superpowers/specs/2026-08-12-task-032-panel-detalle-integrado-design.md` **Hecha (2026-08-13).** Piezas entregadas: `CurricularGrouping` — agrupamiento puro con sus huérfanos, 10 tests (`876b5ac`); `ItemPanelData` — miniatura, medios y anclaje con el curso ancestro resuelto, sin lecturas N+1 (`d03ef92`); `drawer-details` devuelve el panel completo y `drawer-history`, nueva y **perezosa**, con su propio privilegio ACL (`58af6eb`); `detailAreas.js`, modelo puro de áreas con sus **tres** estados (`8fb6285`); la fila expandible sustituye al cajón fijo, `drawer.js` baja de 132 a 113 líneas (`2a1f233`); el pintado — cabecera con miniatura, anclaje agrupado, medios, ficha e historial perezoso (`5b10b05`); la rejilla de dos columnas y las áreas (`397d624`); arnés de contenedor `test/container/detail-panel-check.php`, **10 OK, 0 FAIL** sobre los 19 REA reales (`1299a63`). **Corrección a la cifra de «7 REA con un curso que ninguna materia sostiene» citada más arriba en este mismo párrafo:** el arnés la mide directamente y da **6**, no 7 — `4676, 5047, 37129, 37132, 40425, 40427`. El «7» venía heredado de **ADR-0016 §Contexto** y de la revisión funcional de la rebanada 2 de TASK-028 (2026-08-10/11, ver fila TASK-030). Ni ADR-0016 ni el spec de esta tarea se reescriben (append-only, ADR-0001); la cifra corregida queda registrada aquí y con más detalle en `project-memory.md`. **Hallazgo de método sobre la miniatura, hecho por el implementador del arnés en su autorrevisión:** `$item->thumbnail()` **no sirve** para distinguir miniatura real de icono genérico — da 17 de 19 «reales» porque varios items de distinto tipo de medio comparten literalmente el mismo asset reutilizado (mismo hash SHA1); la señal correcta es `MediaRepresentation::hasThumbnails()` sobre el medio primario, que sí reproduce las **4 de 19** que midió el spec §7.1. **Advertencia sobre el propio arnés, para que nadie se fíe de más:** su comprobación de huérfanos coincide hoy, ítem a ítem, con el `partial=6` que da `columns-check.php` (rebanada 2) — pero **no miden lo mismo**: `partial` es «anclaje incompleto por cualquier causa» (falta etapa, materia, criterio o saber), con o sin curso huérfano; la coincidencia es de los datos de hoy, no una equivalencia que el código garantice. Suites verificadas al cerrar: **323 tests PHP (743 aserciones) y 84 tests JS**, `make lint` limpio (línea base intacta desde la rebanada 3a de TASK-028). **Declarado como no verificado, tal como exige el spec §11 («obligatorio y no negociable»):** la sesión de navegador con sesión real —debe cubrir al menos un REA con dos cursos, uno con el curso huérfano, uno sin medios, uno sin miniatura, y el modo estrecho— **no se ha hecho**; es un cambio de layout que ningún arnés puede juzgar, y queda en la deuda de TASK-030 junto con el resto del JS del módulo | hecha | RF-002, RF-006, RF-008, ADR-0005, ADR-0010, ADR-0013, ADR-0014, ADR-0015 | — |
| TASK-033 | **Pasada de diseño del panel de detalle.** Pedida por el propietario el 2026-08-18: una pasada sobre el panel que cerró TASK-032, respetando la norma —sin tipografías ni paleta propias, el presupuesto en jerarquía y en el único elemento que lo haga memorable— y con el encargo explícito de **simplificar uniendo la re-catalogación con la información curricular**. **Tres defectos de composición que la motivan:** (1) el currículo aparecía **dos veces y separado** —el área «Anclaje curricular» arriba y sus cinco selectores al final del `<td>`, con Integridad e Historial en medio—, y la separación era **accidental**: `.oer-recatalog` es hermano de `.oer-detail-panel` porque `initRecatalog` va detrás de `initDrawerDetails` en `main.js`, así que el orden real quedó al revés del que pedía el spec §6 de TASK-032; (2) **seis encabezados del mismo peso** (Anclaje, Medios, Información, Integridad, Historial, Re-catalogar), ninguno mandando, contra la regla rectora de ADR-0014 §1; (3) **Integridad era la única área que no usaba `.oer-area`** sino `.oer-drawer-section` heredado de TASK-028 3a, así que no recibía las versalitas de `.oer-area > h4` — incoherencia, no decisión. **Restricción que fijó el diseño:** `CurricularGrouping::build()` **deriva** los grupos curso·materia recorriendo el grafo del currículo, pero en RDF el item solo tiene **cinco listas planas**, que es lo que `collectAlignmentPairs()` escribe. Por eso se descartó pintar un selector dentro de cada grupo (prometería una escritura por grupo que no existe, y dejaría sin sitio a los huérfanos y a los REA sin anclaje) y se eligió **sustitución**: lectura agrupada en reposo, selectores planos al editar, en el mismo sitio. **Entra:** el editor se monta dentro del área de anclaje vía evento nuevo `oer:anchor-slot`, con `data-mode` read/edit sobre `.oer-area-alignment` · botón `[Re-catalogar]` y `[Deshacer…]` en una barra de reposo · `Cancelar` reconstruye la fila con `reopenDrawer`, así que los chips sin confirmar **se descartan de verdad** · Integridad deja de ser sección y pasa a **banda con contador** («Integridad · 1 error, 2 avisos», que el spec §6 de TASK-032 pedía y nunca se pintó) o a **marca en la cabecera** cuando no hay incidencias · copy unificado desde `TERM_LABELS`. **No entra, y se declara:** la extracción i18n (`make generate-pot` solo escanea `*.php`/`*.phtml`, así que **ninguna** cadena de `Omeka.jsTranslate()` se extrae y todo el copy del panel es intraducible de facto contra NFR-005; arreglarlo exige tocar el `Makefile`, declarado intocable en `CLAUDE.md`, y merece TASK propia) · unificar las **dos** peticiones del panel (`drawer-details` autenticada + `/api` anónima), sin lo cual un REA privado sigue sin editor · selectores por grupo · el MIME crudo en `.oer-media-meta`. **El elemento memorable, y todo el presupuesto de la pasada: el riel no se corta.** ADR-0014 §4 define el riel como mecanismo anclado a la señal que define la excepción; el `box-shadow: inset` de la fila **lo tapaba el fondo de `.oer-detail-cell`**, así que el canto se rompía justo en la fila que se está mirando. Ahora se repinta como borde y **continúa por el canto del área de anclaje**, hasta el control que arregla lo que denuncia. Sin token nuevo. Para pagarlo se retiran el filete gris entre columnas y el canto ámbar de «Sin encajar», que rimaba con el riel a dos centímetros. **Cuatro fallos de jerarquía que solo aparecieron al maquetar** y que el código no delata: las etiquetas de campo de la ficha (`.oer-area-record dt`) y las del re-catalogador (`.oer-recatalog-dim > label`) iban en versalitas 700, es decir **más marcadas que el rótulo del área que las agrupa**; `.oer-align-axes` iba sangrado como una línea de grupo, así que el eje temático **parecía colgar del último curso** en vez de ser del REA entero; y los encabezados por severidad de la banda sumaban un tercer nivel para tres líneas de contenido — pasan a `visually-hidden`, no se borran, porque ADR-0014 §3 permite graduar por color **siempre que el nombre accesible lo enuncie**. **Copy:** `TERM_LABELS` pasa a ser la única fuente de cómo se llama cada dimensión (antes «Saberes»/«Criterios» en el anclaje y «Saberes básicos»/«Criterios de evaluación» en integridad e historial: la misma dimensión con dos nombres según dónde se pintara) · se quita el `<h4>` del historial, que duplicaba su `<summary>` · el `Cancelar` de la IA pasa a **«Detener la propuesta»**, que si no chocaría con el `Cancelar` nuevo (dos botones vecinos, mismo nombre, distinto efecto) · `Etapa (ayuda, no se guarda)` se parte en etiqueta y pista · `INTEGRITY_OK_TEXT`/`INTEGRITY_UNKNOWN_TEXT` se acortan a «Sin incidencias» / «Integridad no verificada» porque cambian de registro: ya no encabezan una sección, son una marca de cabecera. **No hace falta ADR:** ADR-0014 §«Lo que este ADR NO norma» deja fuera composición, anchuras y glifos, que es todo lo que esta pasada decide. Diseño en `docs/superpowers/specs/2026-08-18-task-033-pasada-diseno-panel-detalle.md` **Hecha (2026-08-18).** **Verificado:** `make test-js` **88/88** con `detailAreas.test.js` y `contrast.test.js` **sin modificar** —no se añadió ningún token de color, así que la medición WCAG de ADR-0014 sigue valiendo—; `make lint` limpio; el panel ejercitado con un stub de DOM en sus **cinco** estados (con incidencias, limpio, no verificado, y los dos degradados: `panel: null` y fetch reventado), que es donde estaba el riesgo real de mover el punto de anclaje — **en ambos degradados el editor se sigue montando**, porque antes colgaba del `<td>` y sobrevivía al fallo por accidente; y maquetado con el CSS real en Chrome headless a 1400px y 760px. **Declarado como NO verificado:** la sesión en el admin real con sesión iniciada, que sigue siendo la deuda que el spec §11 de TASK-032 declara no negociable — no hay `docker compose` accesible desde el repo. Queda en TASK-030 | hecha | RF-002, RF-004, RF-006, ADR-0014 | — |
| TASK-034 | **El panel de detalle vuelve al lateral, sobre el sidebar nativo de Omeka.** Pedido por el propietario el 2026-08-20: se rediseña **cómo se muestra** el panel, no su funcionalidad ni su diseño interno. Deja de abrirse como fila expandida dentro de la tabla (TASK-032) y pasa al sidebar del admin, el mismo que abre el enlace «Detalles» del browse de items. **Decidido en ADR-0017 (aceptado 2026-08-20):** camino nativo completo — `drawer-details` pasa de `JsonModel` a `ViewModel` terminal con plantilla `.phtml`, el disparador es un `a.sidebar-content` con `data-sidebar-content-url`, y el módulo no reimplementa apertura, cierre ni estado de carga. **Medido en el core 4.2 del contenedor, y es lo que obliga a la excepción de anchura:** `.sidebar` es `position: fixed; width: 25%` y `body.sidebar-open #content` baja al 56,25% (a 1400px son ~350px de panel y la tabla encogida a la mitad), con overlay al 100% por debajo de 640px; pero `#sidebar` **lo declara cada vista, no el layout** (`item/browse.phtml:106-112`), así que el módulo declara el suyo y le da su propia anchura sin tocar el core ni afectar a ninguna otra pantalla. **Entra:** el partial servido · el `#sidebar` propio de la vista maestra con su anchura · re-enganchar el re-catalogador y el historial perezoso a `o:sidebar-content-loaded` (el evento del core) en vez de a eventos propios · portar los tres estados de área (`ready`/`empty`/`unknown`) de `detailAreas.js` a una clase pura PHP junto a `ItemPanelData`, con su suite PHPUnit, retirando el módulo JS y sus 13 tests. **No entra:** qué campos muestra el panel (ADR-0013) · el lenguaje visual de lo que se pinta dentro, que sigue siendo ADR-0014 · unificar las dos peticiones del panel (`drawer-details` autenticada + `/api` anónima), sin lo cual un REA privado sigue sin editor. **Gana de paso:** el copy del panel entra por primera vez en el circuito de traducción — hoy `make generate-pot` solo escanea `*.php`/`*.phtml` y **ninguna** cadena de `Omeka.jsTranslate()` se extrae (NFR-005 incumplido de facto). **Cuesta:** se pierde la comparación a todo el ancho que era la razón de ser de TASK-032, salvo en la medida que se le dé al sidebar propio; y hay que decidir qué pasa con el riel de ADR-0014 §4, que TASK-033 continuaba desde la fila hasta el control y en un panel despegado de la fila ya no tiene de dónde salir. **Riesgo principal:** toca el punto de montaje del **re-catalogador**, componente de alto riesgo; el evento cambia de dueño. **Verificación en el admin real con sesión iniciada declarada BLOQUEANTE para cerrar** (ADR-0017): ya van dos tareas de layout cerradas sin ella. Diseño en `docs/superpowers/specs/2026-08-20-task-034-panel-en-sidebar-nativo-design.md`, que fija lo que el ADR deja fuera: anchura `clamp(26rem, 40vw, 44rem)` abierta con `right: 0` —**`left` y `width` están acoplados en el core y ensanchar solo `width` deja el panel descuadrado**, es el fallo más probable de la tarea—; composición **invertida** respecto a TASK-032 (el anclaje se queda todo el ancho porque es lo más ancho que hay, y Medios e Información pasan a dos columnas de 1fr, con container query sobre el panel y no media query de viewport); y el riel de ADR-0014 §4 **se muda al canto del sidebar** en vez de desaparecer, que es el único sitio que le queda cuando el panel deja de tocar la fila. **Implementada (2026-08-20), pendiente de la verificación bloqueante.** Piezas: `PanelAreas` con su suite PHPUnit reproduciendo los 13 casos de `detailAreas.test.js` más uno nuevo —un panel al que le falten claves degrada a vacío en vez de reventar, que en JS no hacía falta porque `undefined` es falsy y aquí sería un 500 **con el sidebar ya abierto**—; `drawerDetailsAction` devolviendo `ViewModel` terminal; el partial `drawer-details.phtml`; el disparador como `a.sidebar-content`; y el `#oer-detail-sidebar` propio de la vista. **`drawerDetails.js` baja de 461 a 204 líneas y `drawer.js` de 178 a 126**: apertura, cierre, colspan, conmutación y estado de carga los hace ahora el core. **Cinco decisiones que solo aparecieron al implementar**, y que el spec no preveía: (1) el `<h3>` pasa a ser hijo directo del sidebar porque el admin le da a `.sidebar h3` una **banda de cabecera propia** con un `::after` gris a sangre —meterlo en un flex con la miniatura la dejaba torcida—, así que se hereda en vez de neutralizarla; (2) el umbral del container query sube de 34rem a **42rem**, porque a 36rem las dos columnas dan ~17rem y los nombres de archivo se partían por la mitad de una palabra; (3) el `data-integrity` lo sube `drawer.js` del partial al sidebar en vez de usar `:has()`, para no colgar la regla de una capacidad del navegador pudiendo ser dos líneas de JS; (4) `reopenDrawer` pierde el parámetro `apiUrl`, que ya no necesita; (5) `data-drawer-details-url` se retira de la tabla: la URL viaja ahora en cada disparador con su id. **Verificado en host:** `make lint` limpio, **337 tests PHP** (778 aserciones) y **75 JS** (los 13 de `detailAreas` retirados con su módulo), y `make generate-pot` confirma la promesa de i18n del ADR — el partial aporta **27 referencias** y el catálogo pasa de 115 a **134 msgids**. **Hecha (2026-08-23): la verificación bloqueante se hizo.** El propietario verificó el panel en el admin real con sesión iniciada; la deuda que ADR-0017 declaraba bloqueante para cerrar —y que TASK-032 y TASK-033 arrastraban desde el 2026-08-13— queda saldada para este panel. **El único defecto que encontró la sesión, y su arreglo, que es CSS puro:** con el panel abierto la tabla quedaba demasiado estrecha para leerse. La causa no era la anchura del panel, sino **heredar la compensación del core**: `body.sidebar-open #content` baja de 81,25% a 56,25%, una resta calibrada para el sidebar del **25%** del core, no para un panel de `clamp(26rem, 40vw, 44rem)` — y la implementación del 2026-08-20 además la recalculaba con la anchura propia, que es lo que la estrechaba todavía más. **Ahora el panel se superpone y la tabla no se encoge:** `body.oer-master-view.sidebar-open #content` fija **81,25%**, que es exactamente el valor que el core da a `div[role=main]` con el sidebar cerrado (el 18,75% restante es el `header` de navegación), así que abrir el panel **no mueve ni una columna**. Hay que declarar un valor porque anular una regla del core lo exige, y `auto` no sirve: `#content` es ítem del `div.flex` del layout admin y quedaría dimensionado por su contenido. Se neutraliza también el desplazamiento del pie (`.sidebar-open footer { margin-right: 25% }` → `0`): si el contenido no se mueve, el pie tampoco. Y el panel gana una `box-shadow` hacia la izquierda, que hace el trabajo que antes hacía el hueco de la compensación — separarlo de lo que ahora le queda debajo. **No hizo falta tocar `z-index`:** el `.sidebar` del core es `position: fixed; z-index: 3` y el `thead` sticky de la tabla es `2`, ambos en el contexto de apilado raíz; el `h1` fijo de cabecera también es `3`, pero ocupa solo los 48px superiores y el sidebar arranca en `top: 48px`. Por debajo de 640px no cambia nada —manda el overlay a pantalla completa del core—, y el umbral es el `@media screen and (max-width: 640px)` real del core, comprobado en su `style.css`, no uno inventado. **Verificado al cerrar:** `make lint` limpio, `make test` **337 tests PHP (778 aserciones)** y `make test-js` **75 JS**; línea base intacta, el arreglo no toca PHP ni JS | hecha | RF-002, NFR-005, ADR-0005, ADR-0013, ADR-0014, ADR-0017 | — |
| TASK-035 | **Una sola llamada autenticada para el panel: el re-catalogador deja de depender de `/api`.** Deuda declarada y aplazada **tres veces** —TASK-032, TASK-033 y TASK-034 la listan en su «No entra»—, dada de alta el 2026-08-23 al cerrar TASK-034. **El defecto, en una línea:** un REA en **privado** no se puede re-catalogar desde el panel. **Mecánica medida en el código actual:** `drawer.js` hace `fetch(row.dataset.apiUrl)` contra `/api`, que conmuta a `NonPersistent`+`KeyAdapter` y **no ve la sesión del admin**, así que un REA privado responde 403/404 **siempre**; el fallo se degrada a `itemJson: null` (no aborta el evento) y `recatalog.js` pinta «Re-catalogación no disponible: no se ha podido leer este REA sin autenticar» y **no monta el editor**. El panel de **lectura** ya no depende de esa llamada desde TASK-034 —lo sirve `drawer-details`, que sí autentica—, así que la petición anónima sobrevive **solo** para prellenar los selectores con lo ya elegido. **Está latente, no visible:** los 19 REA de hoy son públicos, por eso ninguna sesión de verificación lo ha encontrado nunca. **Lo que hace el arreglo barato, y es la razón de darla de alta ahora:** `ItemPanelData::forItem()` ya recorre, autenticado y sin lecturas N+1, los mismos `RecatalogService::ALIGNMENT_TERMS`, y de cada valor ya saca `id` y `title` — que son **exactamente** los dos datos que `buildDimensionSelector` obtiene hoy de `value_resource_id` y `valueText(value)` del JSON-LD crudo. El grafo ya se recorre una vez y ya está pagado; lo que falta es **entregarlo**, no calcularlo. **Entra:** el prellenado viaja con el partial que ya sirve el panel (`drawer-details`), en la forma que decida el diseño · `drawer.js` pierde el `fetch`, y la tabla el `data-api-url` de cada fila · el contrato de `oer:anchor-slot` deja de llevar `itemJson` (JSON-LD crudo) y pasa a una forma propia y estable · `termPicker.buildDimensionSelector` deja de leer JSON-LD · desaparecen la rama `if (!itemJson)` de `recatalog.js` y su copy, que dejan de tener caso. **De paso, a comprobar:** desde TASK-034 nadie usa ya `drawerTitle()` ni `drawerRows()` de `core/drawerModel.js` salvo su propio test (`TERM_LABELS`, del mismo fichero, **sí** se sigue usando); si esta tarea no los revive, se retiran con sus tests. **No entra:** rediseñar el flujo del re-catalogador (preview, confirmación, reversibilidad, acotación en cascada de RF-014) · la ACL · qué campos muestra el panel (ADR-0013) ni su lenguaje visual (ADR-0014). **Riesgo principal:** toca el punto de datos del **re-catalogador**, componente de ALTO RIESGO, y el contrato del evento cambia de dueño por segunda vez en tres tareas; `buildDimensionSelector` es compartido —el selector de Etapa lo llama con `{}` como ayuda que no se guarda—, así que cambiarle la entrada afecta a más de un llamador. **Verificación:** las suites no bastan y no pueden bastar —el defecto vive en un estado (`isPublic = false`) que ningún REA del catálogo tiene hoy—; hay que **poner un REA en privado en el admin real** y comprobar que su panel monta el editor y re-cataloga. Cargar la skill `recatalogador` antes de tocar nada | pendiente | RF-002, RF-003, RF-004, RF-014, ADR-0013, ADR-0017 | — |
| TASK-036 | **Fix: la acotación de Materia perdía el segundo Curso con selección múltiple.** Reportado por el propietario (2026-08-25): con dos Cursos elegidos a la vez (1º Primaria y 2º Bachillerato — cardinalidad múltiple de los cuatro términos curriculares, fijada en PEND-007), escribir «mat» en Materia solo ofrecía Matemáticas de 1º Primaria, nunca Matemáticas II de 2º Bachillerato. **Bug en dos capas, arregladas juntas:** cliente (`termPicker.js`) — `getContext()` llamaba a `firstChipId()`, que solo mira el primer chip de cada dimensión; pasa a reutilizar `chipIdsByTerm()` (ya existía, la usan `searchForm.js`/`aiPropose.js`), que da todos los ids; `firstChipId()` se retira sin usos. Servidor (`CurriculumSearch::contextFilter()`) — `(int) $context['level']` solo leía un id; ahora normaliza a lista con `normalizeContextIds()` (nuevo, estático, puro) y `searchDimension()` lanza una consulta por ancestro y combina resultados, en vez de una fila `res` con varios ids — no se intentó una única consulta `OR` porque `buildPropertyQuery` del core concatena el WHERE como texto plano sin paréntesis entre filas: «tipo AND ctx1 OR ctx2» se lee «(tipo AND ctx1) OR ctx2», perdiendo el filtro de tipo para el segundo ancestro en adelante. **Verificado:** `test/Service/CurriculumSearchTest.php` (nuevo, 7 tests, pieza pura testeable en host); `test/container/materia-multi-curso-check.php` (nuevo, arnés de solo lectura contra el catálogo real — 1º Primaria + 2º Bachillerato, 7/7 OK, sin duplicados si un curso se repite). `make lint` limpio, `make test` 344 tests/790 aserciones (+7 sobre main), `make test-js` 75 tests sin cambios. Commit `e4c42c7` en rama `fix-recatalogador-contexto-multi-curso`, aún sin fusionar a `main` | hecha | RF-004, RF-014, ADR-0009 | — |
| TASK-037 | **Flujo autor→curador de REAs: propone, revisa, publica (RF-016).** Decisión completa en ADR-0018 (aceptado 2026-08-28), diseño en `docs/superpowers/specs/2026-08-28-flujo-autor-curador-rea-design.md`, plan en `docs/superpowers/plans/2026-08-29-flujo-autor-curador-rea.md`. **Arquitectura, verificada contra el core real antes de escribir código, no inventada:** los roles nativos `author`/`reviewer` de Omeka ya bastaban — `author` solo toca sus propios items (`OwnsEntityAssertion`, `AclFactory::addRulesForAuthor()`), `reviewer` toca cualquiera y tiene el privilegio `view-all` que `author` no tiene; **hallazgo clave:** `view-all` ya gobierna el scoping de TODA consulta de recursos del core (`ResourceVisibilityFilter`, `AbstractResourceEntityAdapter::buildPropertyQuery()`), así que un `author` ya solo ve lo suyo en cualquier listado admin —incluida la vista maestra propia del módulo— sin ningún cambio de código para esa parte. Ningún rol nuevo creado. **Modelo de estado:** `curation:status`/`curation:note`, literales del vocabulario `Curation` **ya instalado** (lo trajo el módulo `Access`, sin relación con esta feature) — sin vocabulario nuevo. Deliberadamente constantes de literal simple (`WorkflowStatus::PROPOSED='Propuesto'`, `::REJECTED='Rechazado'`), **no** `CustomVocab` (a diferencia de licencia/tipo de recurso, ya `CustomVocab` en este módulo): la máquina de estados compara por igualdad exacta, así que un valor editable por un `site_admin` sería un foot-gun que rompería el flujo en silencio si alguien lo renombrara. `Borrador` es la ausencia de valor, no una constante; «Publicado» tampoco es un 4º estado — publicar pone `is_public=true` (señal canónica ya existente, RF-003) y **limpia** `curation:status`, en vez de duplicar el estado en dos sitios. **Piezas:** `WorkflowStatus` puro con TDD real en host, 7 tests (`42fa9ea`) → `WorkflowService`, que lee/escribe sobre el item real, sin test de host posible —misma limitación documentada que `RecatalogService`/`CurriculumSearch`— (`bf03744`) → tres acciones nuevas en `IndexController` (`proposeAction`/`rejectProposalAction`/`publishProposalAction`), reusando el CSRF ya existente del controlador (`924cf23`) → ACL: `reviewer` añadido **junto a** `editor`/`site_admin` como curador de este flujo (no los sustituye), y un privilegio `propose` nuevo, abierto **también** a `author` porque proponer no es exclusivo del curador (`a9c66c3`) → botón inyectado en la página **nativa** del item de Omeka (evento `view.show.page_actions`, verificado contra la plantilla real del core antes de escribir código — no la vista maestra propia, porque el ACL de un autor nunca llega a ella) (`a91c7d0`) → filtro nuevo «Propuestos» en la vista maestra existente, para el curador (`740aed3`). **Hallazgo de gobierno anterior a cualquier código, no posterior:** el borrador inicial del plan para `WorkflowService` repetía el incidente ya documentado de `RecatalogService` (2026-06-25) — `$api->update(..., ['isPartial'=>true])` con valores de propiedades en el payload hace que `ValueHydrator` borre en silencio TODA otra propiedad del item (título, descripción, alineamiento curricular) si no se usa `clear_property_values` + `collectionAction=>'append'`. Se detectó al cargar la skill `recatalogador` **antes** de despachar la primera tarea (obligatorio por cabecera del propio plan) y se corrigió en el texto del plan (commit `7678701`), antes de que Task 1 se dispatchara; cada punto de escritura se reverificó de forma independiente en la revisión de Task 2, no solo se confió en la corrección. **Verificación en contenedor, con ronda de arreglo propia:** `test/container/workflow-check.php` (`7a1e1df`), ejecutado de verdad contra un REA real (#3181, «Partes de la célula») autenticado como `editor@example.com` (usuario mínimo ya establecido en este repo, mismo patrón que `apply-harness.php`/`undo-harness.php`). Ciclo completo propose→reject (con motivo)→re-propose (confirma que el motivo anterior se limpia)→publish (confirma `is_public=true` y `curation:status` eliminado)→transiciones inválidas: **14/14 OK**. Verificado de forma independiente, con una segunda lectura fuera del propio arnés, que título, descripción y las 12 relaciones curriculares del item quedaron byte a byte idénticas tras el ciclo — evidencia directa de que la corrección de `ValueHydrator` funciona contra datos reales con anclaje curricular real, no solo en teoría. **Una ronda de arreglo, sobre el propio arnés, no sobre Tasks 1-6:** la primera ejecución necesitó autenticación (Omeka deniega correctamente la escritura anónima, hallazgo esperado, no defecto); la revisión de Task 7 encontró además dos hallazgos Important heredados del texto del propio plan — un `check(..., true)` tautológico, sustituido por una comprobación real de que el estado inicial es uno de los que el arnés sabe interpretar, y un paso de restauración que no cubría `curation:note` (solo `curation:status`/`is_public`), corregido con el mismo mecanismo atómico `clear_property_values` (`97e4930`). **Residuos explícitos, declarados y no maquillados:** (1) verificación manual en el admin real con sesión iniciada (crear `author`, proponer, revisar como `reviewer`, rechazar, re-proponer, publicar, confirmar visibilidad de botones) — sin navegador ni credenciales en esta sesión agéntica; mismo patrón que TASK-006/TASK-032/033/034, queda en la deuda de TASK-030; (2) **PEND-012** (campos obligatorios de la plantilla REA) sigue `[PENDIENTE]` — `oermanager_rea_template_id` sigue `NULL` en la instalación real; el flujo funciona mecánicamente hoy, pero sin PEND-012 no hay plantilla que guíe qué debe rellenar un autor, que es lo que hace la feature plenamente útil en producción, no solo funcional. **Verificado al cerrar:** `make lint` limpio, `make test` **376 tests/836 aserciones** (+7 de `WorkflowStatus`), `make test-js` **82/82** sin cambios (sin JS nuevo — la UI es formularios PHP server-rendered vía el hook nativo, no el bundle `asset/js/*` del módulo) | hecha | RF-016, NFR-003, ADR-0018 | — |
| TASK-038 | **Enforcement real de que un `author` no pueda autopublicar saltándose el flujo de TASK-037.** Hallado en la revisión final de rama de TASK-037 (2026-08-30): el core de Omeka no aplica ningún control sobre `o:is_public` más allá del permiso genérico `update`, y `author` tiene `update` sobre sus propios items — así que un autor puede marcar su REA público desde el formulario **nativo** de edición de Omeka (no el de este módulo), sin pasar nunca por «Proponer»/revisión/«Publicar propuesta». Documentado como limitación conocida en [ADR-0018 §Addendum](decisions/0018-flujo-autor-curador-rea.md), decisión del propietario de no cerrarlo en la rama de TASK-037. **Cierre real exigiría** un listener `api.update.pre` nuevo que niegue `is_public=true` a quien no tenga el privilegio `view-all` (o sea, no sea `reviewer`+) sobre items `lrmi:LearningResource` — afecta a **toda** edición de item REA, no solo a las que pasan por este flujo, así que necesita su propio diseño (¿bloquea con error, o degrada a «queda en revisión»? ¿aplica solo si el item tiene alineamiento de este flujo o siempre?) antes de tocar código | pendiente | RF-016, ADR-0018 | — |
