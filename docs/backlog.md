# Backlog del módulo OERManager

> IDs `TASK-NNN` correlativos estables; no se reutilizan ni renumeran. Estados: `pendiente` / `bloqueada` / `en curso` / `hecha`. Enlaces a [requirements.md](requirements.md) y [decisions/](decisions/).

| ID | Descripción | Estado | RF/NFR/ADR enlazados | Bloqueada por |
| --- | --- | --- | --- | --- |
| TASK-001 | Resolver con el propietario los pendientes bloqueantes y registrar las decisiones (ADRs nuevos donde aplique). Resueltos el 2026-06-12: PEND-001, PEND-002 (Omeka-S 4.2 + PHP 8.4), PEND-003, PEND-004. Resueltos el 2026-06-15: PEND-005 (mapeo RDF, ADR-0004), PEND-006 (auditoría RDF, ADR-0002), PEND-008 (skills descartadas), PEND-009 (paquete/licencia, ADR-0003). Resuelto el 2026-06-16: PEND-007 (RF/NFR detallados, ADR-0005 vista maestra v1 + ADR-0006 raíces de árboles RDF) — **no quedan pendientes abiertos** | hecha | PEND-001…PEND-009, ADR-0001…ADR-0006 | — (la resuelve el propietario) |
| TASK-002 | Scaffolding del módulo (FASE 1): estructura nativa Omeka-S (`Module.php`, `config/`, `src/`, `view/oer-manager/`, `asset/`), `module.ini` con `omeka_version_constraint = ^4.2.0`. Hecho el 2026-06-12 y verificado en el contenedor real (PHP 8.4.15, Omeka 4.2.0): lint OK, `getConfig()` OK, ini/constraint OK; instalación/activación en el admin verificada por el propietario | hecha | RF-001, NFR-001, NFR-002, NFR-007 | — |
| TASK-003 | Vista maestra v1: column types propios sobre el browse del admin + drawer de detalle fijo + curación de visibilidad (JS jQuery + REST API). Diseño aprobado (ADR-0005). **Hecha (2026-06-16):** `IndexController`, `MasterViewQuery`, `ColumnType\AlignmentStatus`, vista + JS/CSS implementados; `make lint`/`make test` en verde; verificado en el contenedor real contra el catálogo (clase `lrmi:LearningResource` resuelta, 20 items, estado de alineamiento calculado correctamente). PHPUnit en host no puede instanciar clases que dependen del core Omeka (limitación del arnés de TASK-008: bootstrap sin el core) — la lógica de `AlignmentStatus`/`MasterViewQuery` se verificó manualmente en el contenedor en vez de con tests automatizados; cobertura automatizada de esa lógica queda como mejora futura (arnés de integración dentro del contenedor). El alcance completo de RF-002/RF-003 (edición inline de alineamiento, columna de integridad, historial de curación) fijado en `requirements.md` queda para una iteración siguiente sobre esta v1 | hecha | RF-001, RF-002, RF-003, NFR-003, ADR-0005 | — |
| TASK-004 | Re-catalogador curricular y por tags (**entrega 4a, manual**) — **ALTO RIESGO**: diseño explícito en plan mode (2026-06-22), preview + confirmación + reversibilidad (reconstrucción desde auditoría, ADR-0002), revisor en contexto fresco. Autocomplete curricular + tags (lazy-load, NFR-004), config form (ADR-0006), ACL (NFR-003), escritura RDF + value annotations, y **acotación contextual por el grafo curricular** (RF-014, cascada Etapa→Curso→Asignatura→{Saberes, Criterios}, ADR-0009). Mapeo RDF en ADR-0004, raíces en ADR-0006, modelo del grafo en ADR-0009. La capa IA-assist se entrega aparte en TASK-010. **Hecha (2026-06-25):** verificado en el contenedor (escritura correcta de resource values, value annotations `@annotation`, sin borrado de otros metadatos tras corregir el gotcha de `isPartial`→`clear_property_values`+`append`); acotación contextual por el grafo; endurecimientos de la revisión en contexto fresco aplicados (validación de tipo/pertenencia del destino, CSRF en la escritura, sin fuga de errores). Residuos: confirmar acceso efectivo por rol (editor/site_admin/global_admin) en el contenedor; reversibilidad real diferida a TASK-007 | hecha | RF-004, RF-005, RF-014, NFR-003, NFR-004, ADR-0002, ADR-0004, ADR-0006, ADR-0009 | — |
| TASK-005 | Comprobación de integridad: servicio de validación de valores RDF + enganche en eventos. Campos a validar y definición de «completo» fijados (ADR-0004, RF-006). **Hecha (2026-06-18):** `IntegrityChecker` (representation-level, `check(ItemRepresentation): IntegrityResult`) + `IntegrityResult` (value object con status ok/warning/error e issues); registrado en `service_manager.invokables`; `Module::attachListeners` engancha `api.create.post`/`api.update.post` en `ItemAdapter` → vuelca issues al logger de Omeka para items `lrmi:LearningResource`. Reglas (RF-006): dead-link de alineamiento = error; completitud con plantilla REA = campos obligatorios; sin plantilla = alineamiento vivo + licencia presente = warning. Lint/tests en verde (8 tests, 18 assertions). La acción ante errores (bloquear guardado o no) queda para un ADR futuro | hecha | RF-006, ADR-0004 | TASK-002 |
| TASK-006 | Estadísticas: agregadores en `Stats/`, gráficos JS, export CSV. Dimensiones y catálogo de gráficos fijados (ADR-0004, RF-007) | pendiente | RF-007, ADR-0004 | — |
| TASK-007 | Auditoría de curación: value annotations `dcterms` sobre el valor curado (ADR-0002). Incluye la **reversibilidad real** (registrar el estado previo: ids added/removed) que 4a dejó pendiente (B3) | pendiente | RF-008, ADR-0002, ADR-0004 | — |
| TASK-008 | Wiring de hooks en `.claude/settings.json`. **Hecha (2026-06-12):** PostToolUse → `make lint` (aviso, solo `.php`); **Stop hook = `make lint` + `make test`** (ambos verdes; probado lint con violación PSR-12 → exit 2). PHPUnit ^11.5 en `require-dev`, `test/phpunit.xml` y `test/ModuleConfigTest.php` (5 tests, contrato del scaffold) creados | hecha | NFR-001, ADR-0001 | — |
| TASK-009 | Destilar la skill `omeka-module` desde el Omeka 4.2 real del contenedor (anatomía, eventos, ACL, column types, REST API). **Hecha (2026-06-12)**, todo verificado contra `application/src` y módulos instalados | hecha | NFR-001 | — |
| TASK-010 | **Clasificador curricular y por etiquetas con LLM (entrega "4b") — ALTO RIESGO y alta importancia.** Dos clasificadores de items (curricular sobre el grafo ADR-0009; por ejes sobre el set de tags `dcterms:relation`) con motor LLM (ADR-0008); la IA propone y el curador confirma (ADR-0007) reusando el flujo endurecido de TASK-004. Entrada: metadatos + contenido textual de medios (pdf-to-text con `smalot/pdfparser`, descompresión ZIP/SCORM con `ZipArchive`). **Hecha (2026-06-25):** arquitectura puertos/adaptadores con **TDD real en host (66 tests, lint PSR-12 en verde)** — núcleo puro testeable, glue del core aislado tras interfaces. Entregado: `ContentExtractor` endurecido (zip-slip imposible por lectura en memoria + rechazo de traversal, anti zip-bomb por ratio/conteo/tamaño, sin recursión anidada, PDF con `Config` de límite de memoria, whitelist, truncado por presupuesto); cliente LLM con dos adaptadores (`AnthropicClient` Messages API + `OpenAiCompatibleClient`) sobre `Laminas\Http\Client` sin redirecciones (sin SSRF), clave write-only nunca logueada; `PromptBuilder` (contenido delimitado como dato no-instrucción, marcas neutralizadas) + `ResponseParser` tolerante; clasificador curricular **jerárquico top-down** (Etapa→Curso→Asignatura→{Saberes,Criterios}, selección por **índice** sobre lista cerrada) + clasificador de ejes; `AiCataloguer` pre-rellena el panel de 4a (no escribe: el curador confirma con preview/apply); `EvaluationScorer` (accuracy vs REAs ya catalogados, NFR-008). Glue del core (controller `aiPropose`/`aiEvaluate` con CSRF+ACL editor+, `OmekaMediaSource` solo ficheros locales, `CurriculumTermResolver`, ConfigForm+factorías, JS de pre-relleno) verificado por sintaxis/config/lint; ejecución contra el core **pendiente de verificar en contenedor** (igual que TASK-003/004/005). **Revisión adversaria** (workflow: 5 finders + verificación escéptica; 13 hallazgos, 5 confirmados) aplicada: selección por índice (anti truncado/colisión), neutralización de delimitador (anti prompt-injection), Config de PDF (anti DoS), gating de base_url. Diferido: RAG/embeddings, visión (RF-012), lote como Job (RF-011). Dependencia `smalot/pdfparser` autorizada e instalada | hecha | RF-009, RF-010, NFR-004, NFR-008, NFR-010, NFR-011, ADR-0002, ADR-0004, ADR-0006, ADR-0007, ADR-0008, ADR-0009 | TASK-004 |
| TASK-011 | Catalogación IA-assistida en lote como Omeka Job en segundo plano: marcar varios items en la vista maestra → Job que los cataloga con IA. **Comparte la infraestructura de Job con TASK-020** (propose síncrono de un item que agota el timeout del proxy → 504); conviene abordarlas juntas o encadenadas | pendiente | RF-011, NFR-010, NFR-011, ADR-0007, ADR-0008 | TASK-010 |
| TASK-012 | Visión para imágenes en la cascada de extracción de contenido (configurable) | pendiente | RF-012, ADR-0007 | TASK-010 |
| TASK-013 | Modelar `dcterms:type` de los términos del currículo como `skos:Concept` (recurso) en vez de literal; adaptar el filtro del re-catalogador de `eq` a `res`. Afecta al dato del currículo (consumido, no poseído por el módulo) | pendiente | RF-013, ADR-0004, ADR-0006 | TASK-004 |
| TASK-014 | CI/CD en GitHub Actions: crear `.github/workflows/` siguiendo el patrón de `ateeducacion/omeka-s-IsolatedSites` — (a) workflow de **CI** en `pull_request`/`push` (`composer install` + `make check-untranslated`/`compile-mo`/`lint`/`test` sobre PHP 8.4); (b) workflow de **release** en tags `v*.*.*` (`make package VERSION=…` + GitHub Release con notas y ZIP vía `GITHUB_TOKEN`); (c) workflow(s) de apoyo a la **revisión de código** en PR (preview en Omeka-S Playground y/o revisión de seguridad/dependencias). Sin exponer secretos en logs/código; sin tocar el core. **En curso (2026-06-25):** creados `ci.yml`, `release.yml`, `dependency-review.yml` y `pr-playground-preview.yml` + `blueprint.json` raíz en la rama `feature/gh_actions`. Decisiones: la release quita el prefijo `v` del tag para `module.ini`/ZIP; la revisión de dependencias usa `composer audit` (no `dependency-review-action`, porque el repo es **privado** sin GitHub Advanced Security); se corrigió el step duplicado del patrón (`compile-mo`); `blueprint.json` y `.github/` añadidos a `archive.exclude` de `composer.json` para no ensuciar el ZIP de release. **Endurecimiento (revisión de seguridad automatizada):** acciones de terceros (`shivammathur/setup-php`, `softprops/action-gh-release`) ancladas a SHA de 40 chars con comentario de versión; `release.yml` con `permissions` por defecto `contents: read` y `contents: write` solo en el job de release (`GITHUB_TOKEN` ya estaba acotado al step); `.github/dependabot.yml` (ecosistema `github-actions`) mantiene los pines al día. Verificado en el host: lint/test/i18n/`composer audit` en verde, `make package` produce `OERManager-X.Y.Z.zip` con estructura `OERManager/` y restaura `module.ini` a `0.0.0`, YAML/JSON válidos. **Residuo (→ `hecha`):** confirmar el run real de los workflows en un PR de GitHub (requiere `git push`, autorización del propietario, CLAUDE.md §Seguridad) | en curso | NFR-009, NFR-001, NFR-005, NFR-007 | — |
| TASK-015 | **Clasificador semántico bottom-up (rework de TASK-010).** Anclaje bottom-up (ADR-0010): E1 candidatos con `dcterms:description`; delimitación por familia de materia; selección de saberes/criterios cruzando cursos; derivación de curso+materia de las hojas (coherencia por construcción); E2 pre-filtro por bloque. TDD real (PromptBuilder, clasificador, fake) + lint PSR-12. | en curso | RF-009, RF-010, NFR-004, NFR-008, NFR-010, NFR-011, ADR-0004, ADR-0007, ADR-0009, ADR-0010 | TASK-010 |
| TASK-016 | **Recuperación semántica por embeddings (roadmap).** `EmbeddingTermResolver` sobre `TermResolverInterface`: índice en fichero de las descripciones, top-K por coseno, LLM rerank, misma derivación bottom-up. Bloqueada por PEND-010. | pendiente | NFR-004, NFR-008, NFR-010, NFR-011, ADR-0010, PEND-010 | TASK-015 |
| TASK-017 | **Defecto: el contenido de los medios no llega al clasificador IA.** Hallado en las pruebas funcionales de TASK-015 (2026-06-29): el trazado del intercambio LLM (debug añadido en la consola/panel) muestra que el contenido enviado al LLM contiene **solo metadatos del item, no el texto de los medios**. Reproducido con items con medio PDF y con SCORM (HTML en ZIP). Sin ese texto el anclaje curricular es inviable. Diagnóstico pendiente; hipótesis de causa raíz aguas arriba del `ContentExtractor` (que sí tiene TDD verde en host): `OmekaMediaSource::filesFor()` no devuelve rutas locales —resolución `extension()`/`filename()`/`getLocalPath()` del store, o el medio SCORM no se reconoce como `zip`— o el extractor las salta. **Primer paso de diagnóstico:** surfacing de `content.sources` y `content.skipped` (mapa nombre→motivo) en el panel/consola de debug, que hoy solo muestra `content_text`; ese mapa revela el motivo exacto del salto. Verificar en el contenedor (rutas reales del store). **Causa raíz CONFIRMADA (2026-06-29, item #3181 SCORM "Partes de la célula", paquete Netex smartbook):** el ZIP **sí se lee** (está en `sources`), pero todo el contenido útil se descarta por whitelist — `project.json → unsupported:json` (el modelo de contenido Netex vive en JSON), los `.js` de contenido (`2304279631.js`, `1228349175.js`), y **todas** las imágenes `.jpg/.png` (`resources/...celula_graficos_NN.png`, donde está el infográfico real). Del HTML solo sale un shell casi vacío (`PARTES DE LA CÉLULA h1 p`). Es decir: el clasificador trabaja **solo con los metadatos de Omeka** (título+descripción), no con el medio. Para este item los metadatos bastaron para acertar materia/saberes, pero con metadatos pobres fallaría. **Decisión (brainstorming 2026-06-29): ambos**, en dos fases (spec `docs/superpowers/specs/2026-06-29-extraccion-contenido-medios-design.md`). **Fase 1 HECHA (2026-06-29):** extracción JSON genérica en `ContentExtractor` (recorrido recursivo acotado por nodos, filtra ruido técnico, sanea HTML embebido; `.json` tratado como fichero suelto y entrada de ZIP; `json` en la whitelist de `OmekaMediaSource`); plan `docs/superpowers/plans/2026-06-29-extraccion-json-fase1.md`, ejecutado por subagentes con TDD (77 tests verdes, lint PSR-12), revisión final "ready to merge", **verificado en el contenedor** (item #3181: `project.json` ya no se salta, el texto real del recurso llega al LLM). **Fase 2 (visión sobre imágenes, configurable) → TASK-012/RF-012.** | hecha (Fase 1) | RF-009, RF-010, RF-012, NFR-010, NFR-011, ADR-0007 | TASK-015 |
| TASK-018 | **Refactor de la secuencia de clasificación curricular (afinado de ADR-0010).** **Verificado (2026-06-29) en el contenedor con el trazado del intercambio LLM:** corre la bottom-up de esta rama `task-015` (secuencia real `Etapa(4)→Materia(10)→[Bloques(3)]→Saberes(106)→Criterios(144)→Ejes(72)`; el curso NO es llamada LLM, se deriva — Fase D). Es decir, derivar curso sin LLM y materia-antes-de-hojas **ya está hecho**. Delta real pendiente: **(a)** permitir **varias materias candidatas** (hoy `pickSubjectName` elige una sola, `maxSelections=1`); **(b)** **precisión en el nivel crítico saberes/criterios** — los criterios (144 candidatos) NO pasan por pre-filtro de bloque (solo los saberes, `if TEACHES`), lista enorme en un solo prompt → extender el pre-filtro por bloque a criterios y/o acotar mejor; **(c)** revisar si la Etapa debe seguir siendo el primer paso con LLM — **evidencia (2026-06-29):** el mismo item #3181 se clasificó como **Primaria→Conocimiento del Medio** en una pasada y **ESO→Biología y Geología** en otra; con contenido pobre el primer paso (Etapa) es una conjetura gruesa que cae a un lado u otro y arrastra toda la cascada (no-determinismo). Liderar por materia(s) o evaluar varias etapas mitigaría esto. **Nota:** que los Criterios salgan vacíos `[]` NO es bug — los criterios LOMLOE son competenciales/genéricos (CE1.1…), rara vez específicos de un tema como "la célula"; coherente con ADR-0010 §5 (se intentan, se omiten si vacíos). **Hecha (2026-06-30):** afinado de la Fase A de ADR-0010 (sin tocar mapeo RDF ni grafo) por SDD con TDD — `CurricularClassifier` reescrito a **etapa multi-select** (`pickEtapaIds`, mitiga el no-determinismo de la etapa única), **materia multi-select** (`pickSubjectNames` + `gatherFamilies` dedup por nombre cross-etapa; supera la limitación "Materia única v1"), hojas reunidas por el producto etapas×materias con dedup por id y tope `LEAF_CAP=200` (`gatherLeaves`), y **criterios acotados a los cursos de los saberes elegidos** (`if courseIds` filtra `assesses`; fallback sin saberes → criterios de la materia) — más preciso en el nivel crítico que el pre-filtro por bloque genérico. Curso (`lrmi:educationalLevel`) y materia (`schema:about`) se siguen derivando de las hojas (Fase D). 82 tests verdes (incl. dedup cross-etapa, hojas cross-materia, filtro de criterios y su fallback), lint PSR-12. Nota de afinado añadida a ADR-0010. Plan `docs/superpowers/plans/2026-06-29-task-018-secuencia-bottom-up.md`. | hecha | RF-009, NFR-008, NFR-010, NFR-011, ADR-0009, ADR-0010 | TASK-015 |
| TASK-019 | **Refactor de la capa de extracción de contexto del item que se pasa al LLM.** Surgida de las pruebas funcionales de TASK-015/017/018 (2026-06-30). Hoy el contexto se ensambla así: `IndexController::itemMetadataText($item)` produce un texto de metadatos, `OmekaMediaSource::filesFor($id)` da las rutas de medios, y `ContentExtractor::extract($metadataText, $files)` los **concatena en un único texto plano** (`content->text()`) truncado por presupuesto de tokens; ese mismo blob se pasa **idéntico** a ambos clasificadores y a **cada paso** del LLM (etapa, materia, bloques, saberes, criterios, ejes). Problemas a evaluar: (a) la concatenación plana **no distingue metadatos de contenido de medios** ni la procedencia (qué medio aportó qué), lo que puede enterrar la señal; (b) el **truncado por presupuesto** puede descartar el contenido de los medios cuando los metadatos van primero (orden de ensamblado); (c) se envía el **mismo contexto completo a todos los pasos** aunque cada paso necesite señal distinta (p. ej. la etapa se decide con poco, los saberes necesitan el detalle del medio); (d) no hay estructura/secciones ni pesos por fuente. **Objetivo:** rediseñar esta capa para que el contexto que llega al LLM sea más útil y trazable, manteniendo las garantías de seguridad ya existentes (contenido como dato no-instrucción, anti zip-bomb/zip-slip, límites de nodos JSON, sin SSRF, clave nunca logueada) y la arquitectura puertos/adaptadores (núcleo testeable en host). **Alcance y diseño a decidir** (regla de oro: preguntar antes que inventar) — abrir con brainstorming/plan mode antes de tocar código. **En curso (2026-06-30):** diseño aprobado en brainstorming y formalizado en **ADR-0011** (spec `docs/superpowers/specs/2026-06-30-refactor-contexto-llm-design.md`). Decisiones: (1) **dos modelos** — extracción barato vision-capable + clasificador actual (extiende ADR-0008); (2) **destilador fiel** que NO infiere currículo → ficha estructurada (tema/conceptos/vocabulario); (3) **visión siempre top-N imágenes + rescate de PDF escaneado**, binario directo al LLM (`image`/`document` pdf), master toggle `vision_enabled` off — **absorbe TASK-012/RF-012**; (4) **contexto por paso** (gruesos→ficha, finos→ficha+crudo); (5) **`ItemContext`** estructurado con procedencia sustituye el blob plano (el truncado protege el medio). Faseado: ADR/gobierno → adaptador multimodal → `ItemContext`/enrutado → `ContextDistiller` → `MediaVisionExtractor` → glue/ConfigForm. TDD real en host; verificación en contenedor diferida. **Progreso (2026-06-30):** Fases 1-3 (ADR-0011 + gobierno «en curso»; adaptador multimodal — `LlmClientInterface::supportsImages()/supportsPdf()`, `AnthropicClient`/`OpenAiCompatibleClient` traducen bloques `image`/`document`; `ItemContext` con `coarseText()`/`fineText()`/`rawForDistillation()` y enrutado por paso en `CurricularClassifier`/`TagClassifier`) hechas en `4b01d8a`. **Fase 4 (`ContextDistiller` + segundo modelo) hecha:** `PromptBuilder::buildDistillationPrompt()` (ficha fiel: tema/conceptos/vocabulario/qué enseña; instrucción explícita de **no inferir currículo**; contenido como dato-no-instrucción con marcas neutralizadas); `ContextDistiller` (puro, `TraceableInterface`, sin-LLM-si-vacío) que lee `rawForDistillation()` y devuelve la ficha; `AiCataloguer` orquesta extracción → destilación → `ItemContext::withFicha()` → clasificación, y expone `ficha`/`distillation` en el debug; factoría del **2º cliente LLM** (`OERManager\Llm\ExtractionClient` con `EXTRACTION_MODEL`, cae a `MODEL`) + factoría de `ContextDistiller` + cableado de `AiCataloguer` en `module.config.php`. **105 tests verdes** (267 assertions; +13), lint PSR-12. **Fase 5 (`MediaVisionExtractor`) hecha:** filtro heurístico PURO `selectImages()` (descarta ruido por nombre `logo|icon|sprite|bg|thumb…` con lookaround + tamaño mín/máx; ordena por tamaño y toma top-N) y `describe()` que manda top-N imágenes + PDF escaneado como bloques `image`/`document` al modelo de extracción; **doble puerta** master toggle `vision_enabled` (off) + capacidad del proveedor (`supportsImages()/supportsPdf()`); `TraceableInterface` con motivo de omisión (`disabled`/`provider_no_vision`/`no_candidates`); `PromptBuilder::buildVisionPrompt()` (fiel, texto visible como dato-no-instrucción); `AiCataloguer` enruta imágenes y rescata los PDF `pdf_unreadable`/`pdf_empty` a la visión → `ItemContext::visionDescriptions`. **Fase 6 (glue + ConfigForm) hecha:** `OmekaMediaSource::imagesFor()` (jpg/png/gif/webp + tamaño, aparte de la cascada de texto); `ConfigForm` + `Module` con `EXTRACTION_MODEL`, `vision_enabled` (off), `vision_max_images` (3); factoría de `MediaVisionExtractor` (gating por settings) inyectada en `AiCataloguer` y `IndexController` pasando `imagesFor()` a `propose()`. **120 tests verdes** (310 assertions; +15), lint PSR-12. **VERIFICADA EN CONTENEDOR (2026-07-22):** `propose` real end-to-end sobre 4 items del corpus vía OpenRouter (12-23 s por item con el arnés CLI, que esquiva el 504 de TASK-020). Confirmado: `debug.ficha` no vacía y estructurada en los 4; enrutado por paso operativo (gruesos→ficha, finos→ficha+crudo) con alineamiento en las 5 dimensiones; `content.sources`/`skipped` con procedencia real; **rescate por visión funcionando** (#4674: `images:0, pdfs:1` → la ficha completa, con «1º ESO»/«MATEMÁTICAS», salió del PDF vía visión cuando la extracción de texto falló). Nota: ese rescate estaba **enmascarando** el defecto TASK-024 — con `vision_enabled` off (el default) esos items se habrían quedado sin señal alguna. | **hecha (verificada en contenedor)** | RF-009, RF-010, RF-012, NFR-004, NFR-008, NFR-010, NFR-011, ADR-0007, ADR-0008, ADR-0010, ADR-0011 | TASK-015, TASK-017 |
| TASK-020 | **`ai-propose` síncrono agota el timeout del proxy → 504 con modelos/proveedores lentos.** Detectado probando «Proponer con IA» (2026-07-01): `POST /admin/oer-manager/ai-propose` devuelve **504 Gateway Time-out** según el modelo/proveedor. **Causa raíz:** la acción es **síncrona** y, tras ADR-0011, encadena **~7-9 llamadas al LLM secuenciales** en una sola petición HTTP: `ContextDistiller` (ficha) → `MediaVisionExtractor` (si `vision_enabled`) → cascada del `CurricularClassifier` (etapa → materia → [bloques] → saberes → criterios) → `TagClassifier` (ejes). Con un modelo lento/grande/«de razonamiento» cada llamada tarda 15-40 s y el **tiempo de pared acumulado** se va a varios minutos. **Dos capas de timeout:** (a) **por llamada**, del módulo — `LaminasHttpTransport` con `timeout = 60s` hardcodeado; si *una* llamada lo supera, el módulo corta y el controlador responde `{error:'llm'}`/`{error:'unexpected'}` con **HTTP 200**, no un 504; (b) **total de la petición**, del stack Docker (proxy inverso / php-fpm: `proxy_read_timeout`/`fastcgi_read_timeout`/`max_execution_time`, ~60 s) — **de aquí sale el 504** cuando la suma de llamadas supera el límite; esa capa **no forma parte del módulo** (CLAUDE.md). **Mitigaciones inmediatas (sin código):** usar modelos rápidos (`EXTRACTION_MODEL` barato, clasificador no-razonador), `vision_enabled` off si no se usa (quita 1 llamada + subida de binario); y, en la infra del propietario, subir el timeout del proxy/PHP. **Arreglo de fondo:** hacer el propose **asíncrono/resiliente** — ejecutarlo como Omeka **Job en segundo plano** con *polling* desde el front (la petición HTTP vuelve al instante y no aplica el timeout del proxy); **comparte la infraestructura de Job con TASK-011** (lote), así que conviene abordarlas juntas o encadenadas. Mejora menor del módulo, independiente: hacer **configurable el timeout por llamada** (hoy 60 s fijo) y afinar el mensaje de error para distinguir «proveedor lento/colgado» de «total excedido». **Alcance/diseño a decidir** (regla de oro: preguntar antes que inventar) — abrir con brainstorming/plan antes de tocar código. | pendiente | RF-011, NFR-008, NFR-010, NFR-011, ADR-0007, ADR-0008, ADR-0011 | — (comparte infra de Job con TASK-011) |
| TASK-021 | **Perfil de inferencia compartido entre proveedores (ADR-0012).** Detectado probando TASK-019 (2026-07-02): con **el mismo modelo** por Anthropic directo vs OpenRouter los resultados divergen — sobre todo la **ficha destilada**, que arrastra a la clasificación. Causa: el módulo dejaba `temperature`/razonamiento al default de cada proveedor (y OpenRouter puede traer reasoning activado por modelo, cuyos tokens consumen `max_tokens` → JSON truncado → selección vacía sin reintento). **Hecha (2026-07-02, host):** settings `oermanager_llm_temperature` (vacía = no enviar; los Opus 4.6+ la rechazan con 400) y `oermanager_llm_max_tokens` (default 1024) en `LlmSettings` (+ parsers puros `parseTemperature`/`parseMaxTokens`) + `ConfigForm`/`Module`; los 4 servicios LLM (`ContextDistiller`, `CurricularClassifier`, `TagClassifier`, `MediaVisionExtractor`) reciben el perfil por factoría y lo envían **idéntico en todos los pasos**, trazando `llm_options` en el panel de debug; `OpenAiCompatibleClient` apaga el razonamiento **solo en OpenRouter** (`reasoning: {"effort":"none"}`, detección por host de la base URL; los endpoints genéricos no reciben params no estándar). TDD: 136 tests verdes (343 assertions; +16), lint PSR-12. **Divergencia de PDF cerrada para OpenRouter (2026-07-02, debugging del destilador):** fichas <700 chars vía OpenRouter vs >4000 vía Anthropic en items con PDF escaneado — el rescate por visión solo corría en Anthropic (`supportsPdf()=false`). OpenRouter soporta PDF de entrada (part `file`, procesado nativo del modelo): `OpenAiCompatibleClient` traduce ahora el bloque `document` a ese part y reporta `supportsPdf()=true` solo con OpenRouter; endpoints genéricos siguen omitiéndolo (138 tests verdes). Pendiente: verificación funcional en contenedor (repetir propose sobre los mismos items por ambos proveedores y comparar tasa de acuerdo). **Incidente en las pruebas (2026-07-02):** con `claude-sonnet-5` como clasificador + temperatura 0.2, Anthropic directo devolvía 400 («El proveedor de IA no respondió correctamente») — **Sonnet 5, Opus 4.6+ y Fable 5 tienen los sampling params eliminados y rechazan `temperature`** (Haiku 4.5 y Sonnet 4.6 la aceptan), y el controlador tragaba la `LlmException` sin loguearla (indiagnosticable). Arreglo: `aiProposeAction` loguea el mensaje saneado de la excepción, el mensaje de UI remite al log, e info del campo Temperatura corregida (en blanco con esos modelos; paridad por omisión). ADR-0012 actualizado. | hecha (host); verif. contenedor diferida | NFR-008, NFR-010, NFR-011, ADR-0008, ADR-0011, ADR-0012 | TASK-019 |
| TASK-022 | **Refactor del destilador de contexto (`ContextDistiller`) guiado por casos reales.** Hipótesis surgida al revisar la calidad de la catalogación IA (2026-07-04, sobre TASK-019): la **ficha destilada** es la **señal primaria** de la clasificación — los pasos gruesos (etapa/materia/bloques) se deciden **solo con la ficha** (`coarseText()`) y los finos (saberes/criterios) con ficha+crudo (`fineText()`), así que una ficha pobre, sesgada o que se desvíe del contenido **arrastra toda la cascada bottom-up** (ADR-0010) por correcto que sea el anclaje curricular. Hoy `PromptBuilder::buildDistillationPrompt()` es **zero-shot**: pide una ficha fiel (Tema/Conceptos clave/Vocabulario/Qué enseña) sin ejemplos que fijen el nivel de detalle, el formato ni la frontera «describir vs. inferir currículo». **Objetivo:** rediseñar el destilador **calibrándolo con casos/ejemplos reales** — (a) **few-shot** en el prompt con 2-4 pares (extracto real de un REA → ficha de referencia) que ejemplifiquen una buena ficha y la regla de **no inferir currículo** (ADR-0011); (b) construir un **pequeño corpus de evaluación** de REAs reales del catálogo (p. ej. #3181 «Partes de la célula» SCORM Netex, un PDF escaneado, un item con metadatos pobres) con ficha de referencia, para medir la calidad de la ficha y detectar regresiones al iterar el prompt; (c) evaluar si conviene una ficha **más estructurada/parseable** (secciones estables) en vez de texto libre, sin perder fidelidad. **Invariantes a preservar (ADR-0011):** el destilador es **fiel y NO clasificador** (no propone etapa/materia/curso salvo literal), el contenido viaja como **dato no-instrucción** (spec §6, marcas neutralizadas), perfil de inferencia compartido (ADR-0012, `llm_options` trazado), y el núcleo sigue **puro/testeable en host** con fake LLM (TDD real; verificación en contenedor diferida). **Ojo coste de tokens:** el few-shot engorda la llamada del destilador, que ya forma parte del propose síncrono → tenerlo presente por NFR-008 (accuracy↔tokens) y TASK-020 (latencia/504). Medición contra REAs ya catalogados vía `EvaluationScorer`/`aiEvaluate` (NFR-011). **Primer paso (bloquea el resto):** el propietario **extrae del contenedor** un pequeño conjunto de REAs reales representativos (p. ej. #3181 SCORM Netex, un PDF escaneado, un item con metadatos pobres) **con su catalogación correcta** (etapa/materia/saberes/criterios ya confirmados); a partir de ese material se redactan las fichas de referencia del few-shot y del corpus de evaluación. Sin estos casos reales no se arranca el rediseño (no inventarlos). **En curso (2026-07-07, host):** primer paso **cumplido** — 6 REAs reales de `resource_class_id=4758` (`localhost:8080`) descargados y pasados por el `ContentExtractor` real. **Diagnóstico clave: el problema no era solo el prompt, sino el crudo que lo alimenta.** Caso #37129 (SCORM 41 MB): los 24k chars del contexto eran basura vendor (samples CKEditor, licencias de fuentes, CSS Wiris, texto demo «Apollo 11») porque el ZIP se recorría en orden de archivo, ~900 entradas de editor agotaban `max_zip_entries`, y el truncado head-first cortaba antes del contenido real (al final del ZIP). Casos #3181/#4359 (Netex): ids de interfaz (`imagelink_<hash>`, `navigationSectionInteracted`) diluían la señal. #40442: PDF escaneado → `pdf_empty` (solo visión). **Solución (dos frentes, ADR-0011 §Afinado TASK-022, sin relajar seguridad):** (1) `ContentExtractor` endurecido — denylist de rutas vendor (`noise_path_segments`, saltadas antes de consumir cuota), **reparto equitativo del presupuesto** entre piezas (water-filling, sustituye al truncado head-first), filtro reforzado de ids de interfaz y CSS embebido en `meaningfulText()`; (2) prompt del destilador guiado por casos reales — instrucción anti-ruido, sección estable «Nivel citado textualmente» (literal o «No consta»), few-shot con 2 ejemplos reales (#4674 nivel literal, #3181 Netex ruidoso) en `PromptBuilder::DISTILLATION_EXAMPLES`. Corpus de evaluación en `test/fixtures/distiller-corpus/` (6 items: extracto real + ficha de referencia + protocolo de medición en contenedor). Re-ejecutado el arnés de diagnóstico: #37129 sin rastro CKEditor/Apollo y con porcentajes/REF repartidos; #3181/#4359 sin ids técnicos; #40437/#4674 intactos. **151 tests verdes, lint PSR-12.** Guías al propietario: activar `vision_enabled` (#40442/#3181), completar metadatos mínimos (#4359 sin título), subir `max_tokens` si la ficha se corta. **VERIFICADA EN CONTENEDOR PARA LOS CASOS ZIP/SCORM (2026-07-22)**, con `propose` real vía OpenRouter (`openai/gpt-4o-mini` clasificador + `openai/gpt-5.4-nano` extracción): **#37129** (SCORM 41 MB) sin rastro de CKEditor ni «Apollo 11» — la denylist saltó 651 entradas `noise_path` — y ficha sobre porcentajes/REF con «2.º ESO», «Matemáticas» citados **igual que la referencia del corpus**; **#3181** y **#4359** sin ids de interfaz (`imagelink_`, `navigationSectionInteracted`), fichas on-topic (célula / vasos sanguíneos) y sección «Nivel citado textualmente» estable («No consta» donde no consta). Concordancia ficha↔referencia confirmada en #3181 y #37129. **Bloqueado para los casos PDF (#4674, #40437, #40442) por TASK-024(b)**: en Alpine/musl el PDF rinde 0 chars, así que su ficha no es medible todavía. Pendiente también la validación de las fichas de referencia por el propietario. | en curso (ZIP/SCORM verificado en contenedor; PDF bloqueado por TASK-024) | RF-009, RF-010, NFR-008, NFR-010, NFR-011, ADR-0007, ADR-0010, ADR-0011, ADR-0012 | TASK-019 |
| TASK-025 | **Rescate confirmado de PDF grandes (`pdf_too_large` no llega ni a texto ni a visión).** Hallado por el propietario el 2026-07-22 probando «Proponer con IA» sobre un item con `IA_alcaravan.pdf`: el panel mostraba `leídos: (ninguno) | saltados: IA_alcaravan.pdf → pdf_too_large`, ficha de **212 chars** (solo el título), Etapa `[1,2,3,4]` (sesgo de inclusividad) y Materia/Ejes vacíos. **Causa raíz:** `AiCataloguer::rescuablePdfs()` filtra por `in_array($reason, ['pdf_unreadable','pdf_empty'])`, así que un PDF de más de 20 MB (`max_pdf_bytes`) se queda **sin texto y sin visión**. De fondo es un error conceptual: `max_pdf_bytes` es una guarda anti PDF-bomb del **parseo en memoria** (TASK-010) que de hecho gobernaba también el camino de **visión, que no parsea nada** — manda el binario al proveedor. Agravado por TASK-024(b): en Alpine casi todo PDF con texto cae en `pdf_empty`, o sea que la visión sostiene en silencio toda la extracción de PDF. **Diseño aprobado (brainstorming 2026-07-22):** (1) rescatar `pdf_too_large` por visión con un **tope propio** `vision_max_pdf_bytes` (default 32 MB, configurable) separado del de parseo; (2) `max_pdf_bytes` (20 MB) **fijo y no expuesto** — es seguridad, no rendimiento; (3) **propose en dos fases** que corta **antes de la primera llamada al LLM** y devuelve `needs_confirmation` con nombre y tamaño (coste cero en tokens); (4) decisión **tri-estado** `ask`/`include`/`skip` (un booleano provocaría un bucle de preguntas al rechazar); (5) al rechazar, el propose **sigue sin ese PDF**; (6) por encima de 32 MB **no se ofrece confirmación**, se informa. Doble puerta de visión intacta: sin `vision_enabled` no hay confirmación que ofrecer. Spec `docs/superpowers/specs/2026-07-22-rescate-confirmado-pdf-grandes-design.md`. TDD real en host (incluido el test de que en `ask` el fake LLM registra **0 llamadas**); glue por el arnés de `test/container/`. Afina ADR-0011; no toca ADR-0004 ni ADR-0009 | pendiente (diseño aprobado) | RF-009, RF-012, NFR-008, NFR-010, NFR-011, ADR-0007, ADR-0008, ADR-0011 | TASK-019 |
| TASK-024 | **La extracción de texto de PDF no funciona en el contenedor (dos defectos encadenados).** Hallado en la verificación funcional de TASK-019/022 (2026-07-22), invisible para los 163 tests del host. **(a) El módulo nunca registraba su propio autoloader:** Omeka NO autocarga el `vendor/` de un módulo y el core no trae `smalot/pdfparser`, así que `Smalot\PdfParser\Config` no existía en runtime; `ContentExtractor::parsePdf()` capturaba el `Error` y marcaba **todo** PDF como `pdf_unreadable` en silencio. Los tests no lo veían porque `test/phpunit.xml` bootstrapea `vendor/autoload.php` directamente. **Arreglado (2026-07-22):** `Module` implementa `InitProviderInterface` y hace `require_once` guardado del autoload en `init()` — se declara la interfaz explícitamente porque `Omeka\Module\AbstractModule` solo implementa `ConfigProviderInterface` y el `InitTrigger` de Laminas solo invoca `init()` sobre un `InitProviderInterface` (copiar el idioma de BulkExport sin la interfaz habría sido otro no-op silencioso). El guard mantiene el módulo cargable si se distribuye sin `vendor/`. Lint PSR-12 y 163 tests en verde. **(b) PENDIENTE — `iconv //TRANSLIT` no existe en Alpine/musl:** con el autoloader ya arreglado, `smalot/pdfparser` sigue devolviendo **0 chars** en el contenedor. Causa raíz confirmada por A/B sobre el MISMO PDF (item #4674, `2a07d892…pdf`): **host (glibc/libiconv) → 901 chars** incluido el «1º ESO MATEMÁTICAS» de la referencia del corpus; **contenedor (Alpine/musl) → 0 chars**. `Font.php:651` hace `iconv($enc, 'UTF-8//TRANSLIT//IGNORE', $text)` y en musl eso devuelve `false` (verificado: `iconv('CP1252','UTF-8//TRANSLIT//IGNORE',…)===false`, `ICONV_IMPL='unknown'`; sin `//TRANSLIT` sí convierte). **Impacto:** en el despliegue real ningún REA en PDF aporta texto — o se pierde (con `vision_enabled` off, que es el default) o se rescata por visión. Invalida el diagnóstico `pdf_empty`=«PDF escaneado» de #40442 en TASK-022, que se hizo en host. **El arreglo es de imagen Docker, NO del módulo** (CLAUDE.md: la config Docker no forma parte del módulo) → decisión del propietario. **Investigación de vías (2026-07-22, con permiso del propietario):** (i) **`gnu-libiconv` + `LD_PRELOAD` NO funciona** en esta imagen — Alpine 3.23 empaqueta `gnu-libiconv` 1.18-r0 **sin** `preloadable_libiconv.so` y exportando solo símbolos con prefijo (`libiconv`, `libiconv_open`, `libiconv_close`), así que no hay interposición posible; probado e instalado/desinstalado en el contenedor, `ICONV_IMPL` seguía en `unknown`. Vía muerta: no recomendar. (ii) **Una imagen con glibc SÍ lo resuelve, sin tocar código**: mismo PDF (#4674) en `php:8.4-cli` (Debian) → `ICONV_IMPL: glibc`, sonda `//TRANSLIT` OK, **901 chars**, idéntico al host. (iii) **Alcance real del daño mayor de lo estimado:** el mapa de `smalot/pdfparser` incluye `WinAnsiEncoding → CP1252`, y `WinAnsiEncoding` es la codificación más común en PDF — no es un caso raro de MacRoman: en Alpine se pierde el texto de casi cualquier PDF estándar. (iv) **Sin arreglo upstream disponible**: v2.12.5 (2026-04-17) es la última publicada y sigue con el `//TRANSLIT`; el arreglo sería de una línea y demostrablemente seguro (en el contenedor `mb_convert_encoding($t,'UTF-8','Windows-1252')` da salida **idéntica** a `iconv` plano, y la propia librería ya lo usa en las líneas 557/605/682). (v) **Obstáculo para la vía glibc:** no existe imagen Debian de Omeka-S 4.2 mantenida (`erseco/alpine-omeka-s` es Alpine en todos sus tags; `omeka/omeka-s` no se actualiza desde 2017) → adoptarla implica construir y mantener un Dockerfile propio desde `php:8.4-fpm`. Sonda de diagnóstico para validar cualquier imagen candidata: `php -r 'var_dump(ICONV_IMPL, @iconv("CP1252","UTF-8//TRANSLIT//IGNORE","a"));'` — debe devolver un string, no `false`. **Decisión del propietario (2026-07-22): degradación honesta en el módulo ahora + imagen glibc cuando pueda.** **Degradación HECHA y verificada en contenedor (2026-07-22, TDD):** `ContentExtractor` gana el límite inyectable `iconv_translit_supported` (`null` = sonda en runtime, evaluada una vez; inyectable porque el host tiene glibc y **nunca podría probar la rama rota**) y, cuando un PDF vuelve vacío en plataforma degradada, reporta **`pdf_iconv_unsupported`** en lugar de un `pdf_empty` que miente; `AiCataloguer::rescuablePdfs()` trata el motivo nuevo igual que `pdf_empty`, así que la visión lo sigue cubriendo sin cambio de comportamiento. 166 tests verdes (404 assertions; +3), lint PSR-12. Verificado en el contenedor real: el mismo PDF #4674 que reportaba `pdf_empty` ahora reporta `pdf_iconv_unsupported`. **Residuo (propietario):** la imagen glibc, que es lo único que recupera el texto; mientras tanto `vision_enabled` es obligatorio de facto para PDF. Pendiente también (propuesto, no implementado): aviso en el ConfigForm cuando la plataforma esté degradada **y** la visión apagada — hoy esa combinación deja los PDF sin aportar nada, en silencio. Alternativa dentro del módulo a evaluar si no se toca la imagen: detectar el `iconv` roto y degradar explícitamente a visión en vez de fingir `pdf_empty`. Bloquea la verificación en contenedor de los items PDF del corpus (#4674, #40437, #40442) | (a) hecha / (b) pendiente — decisión del propietario | RF-009, RF-012, NFR-010, ADR-0007, ADR-0011 | — |
| TASK-023 | **Justificación de saberes/criterios en el clasificador.** El propietario quiere que, además de proponer curso/materia/saberes/criterios, el clasificador añada una **justificación breve de por qué** eligió cada saber/criterio, y que se **persista como value-annotation** sobre el valor del alineamiento, igual que la auditoría de la re-catalogación (quién/cuándo/qué, ADR-0002). Alcance **solo saberes (`lrmi:teaches`) y criterios (`lrmi:assesses`)**: curso y materia se derivan de las hojas (Fase D, ADR-0010), sin selección LLM que justificar. **Diseño aprobado (brainstorming 2026-07-07, defaults):** (1) persistencia como `dcterms:description` dentro del `@annotation`, junto a contributor/modified/provenance; (2) el `propose` genera la justificación pero la escritura ocurre en el `apply` tras confirmación del curador (ADR-0007) → la justificación **viaja oculta con la propuesta a través del panel** (dato `data-justification` en el chip, no se renderiza) hasta el POST del apply; (3) sin UI visible. Cambios **aditivos**: el mapa de alineamiento, preview/apply, scorer e integridad no cambian de forma. Componentes: `ResponseParser::parseSelections()` (índice→porqué, degradante), `PromptBuilder::buildSelectionPrompt($withReason)` (solo pasos finos piden `{"i":n,"why":"…"}`), `CurricularClassifier::getJustifications()` (patrón `TraceableInterface`), `AiCataloguer`/`IndexController` transportan el mapa, `RecatalogService::apply()` gana 4º arg opcional y anota `dcterms:description`. Seguridad: justificación como **dato** (spec §6), literal acotado (~200 chars); la integridad del alineamiento la garantiza `invalidTargets()` (sin cambios). Coste: solo 2 pasos piden texto breve (NFR-008/TASK-020). Spec `docs/superpowers/specs/2026-07-07-justificacion-saberes-criterios-design.md`, plan `docs/superpowers/plans/2026-07-07-justificacion-saberes-criterios.md`. TDD real en host; verificación en contenedor diferida. **Hecha (host, 2026-07-08):** `ResponseParser::parseSelections()` (índice→justificación, degradante: tolera `why` ausente y enteros pelados → nunca se pierde una selección); `PromptBuilder::buildSelectionPrompt($withReason)` (contrato `{"i":n,"why":…}` solo en pasos finos, justificación ≤15 palabras); `CurricularClassifier` con `askWithReasons`/`captureJustifications` + accesor `getJustifications()` (dimensión→{itemId→texto}, reseteado por `classify()`); `AiCataloguer::propose()` expone `justifications`; **glue (verif. contenedor):** `RecatalogService::apply()` gana 4º arg opcional y anota `dcterms:description` (≤200 chars) en el `@annotation` de teaches/assesses; `IndexController` devuelve `justifications` en `aiPropose` y las recoge en el apply (`collectJustifications`, acotadas); JS guarda `data-justification` oculto en el chip y lo emite como `justification[term][id]` en el POST. **163 tests verdes** (399 assertions; +7), lint PSR-12, JS `node --check` OK. **VERIFICADA EN CONTENEDOR (2026-07-22):** `apply` real sobre el item #3181 autenticado como `editor@example.com` (rol mínimo autorizado, NFR-003) con las justificaciones de un `propose` real. El JSON-LD del item confirma los tres criterios: (1) `dcterms:description` aparece como **cuarta** clave del `@annotation` con el texto real de la justificación (p. ej. sobre `PC9N03SBI.2.1`: «Aborda la identificación de estructuras celulares y su función.»), junto a contributor/modified/provenance; (2) aparece **solo** en `lrmi:teaches` y `lrmi:assesses`, y **no** en `educationalLevel`/`about`/`relation`; (3) `dcterms:title` y `dcterms:description` del item **se conservan** — el patrón `clear_property_values`+`append` (trampa crítica de la skill `recatalogador`) no borra el resto de metadatos. Arnés reusable en `test/container/`. | **hecha (verificada en contenedor)** | RF-009, RF-010, NFR-002, NFR-008, NFR-010, NFR-011, ADR-0002, ADR-0007, ADR-0010, ADR-0011 | TASK-015 |
