# Matriz de trazabilidad — OERManager

> Cruza necesidad ↔ RF/NFR ↔ ADR ↔ TASK ↔ criterio de aceptación. Eslabón roto = defecto; eslabón aún sin decidir = `[PENDIENTE]` explícito. Fuentes: [requirements.md](requirements.md), [backlog.md](backlog.md), [decisions/](decisions/).

| Necesidad (origen: docs/referencia/contexto-modulo-rea.md) | RF/NFR | ADR | TASK | Criterio de aceptación |
| --- | --- | --- | --- | --- |
| Gestionar solo el catálogo de REA (`lrmi:LearningResource`) | RF-001 | `[PENDIENTE]` | TASK-002, TASK-003 | Listado admin filtra por `resource_class` |
| Vista maestra de gestión en panel admin | RF-002 | ADR-0005 (columnas, filtros, panel v1; v1 implementada en TASK-003), ADR-0013, ADR-0014, ADR-0016 (§4 de ADR-0005 ampliado: curso huérfano ⇒ parcial), ADR-0017 (el panel vuelve al lateral sobre el sidebar nativo, con partial servido y anchura propia del módulo; aceptado 2026-08-20) | TASK-003, TASK-027, TASK-028 (rebanada 2 hecha 2026-08-05), TASK-032 (panel de detalle integrado, hecha 2026-08-13), TASK-033 (pasada de diseño del panel, hecha 2026-08-18: una sola zona para el currículo —lectura agrupada y edición plana alternando en el mismo sitio—, integridad como veredicto en cabecera o banda con contador, y el riel de ADR-0014 §4 continuado hasta el control que arregla lo que denuncia), TASK-034 (**hecha** 2026-08-23: el panel pasa al sidebar nativo como partial servido y **se superpone sin encoger `#content`**, que era la compensación del core calibrada para su sidebar del 25%; **verificada en el admin real con sesión iniciada**, que ADR-0017 hacía bloqueante para cerrar; diseño en `specs/2026-08-20-task-034-panel-en-sidebar-nativo-design.md`) | v1: tabla `admin/oer-manager` con columnas/filtros del spec aprobado (hecha); objetivo completo (edición inline, columna de integridad, historial) fijado en requirements.md. **TASK-027 (abierta 2026-07-27):** estudio de qué campos debe mostrar/editar la UI (tabla + drawer), previo y bloqueante de **TASK-028** (refactor de la vista maestra y de `asset/js/oer-master-view.js`, decisión del propietario) | **TASK-027 hecha (2026-07-27):** estudio de campos entregado en `docs/superpowers/specs/2026-07-27-campos-ui-design.md` y aprobado por **ADR-0013** (vista maestra v2), que complementa ADR-0005 sin derogarlo. Reequilibra la UI hacia la gobernanza: la columna «Alineamiento» tiene varianza cero (100 % de cobertura) mientras licencia (5,3 %) y autoría (0 %) no tienen superficie. TASK-028 queda desbloqueada. **TASK-028 rebanada 1 (refactor puro) hecha y verificada el 2026-07-29:** misma funcionalidad, cero campos nuevos; cierra **D1** (filtro de tipo, `res`→`eq`: de 0 a 1 resultados sobre datos reales), **D4** (filtro «parcial» con total exacto y páginas estables, patrón `ComputedFilter` obligatorio de ADR-0013), **D5** y **D6** (columnas por el mecanismo nativo bajo la clave `oer_items`, configurables por curador, con selector de orden) y **D7** (el preview muestra títulos con su curso ancestro en vez de «+N/−M»). Criterio comparativo del spec §6 recorrido: las 6 columnas rinden idéntico a la v1 y el browse nativo de items no cambia. **Sin regresión en el componente de alto riesgo, verificado sobre un `apply` real con el código nuevo (item #4674, 2026-07-29):** título y descripción del item se conservan, la justificación de la IA sigue solo en `lrmi:teaches`/`lrmi:assesses`, el `@annotation` mantiene las tres claves de ADR-0002 y el catálogo sigue con 1/19 REA con licencia, la cifra previa a la rebanada. **No verificado y declarado como tal —toda esta deuda queda recogida en TASK-030** (revisión funcional completa sobre imagen Debian/glibc, sprints finales)**:** el propose IA asíncrono end-to-end, el ciclo de guardado real de columnas por curador y el render en el admin con sesión real. **D2 y D3 siguen fuera a propósito** (son de `IntegrityChecker` y de la columna que ADR-0013 retira). **Pasada de diseño visual sobre la rebanada 1 (2026-07-29, pedida ad hoc por el propietario, fuera del spec):** solo presentación —cero campos nuevos, cero cambios en `column_defaults`, query ni controlador—; paleta y tipografía heredadas del chrome del admin (Lato, `#a91919`), jerarquía reordenada hacia la señal de alineamiento (riel de 3px por fila con saturación invertida, escala ordinal ●/◐/○ marcada por forma además de por color), las cuatro filas sueltas de controles agrupadas en barra de herramientas + barra de resultados, anchuras de columna explícitas contra el `table-layout: fixed` del core, y acciones de lote que solo existen con selección. Corrige el rótulo `Is public` **sin traducir** del core (`ColumnType\IsPublic::getLabel()` → `Visibilidad`) y el `Sí`/`No` suelto → `Público`/`Privado`. Criterio recorrido: PSR-12 en verde, 33/33 `node --test`, render comprobado en Chrome headless contra el `style.css` compilado del core a 1300/1540px y en modo `stack` de tablesaw (≤640px), sin desbordamiento horizontal. **No verificado:** render en el admin real logueado (sin credenciales). **Decisión revocable declarada:** los títulos dejan el rojo del browse del core y pasan a tinta, conservando el rojo en `:hover`/`:focus` y reservándolo para el estado defectuoso. **Caveat:** el riel se apoya en `AlignmentStatus`, con **varianza cero** hoy (19/19 `complete`), y ADR-0013 planea retirar esa columna en la v2 — si lo hace, el riel se re-ancla a la señal sucesora (integridad/gobernanza), no se elimina. **TASK-028 rebanada 2 (hecha y verificada, 2026-08-05):** estrena la columna de Integridad (`IntegrityChecker` proyector puro sobre `IntegrityPolicy`/`CurricularSummary`/`ComputedPredicates`), la columna Curricular fusionada con dedup y marca de literal, la columna Tipo de recurso, el renombrado de Alineamiento a **Anclaje**, 4 filtros `nex` de gobernanza y el filtro computado de integridad (segundo consumidor de `ComputedFilter`). Verificado en contenedor sobre 19 REA (`test/container/columns-check.php`, 6 OK/0 FAIL/1 SKIP): `ok=1`, `warning=18`, `error=0`, `literal_in_link_property=4`, `missing_license=18`/19; la rama de plantilla (D-6) quedó SKIP por falta de datos reales (PEND-012 abierto). 285 tests PHP/669 aserciones y 48 tests JS. **No verificado:** UI en navegador con sesión real (TASK-030). **TASK-028 rebanada 3a (superficies de lectura del drawer, hecha 2026-08-11/12):** el drawer estrena la lectura de las incidencias de `IntegrityChecker` (detalle por severidad/campo/mensaje, con la comprobación de enlaces encendida) y del historial de curación de ADR-0015 (RF-008), vía la acción de solo lectura `drawer-details`. Resuelve **PEND-013**: la justificación de la IA se muestra tras un «ver porqué» colapsado. 311 tests PHP/710 aserciones y 67 tests JS. **No verificado:** el arreglo del arnés de restauración exhaustiva (`7116395`, modo `--write`) y la UI en navegador con sesión real, ambos deuda de TASK-030. **TASK-032 (panel de detalle integrado en la tabla, hecha 2026-08-13):** el detalle del REA deja de ser un cajón lateral fijo de `28em` (`role="dialog"`, ADR-0005 §6, que queda desactualizado y se anota sin reescribirse) y se abre **dentro de la fila de la tabla**, con el anclaje curricular agrupado por curso·materia (`CurricularGrouping`) en vez de listas planas, miniatura, medios abribles e historial perezoso, vía una acción `drawer-details` autenticada (necesaria: el JSON-LD público no trae miniatura ni metadatos de medios). Verificado en contenedor con `test/container/detail-panel-check.php`, **10 OK, 0 FAIL** sobre los 19 REA reales. **Dos correcciones registradas al cerrar:** solo **4 de los 19 REA** tienen miniatura real medida con `MediaRepresentation::hasThumbnails()` (`$item->thumbnail()` no sirve como señal, da 17/19 por iconos genéricos reutilizados con el mismo hash); y los REA con curso huérfano (`course-without-subject`) son **6, no 7** como citaban ADR-0016 y la rebanada 2 (`4676, 5047, 37129, 37132, 40425, 40427`) — coincide hoy, ítem a ítem, con `partial=6` de `columns-check.php`, pero por coincidencia de los datos actuales, no porque midan lo mismo. 323 tests PHP/743 aserciones y 84 tests JS. **No verificado, declarado sin maquillar (spec §11, obligatorio y no negociable):** la sesión de navegador con sesión real (dos cursos, curso huérfano, sin medios, sin miniatura, modo estrecho), deuda de TASK-030. |
| Control de autoría y licencia del REA (calidad y homogeneidad de los metadatos de gobernanza) | RF-015, RF-006 | ADR-0013 (catálogo de campos UI v2) + ADR-0004 (licencia como literal de CustomVocab) + ADR-0002 (auditoría) | TASK-027, TASK-028 | **Abierto:** medido el 2026-07-27 sobre el catálogo real — **0/19 REA con autoría** en las 12 properties candidatas y **1/19 con licencia** (literal `ccbysa` sin versión ni URI). Modelo de autoría aprobado por el propietario (ligero controlado: `dcterms:creator` múltiple + `dcterms:publisher` de CustomVocab + `dcterms:rightsHolder` con defecto + `dcterms:source` URI). Criterio de aceptación: la UI muestra el hueco (columna, filtro «sin licencia»/«sin autoría»), permite rellenarlo (edición individual y en lote) y lo audita. Contenido del vocabulario de licencias `[PENDIENTE]` (PEND-011). **Mecanismo resuelto (2026-07-28, ADR-0013 «sembrar, no poseer»):** vocabularios y plantilla identificados por ID en settings; el módulo siembra bajo demanda lo que no existe (licencias, plantilla REA) y consume lo que ya está (tipos de recurso, id 1), sin sobrescribir nunca en `upgrade()` |
| Curar visibilidad público/privado | RF-003 | ADR-0005 (flujo v1; v1 implementada en TASK-003) | TASK-003 | v1: toggle individual + lote con confirmación; ACL nativa de edición (hecha); matriz rol×acción propia (`editor`+) fijada en requirements.md como mejora futura |
| Re-catalogación curricular fiable y reversible | RF-004, NFR-004 | ADR-0002 (vocabularios) + ADR-0004 (mapeo: educationalLevel/about/teaches/assesses) + ADR-0006 (raíz de los DefinedTermSet curriculares) | TASK-004, TASK-035 (**pendiente**, alta 2026-08-23: el prellenado de los selectores deja de venir de `/api` anónima, que no ve la sesión del admin y deja **sin editor a todo REA privado**; `ItemPanelData` ya lee esos mismos valores autenticado), TASK-036 (**hecha** 2026-08-25: con dos Cursos elegidos a la vez —cardinalidad múltiple de PEND-007—, la acotación de Materia solo leía el primer chip de cada dimensión, tanto en cliente [`getContext()`→`firstChipId()`] como en servidor [`CurriculumSearch::contextFilter()`, `(int)` sobre una lista]; corregido para combinar todos los ids elegidos por dimensión, verificado 7/7 en contenedor sobre un caso real de dos cursos) | Properties, cardinalidad (múltiple) y roles (editor+; isPartOf solo admin) fijados en requirements.md; **un REA en privado se re-cataloga desde el panel** (criterio nuevo, hoy no se cumple: TASK-035); **la acotación de una dimensión hija respeta TODOS los ancestros elegidos, no solo el primero** (criterio nuevo, cumplido desde TASK-036) |
| Re-catalogación por etiquetas | RF-005 | ADR-0004 (`dcterms:relation`, controlado por `schema:DefinedTermSet`) + ADR-0006 (raíz configurable por ID de item) | TASK-004 | Rol `editor` o superior; raíz del DefinedTermSet fijada en config del módulo (ADR-0006) |
| Garantizar integridad del catálogo | RF-006 | ADR-0004 (campos a validar), ADR-0013 §Afinado (TASK-028 rebanada 2), ADR-0016 (firma la ampliación) | TASK-005, TASK-028 | **Hecho (TASK-005):** `IntegrityChecker::check()` valida dead-links de alineamiento (error) y completitud mínima/por plantilla (warning); hook en `api.create.post`/`api.update.post` de ItemAdapter. **Ampliado (TASK-028 rebanada 2, hecha y verificada 2026-08-05):** las reglas mínimas (anclaje, licencia) pasan a aplicarse **siempre** y los campos obligatorios de plantilla se **suman** en vez de sustituirlas (D-6); nuevo aviso `literal_in_link_property` (D-3); `IntegrityChecker` pasa a proyector puro sobre `IntegrityPolicy`, con TDD real por primera vez. Verificado en contenedor sobre 19 REA: `ok=1`, `warning=18`, `error=0` (`dead_link` sin productor, D-5), `literal_in_link_property=4`, `missing_license=18`/19; la rama de plantilla quedó SKIP (PEND-012 abierto, sin datos reales) |
| Accesibilidad del panel admin del módulo | NFR-006 | ADR-0014 (norma visual: forma además de color para lo accionable, canal periférico continuo) | TASK-028 | Nivel objetivo = el que ya ofrece el admin nativo de Omeka 4.x, sin WCAG adicional específico del módulo (requirements.md). **Primera medición ejecutable (TASK-028 rebanada 2, hecha 2026-08-05):** los colores de la tríada de estado se declaran como tokens y un test `node --test` calcula el ratio de contraste contra el fondo real del admin, fallando por debajo de 4.5:1 (WCAG AA). **Medir reveló que dos tokens aprobados en la rebanada 1 no cumplían** —`--oer-warn` 4.17:1, `--oer-muted` 3.93:1—, corregidos a `#955c0d`/`#666d79`; los valores viejos quedan citados en ADR-0014 como historia append-only. La obligación deja de ser una nota de documento y pasa a ser una prueba que se rompe si alguien mueve un hexadecimal |
| Estadísticas visuales + export | RF-007 | ADR-0004 (dimensiones) | TASK-006 (**hecha** 2026-08-28) | **Cumplido, con alcance ampliado sobre el ejemplo literal del RF:** conteo simple por dimensión (`DimensionCounter`) sobre las 5 dimensiones fijadas (etapa/materia/eje/proyecto/licencia, no solo materia×etapa); **cruce genérico entre cualquier par de las 5** (`DimensionCrosser`, los 10 pares posibles precalculados server-side en ambos órdenes de clave, selector de cliente sin ida y vuelta); % de completitud (`CompletenessAggregator`, ok/warning/error suman el total); export CSV (`CsvExport`) que reutiliza los mismos agregadores que la pantalla, sin poder divergir de ella. Página aislada de la vista maestra: agrega siempre el catálogo completo, nunca los filtros activos de `MasterViewQuery`. Verificado en contenedor sobre 19 REA reales, 7/7 OK (`test/container/stats-check.php`). **Hallazgo y arreglo de seguridad durante el desarrollo:** inyección de fórmulas CSV (CWE-1236) en `CsvExport`, corregida con el prefijo de apóstrofo de OWASP aplicado uniformemente a toda celda/cabecera, 5 tests de regresión. **Fuera de alcance, por decisión del spec §8:** export PDF, caché de agregados, drill-down desde el gráfico a la lista de items filtrada. **No verificado: la página renderizada en el admin real con sesión iniciada** (deuda de TASK-030, mismo patrón que TASK-032/033/034) |
| Saber quién curó qué y cuándo | RF-008 | ADR-0002, ADR-0015 | TASK-007, TASK-023, TASK-028 | Value annotations `dcterms` sobre el valor curado (quién/cuándo/qué); properties exactas fijadas por PEND-005/ADR-0004. **TASK-023 (2026-07-08):** el valor de saber/criterio propuesto por IA gana además el «porqué» como `dcterms:description` en el mismo `@annotation` (ADR-0002 §Decisión.2 ampliación). **TASK-007 (hecha 2026-07-31, ADR-0015):** la anotación no puede registrar una eliminación —vive en el valor que se borra—, así que cada apply añade además un **evento de curación** privado y append-only en el propio item con el estado previo, y `undo()` lo restaura, justificaciones de la IA incluidas. Verificado en contenedor, 31/31. **TASK-028 rebanada 3a (hecha 2026-08-11/12):** ese registro gana por fin cara de lectura — `RecatalogService::history()` sobre `eventsOf()` (conserva el desempate por microsegundos de que depende `undo()`) proyectado por `CurationHistory` (orden inverso, cambios por dimensión con vaciado total representable, títulos resueltos y cualificados por la dimensión real) y servido por la acción de solo lectura `drawer-details`. Resuelve **PEND-013** (justificación de la IA tras «ver porqué» colapsado). **Corrección de gobierno:** la premisa «0 eventos en 19 REA» que motivó E-2 del spec se midió con la API sin autenticar (ciega a `is_public => false`) y era falsa —hay eventos reales en #40437 y #40442—; corregido en `project-memory.md`, sin reescribir el spec (ADR-0001) |
| Acelerar la catalogación con IA (propone, el curador confirma) | RF-009 | ADR-0007 (IA propone/curador confirma; label→búsqueda incremental) + ADR-0004 (mapeo) + ADR-0006 (raíces) | TASK-010 | **Hecho (TASK-010 4b):** `AiCataloguer` propone por dimensión desde metadatos + contenido de medios; el LLM elige por índice de una lista cerrada de candidatos (sin volcar el árbol, NFR-004); pre-rellena el panel de 4a y el curador confirma con preview/apply (escritura reusa `RecatalogService`). Ejecución contra el core pendiente de verificar en contenedor |
| Conectar a un LLM sin atarse a un proveedor | RF-010 | ADR-0008 (adaptadores; `Laminas\Http\Client`; clave en settings nativos) | TASK-010 | **Hecho (TASK-010 4b):** adaptadores `AnthropicClient`/`OpenAiCompatibleClient` sobre `HttpTransportInterface` (impl. `Laminas\Http\Client`, sin redirecciones→sin SSRF); proveedor/endpoint/modelo/clave configurables; clave write-only, nunca en repo/código/logs/errores; sin dependencia composer para la conexión |
| Clasificación IA óptima (accuracy > tokens) | NFR-008 | ADR-0007 (propone/confirma) + ADR-0009 (grafo para acotar) | TASK-010 | **Hecho (TASK-010 4b):** clasificación jerárquica top-down acotada por el grafo (candidatos pequeños por nivel); selección por índice (barata en tokens); validación + confirmación; `EvaluationScorer` mide accuracy (precision/recall/F1/exact) contra REAs ya catalogados; RAG/embeddings diferido. Spec 2026-06-25-clasificador-ia-4b.md + design |
| Fiabilidad de la clasificación IA (robusta, degrada con elegancia, sin corromper el catálogo) | NFR-010 | ADR-0007 (propone/confirma) + ADR-0011 (capa de contexto) | TASK-010, TASK-011, TASK-015, TASK-016, TASK-017, TASK-018, TASK-019, TASK-020, TASK-026, TASK-031 | **NFR transversal a abordar en diseño/implementación del catalogador:** todo fallo del proveedor (HTTP≠2xx, timeout, JSON inválido) → error limpio y reintentable, sin escritura ni 5xx no controlado ni fuga de clave; propuesta validada contra el grafo antes de ofrecerse (nada sin confirmación); propose no atado a una petición síncrona que agote el proxy (**TASK-020** → Job/async, comparte infra con TASK-011); intercambio LLM trazable (`TraceableInterface`). Parcialmente cubierto (validación + propone/confirma ya implementados); **propose asíncrono HECHO (TASK-020, 2026-07-24):** el propose de 1 item corre como Omeka Job (PhpCli, 2º plano) + polling → ya no depende de una petición síncrona que agote el proxy. 185 tests host + verificación en contenedor (happy/error/cancel) | **TASK-026 (hecha 2026-07-26):** el tope de tiempo del transporte era de INACTIVIDAD por lectura (Socket + `stream_set_timeout`), no de la llamada: una llamada se fue a 10 min 19 s sin agotarlo. Ahora el adaptador **Curl** acota la llamada completa a 120 s y el corte se traduce a `LlmException`; además, ambos adaptadores detectan el error devuelto **con estado 2xx y el fallo en el cuerpo**, que antes se leía como respuesta vacía. **TASK-031(b) (hecha 2026-08-02):** «degradar con elegancia» también ante una **plataforma incompleta**, no solo ante el proveedor — sin la extensión `zip` de PHP el `new \ZipArchive()` lanzaba un `Error` que tumbaba el propose entero y se llevaba por delante los demás medios del item; ahora se salta ese medio con `zip_unsupported`, simétrico a `pdf_iconv_unsupported`, con la sonda inyectable y verificada dentro de la imagen sin la extensión.
| Accuracy de la clasificación IA (calidad medible por dimensión, sin alucinación) | NFR-011 | ADR-0010 (bottom-up, coherencia por construcción) + ADR-0009 (grafo) | TASK-010, TASK-015, TASK-016, TASK-017, TASK-018, TASK-019, TASK-022 | **NFR transversal a abordar en diseño/implementación del catalogador:** precisión/recall/F1 por dimensión contra REAs ya catalogados (`EvaluationScorer`/`aiEvaluate`), umbral objetivo lo fija el propietario; sin alucinación (toda etiqueta resuelve a item-término existente; dimensión sin encaje se omite, ADR-0010 §5); coherencia por construcción bottom-up sin degradar precisión. Complementa NFR-008 (objetivo de optimización accuracy↔tokens). Iteración continua de prompts en TASK-015/017/018/019; **TASK-022** aborda el destilador (`ContextDistiller`) guiado por casos reales — la ficha es la señal primaria de la cascada, calibrarla con few-shot + corpus de evaluación de REAs reales. **TASK-022 CERRADA (2026-08-02):** corpus medido entero — ZIP/SCORM en Alpine (2026-07-22) y PDF sobre la pila glibc con 3 repeticiones (2026-08-02, runbook §6); **acuerdo ficha↔referencia 9/9** en Tema y «Qué enseña», «Nivel citado textualmente» siempre literal y nunca inferido. **Lo que queda del lado del clasificador, no del destilador:** ese nivel citado **no poda la cascada** (un recurso que dice «1º ESO» recibe también 1º Bachillerato), medición pendiente en TASK-030 |
| Catalogar muchos recursos sin bloquear la UI | RF-011 | ADR-0007 + ADR-0008 | TASK-011, TASK-020 | Marcar varios items → Omeka Job en segundo plano; tarea aparte de TASK-004/010. **TASK-020 (abierta 2026-07-01):** el propose síncrono de UN item ya agota el timeout del proxy → **504** con modelos lentos (~7-9 llamadas LLM secuenciales por item tras ADR-0011); el 504 lo genera el proxy/php-fpm de Docker, no el módulo. Mitigación inmediata = modelos rápidos + `vision_enabled` off + timeout de proxy/PHP; arreglo de fondo = propose asíncrono como Job con polling, **comparte infra con TASK-011**. **TASK-020 HECHA (2026-07-24):** propose de 1 item asíncrono (Job PhpCli + polling, progreso/cancelar/reenganche); la infra de Job (`AiProposeJob`, `ProposalStore`, `ProposeRunner`) queda lista para reutilizar en el lote de TASK-011 | **TASK-026 (hecha 2026-07-26):** el rescate por visión de un PDF grande subía el binario original (28,4 MB → ~38 MB en base64) y dejaba el propose 10 min en una sola llamada devolviendo vacío; ahora se rasterizan en local las **4 primeras páginas** con Imagick y el mismo item baja a **33 s** con propuesta útil, lo que **retira la confirmación de TASK-025**.
| Aprovechar el contenido visual de los recursos | RF-012 | ADR-0007 | TASK-012 | Visión para imágenes en la extracción, activable desde la config; mejora posterior | **Hecha (TASK-012, cerrada 2026-07-27):** la entregó TASK-019 (ADR-0011) — `MediaVisionExtractor` con filtro heurístico puro (ruido por nombre, tamaño mínimo/máximo, top-N) y doble puerta: `oermanager_llm_vision_enabled` (apagado por defecto, es egress de binarios) + capacidad del proveedor. Afinada por TASK-025 y TASK-026 (el PDF se rasteriza en local en vez de subirse).
| Vocabulario de tipos de término enlazable (sin literales) | RF-013 | ADR-0004, ADR-0006 (addendum dcterms:type) | TASK-013 | `dcterms:type` literal → `skos:Concept`; el re-catalogador pasaría de filtrar `eq` a `res`; mejora posterior |
| Catalogación guiada por la estructura del currículo | RF-014, NFR-004 | ADR-0009 (grafo curricular LOMLOE) + ADR-0004/0006 | TASK-004 | Cascada Etapa→Curso→Asignatura→{Saberes, Criterios} acotando cada dimensión por el ancestro elegido; `dcterms:type` + enlaces del grafo; búsqueda incremental |
| No romper el core de Omeka | NFR-001 | ADR-0001 (guardrails como parte del gobierno) | TASK-008 | `git diff` del core vacío; hook PreToolUse bloquea escrituras fuera del repo |
| Portabilidad de datos (sin tablas propias) | NFR-002 | ADR-0002 (auditoría RDF nativa, sin excepción de tablas), ADR-0015 | TASK-007 | Sin entidades Doctrine propias; la auditoría usa value annotations `dcterms`, no tablas. La reversibilidad tampoco las necesita: el estado previo va como un valor RDF más sobre el item (ADR-0015), descartado el log a fichero justo por no viajar con el dato |
| Configuración del módulo accesible y restringida al rol adecuado | NFR-003, RF-002 | ADR-0013 | TASK-029 | **Hecha (2026-08-01).** La configuración deja el listado de Módulos (`module.ini: configurable = false`, una sola puerta) y cuelga del menú lateral en `admin/oer-manager/config`, con entrada de navegación que solo se pinta a quien puede entrar. La ACL pasa de **allow por controlador** a **allow por privilegio**, cerrando el roce con NFR-003 que registró ADR-0013 §8.3: curación (12 acciones) para `editor`+`site_admin`; `config` solo para `site_admin` y superior. Verificado en contenedor rol a rol (`test/container/acl-check.php`, 25/25): `editor` y el `site_editor` que añade IsolatedSites conservan la curación y NO entran a la configuración |
| Solo usuarios autorizados curan | NFR-003 | `[PENDIENTE]` | TASK-003, TASK-004, TASK-029 | Matriz rol×acción fijada en requirements.md: `editor`+ para visibilidad/alineamiento/tags, `global_admin`/`site_admin` para proyecto. **Desde TASK-029 se concede por privilegio, no por controlador**: una acción nueva ya no queda alcanzable por `editor` con solo escribirla |
| Gobierno del desarrollo trazable y verificable | — | ADR-0001 | TASK-001, TASK-008 | Esta matriz + hooks en `.claude/settings.json` validables (`jq`); lint = `make lint` (PSR-12), Stop hook diferido hasta que pase en limpio |
| Paquete publicable con licencia definida | NFR-007 | ADR-0003 | TASK-001 | `composer.json`/`config/module.ini` declaran `license = GPL-3.0-or-later`; paquete `ate/oer-manager` |
| Repo mantenible y verificable de forma automática (CI/CD, revisión, pruebas, releases) | NFR-009 | — | TASK-014 | **Hecha (2026-07-27):** `.github/workflows/` con `ci.yml` (composer + `make check-untranslated`/`compile-mo`/`lint`/`test` en PHP 8.4), `release.yml` (tags `v*.*.*` → `make package` + GitHub Release), `dependency-review.yml` (`composer audit`) y `pr-playground-preview.yml` + `blueprint.json`; patrón `omeka-s-IsolatedSites`; sin exponer secretos ni modificar el core. Pendiente el run real en un PR para cerrar | **Residuo saldado:** los workflows han corrido en verde sobre PRs reales — confirmado por el propietario en `e227edc` (PR#3 los fusionó, PR#8/PR#9 los ejercitaron) y reconfirmado en el **PR #15** (`CI / lint_and_test` en `pull_request` y `push`, `PR Playground Preview`, check de `dependabot.yml`). `release.yml` (tag `v*.*.*`) sigue pendiente de la primera release real.
| Coherencia del alineamiento por construcción (anclaje bottom-up, sin cascada frágil) | RF-009, RF-010, NFR-008 | ADR-0010 (anclaje bottom-up; revisa cascada top-down de ADR-0009/NFR-008) | TASK-015, TASK-016, TASK-017, TASK-018, TASK-019 | **En curso (TASK-015):** rework del clasificador con `searchSubjectFamilies`/`searchLeaves`; candidatos enriquecidos con `dcterms:description` (E1); derivación de curso+materia de las hojas elegidas (coherencia por construcción); E2 pre-filtro por bloque. **TASK-017 Fase 1 (hecha, verificada en contenedor):** extracción JSON genérica en `ContentExtractor` para que el contenido real del medio (SCORM/Netex) llegue al LLM. **TASK-018 (hecha 2026-06-30):** afinado de la Fase A de ADR-0010 — etapa y materia multi-select, hojas cross-etapa×materia con tope `LEAF_CAP`, criterios acotados a los cursos de los saberes. **TASK-019 (hecha en host, 2026-06-30; verif. contenedor diferida):** refactor de la capa de contexto al LLM — **ADR-0011** (dos modelos extracción/clasificación, destilador fiel, visión always-top-N + rescate PDF, contexto por paso, `ItemContext`); absorbe TASK-012/RF-012. **Fases 1-6 hechas** (120 tests, lint en verde): adaptador multimodal (`supportsImages()`/`supportsPdf()`, bloques `image`/`document`), `ItemContext` con enrutado por paso, `ContextDistiller` (ficha fiel sin inferir currículo) + 2º cliente LLM (`EXTRACTION_MODEL`), `MediaVisionExtractor` (filtro heurístico top-N + rescate de PDF escaneado, doble puerta `vision_enabled`/capacidad del proveedor), `OmekaMediaSource::imagesFor()` + `ConfigForm`/`Module` (`vision_enabled` off, `vision_max_images`) + glue. Pendiente solo la verificación funcional en contenedor. **TASK-019 afinado de la Fase A.1 (2026-07-03):** sesgo de inclusividad en la etapa acotadora (ante duda de nivel, seleccionar todas las etapas plausibles) para no perder saberes/criterios cercanos por una etapa omitida; aplicado solo a la etapa (no a materia/hojas, donde la precisión define `schema:about`/`lrmi:educationalLevel`); se descartó invertir el orden etapa↔materia (ADR-0010 §Afinado TASK-019). TASK-016 (embeddings) bloqueada por PEND-010 | **TASK-015 cerrada (2026-07-27, revisión de código):** estaba entregada desde TASK-018/019 y el estado se había quedado atrás — fases A.1/A.2/B/C, pre-filtro E2 por bloque con fallback, y curso/materia derivados de las hojas; 19 tests del clasificador.
| Resultados IA consistentes entre proveedores y entre ejecuciones (mismo modelo → misma catalogación) | NFR-008, NFR-010, NFR-011 | ADR-0012 (perfil de inferencia compartido; extiende ADR-0008/ADR-0011) | TASK-021 | **Hecha en host (2026-07-02):** `temperature` (vacía = no enviar) y `max_tokens` configurables una sola vez y enviados idénticos en todos los pasos por ambos adaptadores; reasoning off explícito **solo en OpenRouter** (`reasoning: {"effort":"none"}`); `llm_options` trazado por paso (panel de debug); 136 tests, lint en verde. Criterio de cierre: en contenedor, repetir el propose sobre los mismos items por ambos proveedores y comprobar que la tasa de acuerdo (ficha y selecciones) sube claramente; sin selecciones vacías por truncado. Divergencia aceptada y documentada: sin rescate de PDF vía gateway (`supportsPdf()=false`) |
| Un autor propone un REA candidato; un curador lo revisa, cataloga curricularmente si procede, y lo publica | RF-016, NFR-003 (ampliado) | ADR-0018 (roles nativos `author`/`reviewer`, `curation:status`/`curation:note` como literales simples, gate de publicación por `IntegrityChecker`) | TASK-037 (**hecha** 2026-08-30) | Diseño completo en `docs/superpowers/specs/2026-08-28-flujo-autor-curador-rea-design.md`. Verificado contra el core real: `author`/`reviewer` ya nativos con el scoping por `view-all` que necesita este flujo (`ResourceVisibilityFilter`, `AbstractResourceEntityAdapter::buildPropertyQuery()`) — un `author` ya solo ve lo suyo en cualquier listado admin, incluida la vista maestra propia del módulo, sin código nuevo; `curation:status`/`curation:note` ya instalados (vocabulario `Curation` del módulo `Access`), como literales simples y **no** `CustomVocab` (la máquina de estados compara por igualdad exacta; un valor editable sería un foot-gun). **Entregado y verificado en contenedor:** `WorkflowStatus` (máquina de estados pura, 7 tests) + `WorkflowService` (lee/escribe sobre el item real, sin test de host posible) + tres acciones nuevas en `IndexController` + ACL (`reviewer` añadido junto a `editor`/`site_admin`, privilegio `propose` abierto también a `author`) + botón en la página nativa del item (`view.show.page_actions`) + filtro «Propuestos» en la vista maestra. `test/container/workflow-check.php`: ciclo completo propose→reject→re-propose→publish→transiciones inválidas sobre un REA real (#3181), **14/14 OK**, con el título/descripción/alineamiento curricular verificados idénticos tras el ciclo (confirma que la corrección de la trampa de `ValueHydrator`, detectada y corregida en el propio texto del plan antes de implementar, funciona contra datos reales). **Extensión post-cierre (2026-08-31):** el filtro «Propuestos» pasa también a la barra rápida de la vista maestra (además de búsqueda avanzada), y el drawer de detalle gana rechazar/publicar por AJAX, sin salir de la vista maestra — detalle completo en `docs/backlog.md` (TASK-037). **Depende de PEND-012** (campos obligatorios de la plantilla REA, aún `[PENDIENTE]`) para que la creación del autor sea plenamente útil en producción; no bloquea lo ya entregado. **No verificado:** sesión real en el admin con navegador (crear `author`, proponer, revisar como `reviewer`, publicar) — deuda de TASK-030, mismo patrón que TASK-006/032/033/034 |
