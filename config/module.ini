; OERManager — metadatos del módulo (Omeka S >= 4.2)
; license: GPL-3.0-or-later (PEND-009 resuelto 2026-06-15, ADR-0003). author: aún sin confirmar por el propietario.
; version la gestiona `make package VERSION=x.y.z`; en el repo queda siempre 0.0.0.

[info]
name         = "OER Manager"
description  = "Management of a catalog of Open Educational Resources (lrmi:LearningResource items): curation, curricular alignment, integrity checks and statistics."
version      = "0.0.0"
license      = "GPL-3.0-or-later"
configurable = true
omeka_version_constraint = "^4.2.0"
